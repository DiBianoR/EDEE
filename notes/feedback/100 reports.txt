**Problem 1**


Scaffolding Usable?: Yes
Illustration Usable?: Probably not - super ugly and non-aesthetic
Other notes:
General Problem Class: aesthetics poor

**Post-Mortem Analysis: Aesthetic Failure on Cereal Box Illustration**

---

### What Went Wrong

1. **Over-Constrained Anchor Leakage:** The Python scaffolding introduced crude geometric placeholders (a literal half-circle `patches.Wedge` labeled "CEREAL", basic matplotlib hatch lines `//`, and rigid color blocks `#2B8CBE` / `#E6AB02`). Because the prompt commanded strict anchoring, the diffusion model attempted to faithfully preserve the blocky, dated CAD appearance rather than repainting an appealing commercial asset.
2. **"Style Clashing" in the Prompt:** The prompt attempted to blend flat technical math vectors, cabinet oblique projection, and "high-end casual mobile game art" with glossy banners and smooth gradients. This resulted in an uncanny, awkward graphic that looked like a decorated wireframe rather than cohesive textbook art.
3. **QA Leniency on Aesthetics:** The review stage (`image_verifier:review_aesthetics` and `issue_aggregator`) treated "aesthetics" as a checklist for the *absence of severe rendering glitches/artifacts* rather than evaluating actual visual appeal, balance, and textbook illustration quality.

---

### Involved Agents

* **`scaffolding_designer` & `coder` (Stage 3):** Introduced overly prescriptive, primitive graphic assets (semi-circle bowl + generic centered text) directly into the scaffolding instead of leaving open layout bounding boxes for the artist.
* **`artist` (`plan_finishing` / `render_final`) (Stage 4):** Locked onto the scaffold's primitive graphics and combined contradictory style instructions into a single prompt without overriding the crude placeholder aesthetic.
* **`image_verifier` (`review_aesthetics`) & `issue_aggregator` (Stage 5):** Failed as a quality gate by giving an automatic pass ("No issues found") simply because there were no blatant distortions, completely missing the poor visual quality.

---

### Root Causes in Agent Instructions

* **Instruction Contradiction between Standalone Scaffolding vs. AI Paint-Over:**
* The `STANDALONE RULE` mandates: *"a complete, legible diagram... good enough to ship as the final illustration if the artistic pass added nothing."*
* This forces the Python script to add rudimentary illustrations (like `patches.Wedge` cereal bowls). When fed to `img2img` with directives like *"lock the exact geometry... without expanding beyond bounding boxes,"* the diffusion model is trapped trying to beautify an inherently rigid Matplotlib graphic.


* **Over-Specified, Clashing Style Directives:**
* In `plan_finishing`, the agent is told: *"The style you should normally request is best described as [casual mobile game art]... crisp clean vector-style edges... on a clean white background."* Forcing game-art styling onto an oblique geometric projection often produces jarring, plastic-looking assets.


* **Superficial Aesthetic Review Rubric:**
* `review_aesthetics` only asks to *"Check for color cohesion, composition balance, clarity... and absence of generated artifacts."* It lacks criteria to judge overall artistic cohesion, typography integration, and commercial illustration polish.



---

### Suggestions for Systemic Improvement

* **Separate Mathematical Anchors from Illustrative Placeholders:**
* Update `design_scaffolding` and `write_code` instructions to draw **only pure mathematical scaffolding** (bounding boxes, axes, brackets, dividing planes, and dimension text). Instruct the coder explicitly **never** to attempt drawing decorative clip art (bowls, characters, items) in Matplotlib.
* In `plan_finishing`, instruct the artist to generate decorative subjects *inside* the bounding areas rather than trying to preserve rasterized Matplotlib polygons.


* **Align Projection Modes with Style Presets:**
* If a diagram requires strict mathematical projection (like oblique or orthographic), instruct `plan_finishing` to default to **flat, modern vector editorial style** (e.g., flat design, clean line art) rather than 3D/mobile-game gradients, which clash with flat technical projections.


* **Calibrate the Aesthetic Gatekeeper:**
* Refine `review_aesthetics` and `issue_aggregator` prompts to evaluate visual professionalism and style cohesion. Provide explicit grading criteria (e.g., "Does this look like an awkward paint-over of a CAD block, or a cohesive educational graphic?") to prevent rubber-stamping visually poor outputs.


Scaffolding Usable?: No - gives away fraction
Illustration Usable?: No - gives away fraction and is aesthetically the same as the scaffolding image
Other notes: 
General Problem Class: aesthetics poor, too much info

### Post-Mortem Analysis: Cereal Box 20% Increase Run

**Overall Pipeline Failure:** The generated diagram committed two critical errors: it gave away the underlying arithmetic structure (deconstructing the problem into 5 equal base segments plus 1 extra segment), and the final artistic pass failed to transform the raw Matplotlib scaffolding into an authentic illustration, outputting essentially the same flat CAD-like vector diagram.

---

### 1. What Went Wrong

* **Pedagogical Solution Leakage (Giving Away the Fraction):**
* The user query asked for a cereal box with the *"top 1/6 shaded."*
* Instead of drawing a solid cereal box with only a single dividing line at the top, the pipeline partitioned the entire vertical height into **6 discrete horizontal slices** (5 blue at the bottom, 1 yellow at the top).
* This explicit unit-grid decomposition handed the student the exact conceptual step needed to solve the problem (converting $20\%$ into $\frac{1}{5}$, yielding 5 base units $+ 1$ bonus unit $= 6$ total parts).


* **Aesthetic Stagnation (Scaffolding $\approx$ Final Illustration):**
* The final image is visually indistinguishable from the Python base render. It looks like an annotated CAD solid rather than a textbook illustration of a cereal box.
* The prompt constraints were so rigid that the image model simply reproduced the flat Matplotlib geometry, solid color fills, and programmatic curly brackets rather than painting a textured, stylized object.



---

### 2. Agents Involved

| Agent | Task | Failure Contribution |
| --- | --- | --- |
| **`layout_expert` & `educator**` | `plan_composition`, `enhance_clarity` | Invented the requirement to divide the box into 6 equal horizontal segments, misidentifying solution leakage as "clarity." |
| **`scaffolding_designer` & `coder**` | `design_scaffolding`, `write_code` | Hardcoded 5 interior divider lines (`y = [2, 4, 6, 8, 10]`) across the front and side faces. |
| **`reviewer` & `image_verifier**` | `math_check`, `review_math` | Checked only whether the final numerical answer ($22.2\text{ oz}$) was shown; praised the 6-slice grid as mathematically sound without evaluating pedagogical giveaway. |
| **`artist`** | `plan_finishing`, `render_final` | Over-anchored on geometric preservation and text locking, preventing any artistic texture, package branding, or stylistic rendering from emerging. |

---

### 3. Root Causes in Agent Instructions

* **Superficial Definition of "Giving Away the Answer":**
* Core Directive 3 states: *"They shouldn't give away the answer to the problem."*
* Agents interpret "the answer" strictly as the **final scalar output** (e.g., displaying $22.2\text{ oz}$). The instructions lack guidance warning against illustrating **intermediate reasoning steps** or pre-partitioning ratios into counting units.


* **Over-Engineering User Requests:**
* When the prompt requested "top 1/6 shaded," `layout_expert` and `educator` assumed the student needed visual proof of the fraction $\frac{1}{6}$, prompting them to draw all 6 divisions.


* **The "Two-Pass" img2img Paradox:**
* The instructions for `plan_finishing` require the agent to *"lock the exact geometry, aspect ratios... measuring lines, and text labels."*
* When Matplotlib renders flat polygons, text labels, and Bezier curly brackets, commanding an image model to lock all lines and text forces it to act as a vector photocopier rather than an illustrator.



---

### 4. Suggestions for Improvement

**A. Refine the Pedagogical Guardrail (Preventing Solution Leakage)**

* Add an explicit instruction to `educator`, `reviewer (math_check)`, and `image_verifier`:
> *"Do not illustrate intermediate solution mechanics. Diagrams should depict the initial scenario/givens, not pre-segmented unit fractions or bar-model solutions, unless explicitly requested."*


* Introduce a distinction between **Demarcation** (marking a single region boundary, like the top cap) and **Subdivision** (ticking/gridding the entire body into unit parts).

**B. Decouple Typography and Brackets from the Image Diffusion Pass**

* If annotations, curly brackets, and text are drawn in Matplotlib before img2img, the diffusion model either distorts them or gets locked into a flat vector style trying to preserve them.
* **Alternative Workflow:** Have Stage 3/4 produce only the visual subject (the 3D box), run the artistic pass to make it look like a real illustrated cereal box, and overlay vector brackets/text dynamically in post-processing.

**C. Loosen Negative Constraints in `plan_finishing` for Object Identity**

* When transforming primitive geometry into real-world objects, explicitly grant the artist permission to replace flat polygon faces with contextual textures (cardboard seams, package art, cereal graphics) while preserving only the primary bounding ratios.


Scaffolding Usable?: Yes
Illustration Usable?: Probably not - super ugly and non-aesthetic
Other notes: Don't want labels too close to edge
General Problem Class: aesthetics poor

### Post-Mortem Analysis: Ugly/Non-Aesthetic Render & Margin Crowding

---

**What Went Wrong**

* **Aesthetic Quality ("Super Ugly"):** The final illustration failed subjective aesthetic standards. The img2img prompt over-constrained the image generator to rigidly copy flat matplotlib polygons, crude diagonal line hatching (`//`), and blocky text. Rather than producing a modern, polished vector illustration, the output looked like an awkward, slightly smoothed version of a basic technical drawing.
* **Overly Lenient / Rubber-Stamping QA:** Stage 5 (`image_verifier` on aesthetics and `issue_aggregator`) gave the output an immediate pass with no critical feedback, praising a render that the user found visually unacceptable.
* **Edge Margin Crowding:** Text annotations on the right (`Original: 18.5 oz`, `+20%`) were positioned too close to the canvas boundary because the matplotlib script set `ax.set_xlim(-1, X_bracket + 5)` and the execution wrapper auto-cropped whitespace tightly against bounding boxes.

---

**Agents Involved & Failure Points**

| Stage | Agent & Task | Primary Failure Mode |
| --- | --- | --- |
| **Stage 3** | `scaffolding_designer` / `coder` | Injected literal hatch patterns (`hatch='//'`) and tight plot limits into the scaffold, locking coarse visual artifacts into the img2img base. |
| **Stage 4** | `artist` (`plan_finishing`) | Over-anchored on literal scaffold traits (commanding the AI to keep "diagonal hatching" and rigid polygon colors) instead of treating the scaffold as purely geometric bounding guides. |
| **Stage 5** | `image_verifier` (`review_aesthetics`) | Hallucinated high aesthetic quality ("exceptionally clean", "balanced") and failed to apply critical textbook standards. |
| **Stage 5** | `issue_aggregator` (`aggregate_feedback`) | Blindly accepted the verifier's clean bill of health without evaluating overall visual appeal or composition balance. |

---

**Root Causes in Agent Instructions**

* **Scaffolding Code Adding Final Styling:** Matplotlib scripts are drawing hatch marks (`hatch='//'`) and text decorations. When an img2img model sees harsh rasterized diagonal lines in the base image, it tries to preserve them as dirty, noisy artifacts rather than clean modern fills.
* **The "Anchor" Directive is Too Rigid on Non-Math Traits:** `plan_finishing` instructs the artist to *"lock the exact geometry... what stays the same, what gets added, what gets replaced"*. Because the artist saw hatching and flat polygons in the scaffold, it explicitly told the image model to reproduce *"subtle diagonal hatching"*, reinforcing ugly low-fidelity elements.
* **Lack of Concrete Aesthetic Criteria in QA:** `review_aesthetics` simply asks to *"Evaluate visual appeal"*. Without concrete negative patterns to fail (e.g., muddy raster lines, harsh CAD-style hatch marks, rigid flat polygons, cluttered annotations), vision models default to generic praise.
* **Execution Wrapper Cropping Without Padding Guarantees:** The execution contract specifies that the wrapper automatically *"crops surrounding whitespace"*. If the python script does not allocate explicit margin buffers around text objects, tight cropping places labels flush against the image border.

---

**Actionable Suggestions for Improvement**

* **Ban Hatching & Complex Fills in Base Diagram Coding:**
* Update `coder` and `scaffolding_designer` directives: *“Use only flat, solid, high-contrast block colors for scaffolding. Never use matplotlib hatch patterns (`/`, `\\`, `x`, `o`) to denote shading or regions, as these create noisy artifacts during image generation. Distinguish regions purely by solid color hue/value.”*


* **Separate Geometric Anchoring from Style Transfer in Stage 4:**
* Refine `plan_finishing` Directive 1 & 2: Explicitly state that flat colors, lines, and boxes in the scaffold represent *volumes, bounds, and layout*, not literal textures. The artist prompt should instruct the generator to replace flat solid fills with modern, soft gradient shading or professional vector materials rather than copying polygon boundaries literally.


* **Enforce Safe Margins in the Python Execution Contract:**
* Add a rule to `write_code` and `EXECUTION_CONTRACT`: *“Ensure a minimum 10–15% padding margin between the outermost labels/brackets and the axis limits (e.g., `ax.margins(0.15)`) so automatic whitespace cropping does not leave text flush against the image edge.”*


* **Recalibrate Aesthetic Review Standards in Stage 5:**
* Update `review_aesthetics` to explicitly flag:
* Harsh or messy lines resembling unpolished CAD / raw matplotlib output.
* Inconsistent or muddy texture shading (like smeared hatch marks).
* Text/brackets positioned within 5% of canvas borders.


* Require `review_aesthetics` to state at least one aesthetic critique before deciding whether it meets textbook publishing standards.


Scaffolding Usable?: Yes but rotation is weird
Illustration Usable?: No - Super ugly and shadow is messed up
Other notes: no shadow better than round shadow; artist can add shadow after scaffolding
General Problem Class: aesthetics poor, shadow wrong shape


**Executive Summary of Failure**

The pipeline successfully matched the 5:1 mathematical ratio (5/6 base vs. 1/6 bonus) using isometric projection, but produced an aesthetically failed illustration marked by awkward axis rotation and a distorted, ungrounded shadow.

---

**1. What Went Wrong**

* **Awkward Perspective / Rotation:** The coordinate projection used an extreme 3/4 isometric projection with $-30^\circ$ and $+30^\circ$ base angles, creating an unnatural, floating viewing angle for a standard upright cereal box.
* **Degraded Ground Shadow:** The base diagram plotted a hardcoded 2D parametric ellipse on the $Z=0$ plane (`w_circ = 1.5 + 1.8 * np.cos(theta)`). Projecting a rounded oval underneath an angular, polygonal prism caused the final AI paint pass to preserve and exaggerate an unnatural puddle shape rather than a coherent cast shadow.
* **Over-Specified Scaffolding Details:** The scaffolding locked in decorative packaging details (`CRUNCH!` text and a semi-circular bowl) in Matplotlib, forcing the downstream diffusion model to rigidly paint around awkward vectors instead of generating clean product packaging.

---

**2. Agents Involved**

* **`scaffolding_designer`:** Explicitly mandated adding an oval cast shadow (`#E0E0E0`) and heavy front-face graphics into the Python scaffolding blueprint.
* **`coder`:** Hardcoded an arbitrary parametric oval equation at $Z=0$, locking an ill-fitting geometry into the base image.
* **`artist` (`plan_finishing` & `render_final`):** Instructed the model to lock the oval shadow as an anchor rather than dropping or replacing it with an organic cast shadow.
* **`image_verifier` & `issue_aggregator`:** Passed the image with a clean aesthetic review despite noticeable visual artifacts in the grounding and packaging.

---

**3. Root Causes in Agent Instructions**

* **Scaffolding Scope Creep in `design_scaffolding`:**
* *Prompt Flaw:* Directives instruct the agent to abstract complex objects, but lack an explicit restriction against generating decorative lighting, ground shadows, or commercial graphic mockups in Python.
* *Consequence:* The agent treats Python/Matplotlib as an illustration canvas rather than a pure geometric skeleton.


* **Overly Rigid Geometry Anchor in `plan_finishing` / `render_final`:**
* *Prompt Flaw:* The artist is told to *“lock the exact geometry, aspect ratios, spatial relationships... of the base diagram”* without differentiating between **mathematical anchors** (box edges, proportions, measurement brackets) and **transient scaffolding cues** (ground shadows, placeholder text).
* *Consequence:* The diffusion model attempts to preserve the exact bounding box of the circular grey blob, producing a bizarre, deformed puddle.


* **Lack of Canonical Perspective Guidance for Everyday Objects:**
* *Prompt Flaw:* `visual_director` and `3d_specialist` default to full 3D isometric setups for any 3D entity, even when an orthographic front-facing view with subtle depth cues or standard oblique projection would provide a more natural textbook appearance.



---

**4. Suggestions for System Improvement**

| Target Agent / Component | Proposed Prompt / System Change | Potential Systemic Trade-off / Risk |
| --- | --- | --- |
| **`scaffolding_designer` & `coder**` | **Explicit Ban on Scaffolding Shadows:** Add a strict rule forbidding `coder` and `scaffolding_designer` from drawing cast shadows, ground planes, or ambient lighting in Matplotlib. Defer 100% of grounding and lighting to Stage 4. | Removing ground shadows entirely might make standalone scaffolding less visually complete if used unpainted on direct standard paths. |
| **`artist` (`plan_finishing`)** | **Selective Geometric Anchoring:** Refactor the prompt to distinguish between *Functional Anchors* (measurements, proportions, angles) and *Stylistic Placeholders* (shadows, background planes, mock logos). Instruct the artist to replace flat base placeholders with natural diffuse shading. | If the boundary between functional and stylistic elements is ambiguous, the artist might alter essential mathematical markers or text brackets. |
| **`visual_director` / `3d_specialist**` | **Default Aspect Ratios for Common Packaging:** Provide canonical viewing defaults for standard upright prisms (e.g., slight oblique/cabinet angle or gentle $15^\circ$ top-down tilt rather than full $\pm 30^\circ$ symmetric isometric rotations). | Rigid perspective presets may not generalize well to complex compound geometry or cross-sectional cuts. |
| **`image_verifier` (`review_aesthetics`)** | **Targeted Shadow & Grounding Checks:** Explicitly add shadow coherence and object grounding checks to the aesthetic review checklist to prevent deformed visual blobs from passing QA. | Reviewers might become overly sensitive to minor shadow falloffs and trigger unnecessary retry loops. |


Scaffolding Usable?: No - not shaped like cereal box
Illustration Usable?: NA, Unknown system crash
Other notes: 
General Problem Class: wrong shape

**Post-Mortem Analysis: Cereal Box Shape Failure & System Crash**

---

### What Went Wrong

1. **Incorrect Geometric Proportions (Scaffolding Failure):**
The base scaffolding failed to model the distinct aspect ratio and 3D cuboid/prism geometry of a standard rectangular cereal box (typically tall, wide, and narrow in depth, e.g., $\approx 8 \times 11 \times 2.5\text{ in}$). It generated an inaccurate shape (such as a generic cube or flattened 2D rectangle) that failed mathematical and physical plausibility.
2. **Upstream Acceptance & Downstream Crash:**
The review and inspection agents allowed the flawed geometry to pass downstream without enforcing realistic aspect ratios. Downstream, the execution pipeline hit an unhandled exception or missing artifact in cloud storage (`NoSuchKey` on `history.json`), causing an unrecoverable system crash before or during Stage 4/6.

---

### Key Agents Involved

| Agent | Role in Failure |
| --- | --- |
| `dimension_expert` / `layout_expert` | Failed to supply explicit length-width-height ratios for a cereal box. |
| `scaffolding_designer` | Stripped away realistic bounding-box proportions into an oversimplified primitive. |
| `coder` | Implemented incorrect vertex dimensions or omitted depth scaling in Matplotlib. |
| `reviewer` / `inspector` | Waved through unrealistic dimensions as "cosmetic" rather than flagging them. |
| `orchestrator` / `error_handler` | Failed to cleanly capture the failure before the pipeline aborted. |

---

### Root Causes in Agent Instructions

* **Ambiguous "Common Object" Directives:**
The prompt for `review_request` explicitly cites *"A 2' cubic packing box is BOTH... a drawing library can certainly generate a cube, but a picture of a cube is NOT a picture of a packing box"*, and `plan_finishing` cites *"if the image request asks for a box of cereal... you can make up a theme"*. This inadvertently encourages agents to offload basic dimensional accuracy to the AI artist rather than enforcing strict aspect ratios in Python scaffolding.
* **Lack of Mandatory Ratio Constraints for Real-World 3D Primitives:**
While `DIRECTIVE_3D` mentions estimating length, width, and height, `dimension_expert` and `scaffolding_designer` do not enforce bounding aspect-ratio bounding checks for standardized everyday packaging.
* **Leniency Drift in Inspection Managers:**
`consolidate_inspection` instructs the manager that *"Cosmetic blemishes the artistic pass will cover don't matter."* Inspectors frequently classify incorrect real-world proportions as "cosmetic" rather than structural failures.
* **Unprotected Archive Fetching on Pipeline Abort:**
A hard crash during execution bypassed standard state persistence, leaving an empty S3/GCS path where `history.json` was expected by downstream reporting.

---

### Systemic Improvement Suggestions

* **Hard-Pin Bounding Aspect Ratios in `scaffolding_designer`:**
Require `dimension_expert` and `scaffolding_designer` to output an explicit $(x : y : z)$ ratio tuple for any named real-world object. A cereal box blueprint must mandate $H \gg D$ to prevent cubes or flat planes.
* **Tighten "Cosmetic vs. Structural" Definitions in QA Prompts:**
Update `inspector` and `inspection_manager` instructions to explicitly state: *“Aspect ratio distortions or geometric misclassifications of real-world objects (e.g., drawing a cereal box as an equilateral cube) are MAJOR structural defects, not cosmetic imperfections.”*
* **Fallback Placeholder Generation on Pipeline Faults:**
Ensure the orchestrator's global error wrapper writes a minimal valid `history.json` stub prior to raising terminal exceptions, preventing broken archive references in downstream analytics.


Scaffolding Usable?: No - not shaped like cereal box
Illustration Usable?: No - not shaped like cereal box; ugly
Other notes: 
General Problem Class: aesthetics poor, wrong shape

### Summary of What Went Wrong

1. **Incorrect Shape (Squat Cube / Monolith instead of a Cereal Box):**
* Real cereal boxes are tall, wide, and thin (typical real-world aspect ratio: roughly **8" W × 12" H × 2.5–3" D**, or a normalized ratio of **2.7 : 4 : 1**).
* The scaffolding coder set `width = 4.0`, `total_height = 6.0`, and `dx = 1.2, dy = 1.2`. In a 45° oblique projection without foreshortening, `(dx, dy) = (1.2, 1.2)` creates an apparent depth of $\sqrt{1.2^2 + 1.2^2} \approx 1.70$.
* Resulting normalized ratio was **4.0 W : 6.0 H : 1.7 D** (or **2.35 : 3.5 : 1**). Combined with a square figure box (`8x8`) and heavy top/side oblique panels, the object looked like a thick, squat toy block or packaging cube rather than a standard cereal box.


2. **Poor Aesthetics ("Ugly" & Clunky Visuals):**
* The coder hard-coded a massive, clunky 2D Matplotlib wedge and ellipse for a cereal bowl (`r=0.9`, bright yellow/white) plus randomly scattered scatter-dots directly onto the front face.
* Because Stage 4 uses strict **context-preserving image-to-image / ControlNet conditioning** (`render_final` was told: *"Strictly maintain the exact oblique 3D projection... morph primitives into described objects WITHOUT expanding beyond bounding boxes"*), the AI artist locked onto the blocky layout, thick flat polygons, and crude bowl placement, resulting in a stiff, unappealing hybrid render.



---

### Agents Involved

* **`dimension_expert`:** Provided realistic absolute dimensions (*12" H × 8" W × 3" D*), but only as free text. It failed to output a strict, normalized ratio for the visual modelers.
* **`image_detail_planner` (`merge_plans` & `review_description`):** Hardcoded a 2:3 ($W:H$) front-face ratio into `latest_description`, ignoring depth thickness relative to width/height and cementing a stubby profile.
* **`scaffolding_designer` & `coder`:** Hardcoded dimensions ($W=4, H=6, D=1.7$) and drew literal 2D clip-art patches (bowl, dots) directly on the scaffolding canvas instead of keeping the scaffolding clean and geometric.
* **`artist` (`plan_finishing` & `render_final`):** Over-constrained the img2img prompt by strictly locking every primitive boundary, forcing the generative model to render a rigid, blocky toy carton rather than a natural, polished product package.
* **Reviewers (`reviewer`, `inspector`, `inspection_manager`, `image_verifier`):** Suffered from tunnel vision on math/annotation alignment (getting bogged down by bracket extension lines) while completely ignoring basic object silhouette realism and overall visual appeal.

---

### Root Causes in Agent Instructions

1. **No Normalized Aspect Ratio Enforcement in Stage 2/3 Blueprinting:**
* `dimension_expert` instructions state: *"You deal with real-world units or aspect ratios of objects in the image... not the image size/shape itself."* It outputs arbitrary strings like `"12x8x3 inches"`, but `scaffolding_designer` and `coder` translate this arbitrarily into code coordinates without a normalized coordinate bounding box $(W : H : D)$.


2. **Over-Scaffolding / Premature Illustration by the Coder:**
* The coder prompt instructs: *"Primitives: If complex objects... are needed, plan to use simplified placeholders, shapes..."*
* This encourages the Python script to attempt crude vector art (e.g., drawing breakfast bowls with Matplotlib wedges). Because Stage 4 locks onto base geometries, these vector blobs pollute the img2img generator.


3. **Over-Constrained img2img Anchoring Directives in Stage 4:**
* `DIRECTIVE 1 (THE ANCHOR)` in `plan_finishing` demands: *"lock the exact geometry, aspect ratios, spatial relationships... do not warp straight lines."*
* When the underlying scaffolding has poor proportions or crude placeholders, this directive prevents the image model from adding natural bevels, packaging tapers, or realistic lighting and textures.


4. **Adherence Checklist Prioritizes Metric Verification Over Semantic Plausibility:**
* `verify_adherence` and `review_aesthetics` prompts instruct agents to check labels, ticks, and color contrast, but lack a basic semantic check: *"Does this object look authentic and recognizable as the real-world item it represents?"*



---

### Suggestions for Improvement

```
Stage 2 (Dimensions)          Stage 3 (Scaffolding)                 Stage 4 (Artistic)
 ┌──────────────────────┐      ┌───────────────────────────────┐     ┌────────────────────────┐
 │ Explicit Normalized  │ ───► │ Pure Geometry Only            │───► │ Semantic Freedom:      │
 │ Aspect Ratio Spec    │      │ (No Matplotlib clip-art/bowls)│     │ Morph placeholders     │
 └──────────────────────┘      └───────────────────────────────┘     └────────────────────────┘

```

1. **Require Normalized Aspect Ratio Envelopes in Stage 2:**
* Update `dimension_expert` schema to require a structured `aspect_ratio_3d` object (e.g., `{"width": 2.7, "height": 4.0, "depth": 1.0}`).
* Update `scaffolding_designer` to strictly consume these normalized ratios when setting up 3D vertices and oblique receding vectors ($dx, dy = \frac{1}{2} \times \text{depth} \times \cos(45^\circ)$).


2. **Ban Decorative "Clip-Art" in Python Scaffolding:**
* Amend `coder` and `scaffolding_designer` system prompts:
> *"Do not draw complex decorative subjects (e.g., cereal bowls, animals, logos) using Matplotlib primitives. The base diagram must contain ONLY the mathematical geometry, bounding regions, division lines, and dimension markup. Leave decorative illustration entirely to the Stage 4 artist."*




3. **Loosen Non-Mathematical Bounding Anchors in Stage 4:**
* In `plan_finishing` and `render_final`, distinguish between **Mathematical Anchors** (the 5/6 vs. 1/6 height split line, dimension brackets, numerical text) and **Semantic Fill Zones** (the front face branding, product artwork).
* Allow the diffusion model creative freedom to texture and illustrate inside the bounding box without clamping to internal Matplotlib vector shapes.


4. **Add Realism / Silhouette Checks to Inspection Passes:**
* In `verify_adherence` and `detect_artifacts`, add an explicit check:
> *"Object Realism: Does the proportions and silhouette match the real-world object (e.g., does a cereal box look tall and slender rather than a flat cube)?"*


Scaffolding Usable?: Yes
Illustration Usable?: Probably not - super ugly and non-aesthetic
Other notes: 
General Problem Class: aesthetics poor


### Post-Mortem Analysis: Aesthetic Failure on Cereal Box Diagram

---

### 1. What Went Wrong

1. **"Frankenstein" Visual Style (Mobile Game Asset + Math Diagram):** The final render attempted to paint a hyper-glossy, high-end mobile-game-style ceramic bowl directly onto a flat, stark geometric prism. This created an awkward clash between casual game assets and dry textbook vector geometry.
2. **Glaring Color Inconsistency on 3D Sub-Volume:** While the front and top of the 1/6 bonus section were colored amber/yellow, the right-side receding face remained teal (matching the base 5/6 block). This gave the top extension an unfinished, broken appearance.
3. **Over-Indexing on Text Preservation at the Cost of Visual Quality:** The gatekeeper (`issue_aggregator`) waved through poor aesthetics solely because the OCR/text generation happened to render legibly on the first try, creating a fear-driven bottleneck where ugly outputs pass as long as the text doesn't hallucinate.

---

### 2. Agents Involved

| Agent | Role in Failure |
| --- | --- |
| **`artist` (`plan_finishing` / `render_final`)** | Injected conflicting visual metaphors ("casual mobile game art" alongside flat vector diagrams) and failed to explicitly bind the right-side polygon color of the top volume to the amber/yellow color group. |
| **`image_verifier` (`review_aesthetics`)** | Flagged the broken color grouping on the 3D face as merely `[MINOR]`, minimizing a visible rendering artifact. |
| **`issue_aggregator` (`aggregate_feedback`)** | Overruled aesthetic QA entirely out of fear that a diffusion retry would degrade the clean text labels (`18.5 oz`, `20% more`, `? oz`). |
| **`inspection_manager` & `reviewer**` | Induced an unnecessary code retry cycle upstream by misjudging aspect ratio and coordinate offsets, anchoring the system's attention on technical trivia rather than overall visual composition. |

---

### 3. Root Causes in Agent Instructions

* **Style Contradiction in Injected Directives (`activeStyle` vs. Task Core):**
* `plan_finishing` forces a style described as *"heavily inspired by high-end casual mobile game art"*.
* Combining "mobile game art" with `GLOBAL_TASK_EXPLANATION` directive #2 (*"clean, high-contrast, readable, and free of clutter"*) causes image models to paste overly rendered 3D clip-art (e.g., hyper-detailed glossy bowls) onto stark flat-shaded diagram skeletons.


* **Lack of Face-by-Face Color Binding in Oblique 3D Prompts:**
* In `plan_finishing`, the prompt instructed: *"The upper small section (1/6 volume) must remain vibrant yellow/amber"*. Image diffusion models process 2D spatial patches, not 3D manifold geometry; without explicitly declaring *"all visible faces of the top block (front, top, AND right-side receding face) must share the identical amber/yellow palette"*, color leakage across boundaries is inevitable.


* **Asymmetric Risk Incentives in `issue_aggregator`:**
* Because generating clean, ungarbled text inside standard diffusion passes is historically high-variance, the gatekeeper's directive (*"pass a good-enough image rather than quibble over minor details"*) creates a perverse incentive: it will accept visually atrocious art to protect a lucky text generation roll.



---

### 4. Systematic Suggestions for Improvement

* **Decouple Diagrammatic Objects from Clip-Art Embellishment:**
* **Instruction Patch (`artist` / `plan_finishing`):** Remove game-style descriptors when working with geometric word problems. Replace with: *"Consistent flat vector textbook infographic style with clean, subtle cell-shading and unified material properties across all geometric faces."*


* **Explicit Face-Group Color Rules for 3D Geometry:**
* **Instruction Patch (`scaffolding_designer` & `artist`):** When describing segmented 3D prisms, require explicit color groups by volumetric component: `Block A (Front, Top, Side) = Color 1; Block B (Front, Top, Side) = Color 2`.


* **Separate Text Markup from Diffusion Generation (Composition Layering):**
* Rather than forcing the AI image model to draw text labels and brackets during the img2img diffusion step (which causes the gatekeeper to fear retrying bad art), use Python/Matplotlib strictly to render the final background and objects, or burn the text/brackets as an SVG overlay *after* the artistic pass.


* **Tune Aesthetic Severity Thresholds in `image_verifier`:**
* Inconsistent color blocking across contiguous geometric planes in a math diagram should be classified as a **MAJOR** aesthetic defect rather than a MINOR one, triggering an automated targeted prompt adjustment.


Scaffolding Usable?: No - not shaped like cereal box
Illustration Usable?: No - not shaped like cereal box and shadow is messed up
Other notes: round shadow on a box is worse than no shadow; artist can add shadow after scaffolding step
General Problem Class: wrong shape, shadow wrong shape

Here is the post-mortem analysis breaking down the failure modes, responsible agents, prompt root causes, and systematic suggestions.

---

### What Went Wrong

1. **Incorrect Aspect Ratio & Form Factor (Not Shaped Like a Cereal Box):**
* Real standard cereal boxes have an approximate aspect ratio of roughly **$7.5\text{–}8.5\text{ in (W)} \times 2\text{–}2.5\text{ in (D)} \times 11.5\text{–}12.5\text{ in (H)}$**, giving a characteristic tall, wide, and very shallow profile ($\text{Width} : \text{Depth} \approx 3.5:1$ to $4:1$).
* The coder used `WIDTH = 6`, `DEPTH = 3`, `TOTAL_HEIGHT = 12`. With depth scaled by 0.5 at $45^\circ$, the projected depth was overly thick, and the front face was too narrow ($\text{Width} : \text{Depth} = 2:1$, $\text{Height} : \text{Width} = 2:1$), making the rendered shape look like a blocky package, shipping carton, or pillar rather than a cereal box.
* Because the pipeline enforced strict geometry preservation in Stage 4, the artist faithfully rendered this bulky container without correcting the silhouette.


2. **Illogical Geometric Grounding Shadow:**
* An elliptical/round radial shadow (`Ellipse(width=7, height=1.5)`) was hard-coded directly under the base of a rectangular prism.
* Flat-bottomed rectangular solids cast polygonal/skewed quadrilateral shadows under directional lighting, or sharp planar ambient occlusion under orthographic setups. An oval shadow under a box creates a floating or mismatched physical artifact.



---

### Involved Agents & Failure Points

| Agent | Task | Where It Failed |
| --- | --- | --- |
| **`dimension_expert`** | `estimate_dimensions` | Proposed $8 \times 3 \times 12\text{ in}$, which is already slightly too deep for a standard cereal box ($8 \times 2\text{ to }2.5 \times 12$ is typical). |
| **`scaffolding_designer`** | `design_scaffolding` | Overrode realistic dimensions, compressing width down to `Width = 6, Depth = 3, Height = 12`. Explicitly mandated an `"oval grey ellipse/shadow under the box base"`. |
| **`coder`** | `plan_logic` & `write_code` | Implemented the `Ellipse` patch directly and locked in the $6 \times 3 \times 12$ proportions without questioning if the resulting footprint matches a cereal box. |
| **`inspector` / `inspection_manager**` | `detect_artifacts` / `consolidate_inspection` | Verified the oval shadow as correct adherence to the blueprint rather than recognizing it as a physical/geometric rendering defect. |
| **`artist`** | `plan_finishing` & `render_final` | Instructed to lock the base diagram's exact proportions and shadow, cementing the bulky shape and flawed shadow into the final AI render. |

---

### Root Causes in Agent Instructions

* **Explicitly Directing Scaffolding to Draw Shadows:**
* `DIRECTIVE_3D` instructs: *"Analyze the scene for 3D logic. Ensure depth cues (shading, perspective) are defined."*
* Scaffolding designers interpret this as a mandate to draw shadows in Matplotlib. However, Matplotlib primitive scripts struggle with realistic cast shadow projection, resulting in crude ellipses that the downstream diffusion model treats as strict geometric anchors.


* **Lack of Concrete Real-World Aspect Ratio Anchors:**
* While `dimension_expert` has a directive to estimate real-world units, agents downstream (`scaffolding_designer`, `coder`) frequently adjust numbers to clean round integers ($6, 12, 3$) for easier coordinate math, destroying the real-world object's silhouette.


* **Reviewers Checking Adherence over Physical Plausibility:**
* The QA inspectors check if the code followed the blueprint (`scaffolding_blueprint` asked for an oval shadow $\rightarrow$ coder drew an oval shadow $\rightarrow$ passed). There is no explicit negative check for **non-functional decorative additions** that should belong exclusively to Stage 4.



---

### Systematic Suggestions for Improvement

* **Outsource Lighting and Shadows to Stage 4 (Artist Pass):**
* Update `EXECUTION_CONTRACT` and `scaffolding_designer` directives:
> *"Do NOT plot ground shadows, ambient occlusion ovals, or lighting gradients in Python scaffolding unless the shadow length/shape is an explicit measured variable in the math problem. Let the artistic rendering stage handle realistic grounding shadows and contact occlusion."*




* **Anchor Common Object Aspect Ratios & Prevent Math-Convenient Distortions:**
* Add a guideline to `scaffolding_designer` and `dimension_expert`:
> *"Preserve realistic aspect ratios from the dimension estimate. Do not alter width-to-depth or height-to-width proportions just to achieve convenient round numbers for plotting coordinates."*




* **Separate Structural Scaffolding from Decorative Embellishments:**
* Reinforce the **Stand-Alone Rule** vs. **Over-Scaffolding**: Matplotlib should strictly plot what provides *mathematical utility* (proportions, boundaries, measuring brackets, text labels). Purely aesthetic cues (decorative box text, fake shadows, fake logo outlines) pollute the ControlNet/img2img conditioning and constrain the artist's ability to render natural objects.


Scaffolding Usable?: No - not shaped like cereal box
Illustration Usable?: No - not shaped like cereal box
Other notes: 
General Problem Class: wrong shape

**Executive Summary**

The user rejected both the scaffolding and final illustration because the generated 3D rectangular prism was not shaped like a real-world cereal box. Instead of looking like a tall, relatively thin grocery item (standard ratio roughly 8:3:12 or 2.7:1:4 for Width:Depth:Height), it ended up rendered with a 4:1:6 aspect ratio in an oblique cabinet projection with equal horizontal and vertical depth offsets ($dx=1.0, dy=1.0$), making the box appear overly squat, thick, blocky, and unnatural.

---

**What Went Wrong**

1. **Aspect Ratio & Dimension Degradation Across Pipeline Transitions:**
* In Stage 2, `dimension_expert` proposed realistic dimensions ($8 \times 3 \times 12$ inches, ratio **$2.67 : 1 : 4$**).
* `image_detail_planner` summarized this into a vague "approximate 2:3 width-to-height aspect ratio" for the front face without pinning down depth.
* `scaffolding_designer` translated this into arbitrary Python plot coordinates: **Width = 4.0, Total Height = 6.0, $dx=1.0, dy=1.0$** ($W:H = 2:3$).
* In oblique projection, setting $dx=1.0, dy=1.0$ across a width of 4.0 and height of 6.0 produces a depth that visually looks like 25% of the width and 17% of the height. Combined with a wide $2:3$ front face, the box looks like an oversized shipping carton or generic cube block rather than a slim cereal box (which typically has a front face ratio closer to $1 : 1.5$ to $1 : 1.7$ and a very narrow side profile).


2. **Review Blind Spots:**
* Reviewers and inspectors focused almost exclusively on the **1/6 vs. 5/6 vertical fraction ratio** and green-lit the shape because the mathematical partition was correct, completely overlooking real-world object plausibility.



---

**Agents Involved**

* **`dimension_expert`**: Provided reasonable absolute measurements ($8'' \times 3'' \times 12''$), but did not enforce normalized aspect ratios or emphasize the thin side profile.
* **`image_detail_planner` (`merge_plans`, `review_description`)**: Stripped the 3D depth context when synthesizing findings and simplified the front face to a generic "2:3 rectangle."
* **`scaffolding_designer`**: Chose arbitrary coordinate dimensions (`Width = 4.0, Height = 6.0, dx=1.0, dy=1.0`) that exaggerated width and depth relative to height.
* **`reviewer` & `inspector` (`syntax_check`, `logic_check`, `detect_artifacts`, `verify_adherence`)**: Checked solely against the geometric blueprint's numbers rather than questioning whether the blueprint captured a plausible cereal box.

---

**Root Causes in Agent Instructions**

| Agent / Directive | Instruction Gap |
| --- | --- |
| **`DIRECTIVE_3D`** | Tells agents to "estimate length, width, height... and ensure aspect ratio is similar," but provides no guidance on defining explicit **normalized coordinate ratios $(W:D:H)$** that subsequent stages must strictly maintain. |
| **`image_detail_planner` (`merge_plans`)** | Focuses heavily on 2D mathematical safety and label suppression, losing the 3D proportion constraints established by the `dimension_expert`. |
| **`scaffolding_designer`** | Lacks an instruction requiring explicit parameterization of normalized $(W, D, H)$ bounding boxes based on the `dimension_expert` profile rather than picking arbitrary integer coordinates. |
| **`inspector` (`detect_artifacts`, `verify_adherence`)** | Lacks a dedicated check for *object plausibility/real-world realism*. It strictly verifies internal consistency against the blueprint rather than against the real-world semantic object. |

---

**Suggestions for Improvement & System Mitigations**

* **Standardize Dimension Specification as Normalized Ratios:**
* Require `dimension_expert` to output explicit normalized bounding-box ratios (e.g., `Normalized Aspect Ratio (W:D:H) = [1.0 : 0.35 : 1.6]`) in its structured output schema, rather than freeform text strings like `"8x3x12 inches"`.
* Pass this structured tuple directly through to `scaffolding_designer` and `coder` so coordinates are computed directly from the aspect ratio ($X = W \cdot scale, Y = H \cdot scale, dx = D \cdot \cos(\theta) \cdot scale$).


* **Tighten Oblique Projection Formula Guidelines:**
* Update `DIRECTIVE_3D` with explicit standard cabinet projection parameter ranges:
* Standard cabinet projection should use $dx = \frac{D}{2} \cos(30^\circ \text{ or } 45^\circ)$, $dy = \frac{D}{2} \sin(30^\circ \text{ or } 45^\circ)$.
* Explicitly warn against equal $dx=1, dy=1$ shorthand unless the object's depth profile actually warrants it.




* **Add Semantic Object Plausibility to Inspector Checklists:**
* In `verify_adherence` and `detect_artifacts`, add a specific check: *"Does the overall aspect ratio and 3D silhouette look like the actual real-world object named in the prompt (e.g., cereal box, soda can, shipping box), or is it distorted into a generic cube/block?"*


* **Cross-Impact Consideration (Trend Analysis):**
* *Watch out:* Over-constraining aspect ratios to ultra-slender real-world profiles (e.g., very thin cereal boxes) can compress text space for annotations like `"18.5 oz"` or `"+20%"`, causing label-overlap failures in `check_overlaps`. The balance is ensuring the front face stays tall enough ($H/W \approx 1.5 - 1.8$) while keeping the depth face thin ($D/W \approx 0.25 - 0.35$).


Scaffolding Usable?: No - gives extra information
Illustration Usable?: No - gives extra information
Other notes: figuring out that 20% is 1/6 is part of the trick to the problem!
General Problem Class: too much info 

### Post-Mortem Analysis: "Too Much Info" (Premature Solution Leakage)

---

### What Went Wrong

The diagram generated explicit visual scaffolding or annotations that gave away an essential mathematical deduction required by the student—specifically revealing or directly visualizing a fractional equivalence (such as visually constructing a precise 1/6 division or labeling an explicit partition corresponding to the 20% trick).

Instead of serving as a neutral problem setup (contextual scaffolding), the diagram functioned as a solution map, violating **Core Directive 3 (Utility)**: *"They shouldn't give away the answer to the problem."*

---

### Agents Involved

| Agent | Role | Failure Mode |
| --- | --- | --- |
| **`educator`** (`enhance_clarity`) | Stage 2 Pedagogical Review | Failed to identify that making the representation too explicit removes the central mathematical challenge of the problem. |
| **`image_detail_planner`** (`merge_plans`, `review_description`) | Stage 2 Management | Approved a refined description containing derived/computed intermediate states instead of given parameters. |
| **`scaffolding_designer`** (`design_scaffolding`) | Stage 3 Blueprinting | Over-specified geometric partitions/markings rather than relying on abstract, non-revealing primitives. |
| **`reviewer`** (`math_check`) | Stage 3 Code Review | Checked only whether the math was *factually correct*, not whether it *prematurely exposed the student's problem-solving step*. |

---

### Root Causes in Agent Instructions

* **Literal Alignment over Information Hiding:** `math_check` instructs: *"Verify the MATHEMATICS of the diagram... Recompute every quantity the code hard-codes... Do they match what the problem's math actually implies?"* This directs the reviewer to ensure mathematical fidelity to the *solution*, rather than checking whether the diagram visualizes *unrevealed deductions*.
* **Vague Guidance in `enhance_clarity`:** The instruction notes *"don't do the student's work for them, or give the answer to the problem"*, but lacks operational definitions for **intermediate insights**, **hidden ratios**, or **deductive steps**. Agents often interpret "the answer" purely as the final numerical output string rather than intermediate structural breakthroughs.
* **Over-Scaffolding in `design_scaffolding`:** Directive 1 instructs: *"What counts, shapes, graphs, grids, lines, or angles are part of the problem and must be perfectly accurate? These must be plotted."* This encourages plotting the *underlying underlying mathematical structure* even when the problem requires the student to construct or deduce that exact structure themselves.

---

### Systemic Suggestions & Trade-off Analysis

**1. Introduce Explicit Information-Boundary Constraints to `design_scaffolding**`

* **Action:** Update the task instruction to differentiate between **Given Entities** and **Target Deductions/Calculated Equivalencies**.
* **Instruction Addition:** *"Plot ONLY given initial values, explicit entities, and context. Do NOT divide shapes into solution fractions, plot intermediate computed values, or label derived equivalencies (e.g., if a problem requires discovering a ratio or fraction, show the whole entity or generic placeholders, not the resolved sub-partitions)."*
* **Trade-off / Risk:** If over-enforced, diagrams for simple geometry problems (e.g., "Find the hypotenuse of a 3-4-5 right triangle") might omit standard scaffolding like right-angle squares or given side measurements, reducing diagram utility.

**2. Add a Dedicated "Giveaway Check" to `educator` & `reviewer` (`math_check`)**

* **Action:** Add a mandatory schema boolean and check: `reveals_intermediate_solution`.
* **Instruction Addition:** *"DEDUCTION AUDIT: Does the visual representation explicitly display the core realization/insight needed to solve the problem (e.g., revealing a hidden fraction, drawing auxiliary lines that trivialise a proof, or highlighting the solution partition)? Mark FAIL if the diagram does the deductive thinking for the student."*
* **Trade-off / Risk:** Evaluator agents may produce false positives on standard visual models (like bar models or unit squares) designed to teach problem representations.

**3. Formalize "Given vs. Deduced" Geometry Classes in Stage 2**

* **Action:** In `review_request` or `situational_planning`, classify whether the problem relies on **Visual Deduction** (where visual division must remain ambiguous/neutral) versus **Direct Representation** (where explicit visual counts/dimensions are given directly in the prompt text).
* **Trade-off / Risk:** Adds another categorization layer to Stage 2, slightly increasing latency and complexity for straightforward diagram requests.


Problem 1 User Notes:
- we should be able to do better completely transforming with img2img; Gemini is capable of impressive transformations with the right prompt.
- Might be worth recording the dimensions explicitly in a required field of merge_plans. In fact anything we're pretty sure we need it to explicitly save should come in a required field. Maybe after the summary between "plan_response" & "final_polish"
- the scaffolding definitely needs to work as the textbook image; some teachers prefer these. But the artist can add in things like shadows; half-assing that was a mistake



**Problem 2**


Scaffolding Usable?: Maybe - don't like dotted line between mice
Illustration Usable?: Maybe - don't like dotted line between mice; mice kind of ugly and have no legs
Other notes: I can see theres 2 mice, the label is redundant
General Problem Class: too much info


### What Went Wrong

1. **Unsolicited Structural Markup (Dotted Divider & Redundant Labels):** The pipeline introduced structural dividers (`vertical dashed line`) and explicit counting labels (`'6 Brown Mice'`, `'2 White Mice'`) that the user never requested. Because 2 white mice and 6 brown mice are visually obvious at a glance, labeling the count cluttered the figure and insulted the reader's intelligence.
2. **Deficient Biological Abstraction in Scaffolding:** The scaffolding primitives defined a mouse as merely a horizontal ellipse body, an ear, an eye, and a tail, completely omitting legs/paws or realistic snout tapering.
3. **Over-Constrained Image-to-Image Anchor:** During Stage 4, the artist was explicitly commanded to lock the bounding boxes and strictly preserve every element from the base diagram. This forced the final diffusion pass to retain the dotted line, redundant text headers, and legless "blob" silhouettes.

---

### Key Agents Involved

* **`markup_specialist`:** Initiated the addition of explicit group labels and dividing lines under the guise of "accessibility."
* **`image_detail_planner`:** Attempted to reject text labels in `conflict_resolution_notes`, but reintroduced the vertical divider and explicit grid definitions into `latest_description`.
* **`scaffolding_designer` & `coder`:** Hard-coded the dotted line at $x=6.0$, created the `'6 Brown Mice'` / `'2 White Mice'` text labels, and modeled the mouse primitive without legs.
* **`artist` (`plan_finishing`):** Anchored the redundant text labels and divider line as immutable mathematical constraints (`THE ANCHOR`), preventing the generative model from polishing them away.

---

### Root Causes in Agent Instructions

| Failure Area | Root Cause in Instructions |
| --- | --- |
| **Dotted Divider & Labels** | `plan_markings` prompts: *"What labels, if any, would this sort of diagram normally have?"* This leads the agent to invent labels to justify its turn. In `merge_plans`, the directive *"Don't add extraneous labels..."* only mentioned labels, leaving dividing lines and boundary boxes unrestricted. |
| **Ugly/Legless Scaffolding** | `DIRECTIVE_PRIMITIVES` states: *"If in doubt, fall back to circles to denote approximate size and location."* Furthermore, `design_scaffolding` dictates: *"Strip away all the artistic flair... describe ONLY the geometric shapes... The artist can add details later."* |
| **Flaws Locked into Final Render** | `plan_finishing` Directive 1: *"Explicitly instruct the Artist AI to lock the exact geometry, aspect ratios, spatial relationships, measuring lines, and text labels of the base diagram."* When combined with Directive 2 (*"WITHOUT expanding beyond their original bounding boxes"*), the artist is barred from adding legs, snouts, or removing structural scaffold lines. |

---

### Strategic Suggestions & System Trade-Offs

**1. Negative Constraint on Redundant Counting Labels & Dividers**

* **Change:** Update `plan_markings` and `image_detail_planner` to enforce a "Minimal Markup Policy": If an object count is under 10 and objects are visually distinct by color/shape, hard-forbid explicit count labels and artificial divider lines unless explicitly requested in `original_query`.
* *Trade-Off / Risk:* For complex charts or multi-class sets ($\ge 4$ classes), omitting category labels can decrease readability for color-blind students or younger grade levels.

**2. Decouple "Mathematical Anchor" from "Artistic Scaffolding" in Stage 4**

* **Change:** In `plan_finishing` and `render_final`, clarify that the scaffold's bounding boxes define **approximate subject placement and centerpoints**, not rigid silhouettes. Allow the artist to organically extend silhouettes (adding paws, limbs, snouts) while preserving centroid locations and scale ratios.
* *Trade-Off / Risk:* Loosening the bounding-box constraint increases the likelihood of diffusion models hallucinating merged limbs, overlapping items, or warped geometric angles in strict geometry tasks (e.g., tangent circles or right triangles).

**3. Categorize Scaffolding Elements into "Structural" vs. "Disposable Guides"**

* **Change:** Instruct `scaffolding_designer` and `coder` to mark non-essential layout aids (like grid lines, dividers, and reference placeholders) in a distinct light key (e.g., `#E0E0E0` guide lines) and command the `artist` to paint *over* or eliminate guide lines rather than preserving them.
* *Trade-Off / Risk:* The artist pass might accidentally erase genuine mathematical lines (such as coordinate axes, function curves, or true geometric segment dividers) if the prompt fails to distinguish between a layout guide and a problem constraint.


Scaffolding Usable?: Maybe - would rather mice be mixed together
Illustration Usable?: Maybe - would rather mice be mixed together; shadows are wrong
Other notes: i don't think round shadows look bad on the mice; but artist can add nice shadows(or not) after scaffolding. there don't need to be boxes around them, they don't need to be arranged in grids, and i can SEE tehres 6 mice ans 2 mice, I know how to count. And even if I didn't it's a math class I'm supposed to learn, not have an AI count for me.
General Problem Class: too much info

### What Went Wrong

The pipeline suffered from severe **over-structuring and information pre-digestion ("Too Much Info")**:

* **Over-Grouping & Segmentation:** Instead of drawing a natural set of 8 mice (6 brown, 2 white) scattered or loosely grouped together, the pipeline segregated them into two separate container cards/boxes.
* **Redundant / Answer-Spoiling Markings:** Explicit labels (`Brown (6)` and `White (2)`) were placed under the panels. This pre-counts the items and spoils the counting/observation exercise for a math student.
* **Rigid Grid Layout:** The mice were forced into mechanical $2 \times 3$ and $1 \times 2$ grid arrays rather than natural placements.
* **Render Artifacts:** The artist pass generated awkward round/pill-shaped drop shadows underneath the primitives instead of natural grounding shadows.

---

### Agents Involved

| Agent | Task | Failure Contribution |
| --- | --- | --- |
| `markup_specialist` | `plan_markings` | Proposed explicit category labels (`Brown Mice (6)` and `White Mice (2)`), ignoring the prompt's instruction to evaluate whether labels were actually necessary. |
| `arrangement_planner` | `plan_object_arrangement` | Introduced the strict separation into two isolated groupings ($2 \times 3$ grid on left, $1 \times 2$ row on right) with a large separator gap. |
| `artistic_planner` | `artistic_planning` | Introduced the rounded container panels/cards behind each group and prescribed explicit text typography for the count labels. |
| `image_detail_planner` | `merge_plans` & `review_description` | **Primary failure point:** Explicitly overrode the layout expert's mixed grid in favor of separated panels; only trimmed `"Brown Mice (6)"` to `"Brown (6)"` instead of deleting the redundant labels entirely; codified the $60\%/35\%$ two-panel canvas split. |
| `educator` | `enhance_clarity` | Failed to enforce the core directive: *"don't do the student's work for them, or give the answer to the problem."* |

---

### Root Causes in Agent Instructions

* **Managerial Over-Enthusiasm for Categorization (`image_detail_planner`):**
* In `merge_plans`, the prompt warns: *"Don't add extraneous labels... A group of mice doesn't need to be labeled 'mice'."* The agent interpreted this narrowly (shortening the label string) rather than recognizing that **count labels on countable objects spoil educational utility**.
* The prompt lacks an explicit directive against **pre-sorting / segregating items** when the problem involves counting, ratios, or sample spaces.


* **Misaligned Heuristic for Count Accuracy (`arrangement_planner` & `image_detail_planner`):**
* Agents defaulted to rigid grids and separate boxes as a defensive mechanism to ensure programmatic accuracy and prevent AI counting errors, sacrificing natural illustration quality.


* **Permissive Clarity Checks (`educator` & `review_description`):**
* `enhance_clarity` and `review_description` ask *"Does the figure do the student's job for them?"*, but LLMs routinely pass count labels because they perceive explicit labels as "clear" rather than "pedagogically damaging."


* **Shadow Specification Vacuum (`plan_finishing` & `render_final`):**
* Scaffolding primitives (flat ellipses) have no elevation or contact geometry. The `plan_finishing` prompt instructed the model to *"morph those primitives into the described objects WITHOUT expanding beyond their bounding boxes,"* leading the diffusion model to interpret the bottom bounding edge as a flat contact boundary and render pill-like blob shadows.



---

### Suggestions for Systemic Improvement

**1. Add an Explicit "No Pre-Counting / No Answer Spoiling" Directive in `plan_markings` and `image_detail_planner**`

* **Change:** Add a rule: *"When the mathematical task or visual request involves counting, proportions, or probability distributions of discrete objects, NEVER display the count as a label or group title (e.g., do not write 'Mice (6)'). Let the student count the visual objects themselves."*

**2. Establish Natural Composition Defaults for Discrete Objects (`DIRECTIVE_PRIMITIVES` & `arrangement_planner`)**

* **Change:** Update primitive composition rules: *"Avoid clinical grid matrices and isolated container boxes unless specifically requested (e.g., ten-frames or array multiplication). For sets, clusters, or probability contexts, prefer naturalistic, organic clustering (loose groupings, mixed arrangements, slight jitter in position and orientation) with level ground planes."*

**3. Calibrate the Tradeoff Between Count Control and Visual Naturalism**

* *Risk Analysis:* Loosening grid constraints can increase the risk of primitive overlap or miscounts during the Matplotlib scaffolding stage.
* *Mitigation:* Have `arrangement_planner` define **organic non-overlapping coordinate clusters** (e.g., Poisson disc sampling / jittered points with collision margins) rather than rigid Cartesian grids.

**4. Explicit Shadow & Grounding Instructions in Stage 4 (`plan_finishing`)**

* **Change:** In `plan_finishing`, add grounding guidance: *"Render natural contact and directional cast shadows grounded on the floor plane. Do not draw isolated dark ovals or floating pill shadows directly under floating primitives."*


Scaffolding Usable?: Maybe - would rather mice be mixed together
Illustration Usable?: No - don't like dotted line between mice; mice kind of ugly and have no legs
Other notes: labels saying 2&6 mice are redundant
General Problem Class: aesthetics poor

Here is the post-mortem analysis of the run based on the user feedback, tracing the aesthetic and compositional failures back through the agent chain and instruction prompts.

---

### 1. What Went Wrong

1. **Separation Instead of Mixing (Composition / Educational Framing):**
* The user wanted the mice mixed together (reflecting a natural or randomized litter of mice for a probability simulation).
* The pipeline rigidly separated the population into two isolated groups: a 2×3 grid on the left and a 1×2 column on the right.


2. **Redundant & Cluttered Text Labels:**
* Text labels (`6 Brown Mice` and `2 White Mice`) were added beneath the groups.
* For an elementary counting problem where the counts are immediately obvious (6 and 2), hard-coded text labels stating the quantities add clutter and defeat the visual purpose.


3. **Aesthetic & Anatomical Defects (Ugly, Legless Mice):**
* The base diagram simplified each mouse into an ellipse body, an ear, an eye, a nose dot, and a tail—completely omitting legs.
* Because the downstream image generation used strict ControlNet / img2img anchoring (`"lock the exact geometry"`, `"morph those primitives WITHOUT expanding beyond their original bounding boxes"`), the AI artist painted directly over the legless oval silhouettes, resulting in legless, blob-like final illustrations.


4. **Artifacts (Dotted Line / Divider Artifacts):**
* An unintended boundary/dotted line appeared between the two groups during the image synthesis pass, likely induced by the strong vertical spatial gap and separate label alignments.



---

### 2. Agents Involved

| Agent | Task | Direct Impact on Failure |
| --- | --- | --- |
| **`educator` & `layout_expert**` | `enhance_clarity`, `plan_composition` | Prematurely insisted on rigid segregated clustering ("two clear groups or rows: 6 brown in one group and 2 white in another") rather than a natural mixed arrangement. |
| **`markup_specialist`** | `plan_markings` | Prescribed redundant labels (`"6 Brown Mice"` / `"2 White Mice"`), assuming every entity group warrants text annotations. |
| **`scaffolding_designer` & `coder**` | `design_scaffolding`, `write_code` | Decomposed mice into legless composite primitives (horizontal ellipses + tails). |
| **`artist`** | `plan_finishing`, `render_final` | Constrained by rigid negative prompts prohibiting drawing outside bounding boxes, locking in the legless, blobby look of the scaffolding primitives. |
| **`image_verifier` & `issue_aggregator**` | `review_aesthetics`, `aggregate_feedback` | Gave 100% aesthetic passes because they verified presence and alignment rather than judging natural anatomical appeal or excessive compartmentalization. |

---

### 3. Root Causes in Agent Instructions

* **Overzealous Labeling & Grouping in Upstream Stage 2:**
* `markup_specialist` instructions ask: *"What labels, if any, would this sort of diagram normally have?"* This leads the model to default to adding labels. Even though `merge_plans` contains a warning (*"A group of mice doesn't need to be labeled 'mice'"*), the planner justified it as reinforcing numerical constraints.
* `layout_expert` and `educator` bias toward rigid grids and categorized clusters rather than organic or mixed distributions unless explicitly instructed.


* **Primitive Oversimplification in Stage 3:**
* `DIRECTIVE_PRIMITIVES` suggests *"Break down complex objects into geometric primitives (e.g., 'circles for cats')… If in doubt, fall back to circles to denote approximate size and location."*
* When a mouse is reduced strictly to an ellipse, the scaffolding coder generates a pure oval.


* **Over-Constrained img2img Anchoring in Stage 4:**
* `plan_finishing` explicitly commands the artist: *"morph those primitives into the described objects WITHOUT expanding beyond their original bounding boxes or center points"* and *"lock the exact geometry."*
* This prevents the diffusion model from adding essential anatomical features like legs, paws, or realistic posture, trapping the output in the silhouette of the primitive.



---

### 4. Systematic Suggestions & Tradeoff Considerations

| Suggested Change | Intended Fix | Potential Risk / Tradeoff to Monitor |
| --- | --- | --- |
| **1. Relax Bounding Box Restrictions on Organic Primitives (`plan_finishing`)**<br>

<br>Allow the artist model to add appendages (legs, paws, ears) outward from placeholder centroids, treating simple primitives as position/scale anchors rather than hard silhouettes. | Eliminates "legless blob" characters; yields natural, high-quality character anatomy. | If relaxed too much, the image model might hallucinate extra objects or distort precise spacing and alignment. |
| **2. Default to Mixed/Organic Layouts for Probability & Counting Sets (`layout_expert` / `educator`)**<br>

<br>Add explicit guidance that for probability/sampling word problems, objects should be naturally mingled/dispersed (while remaining distinct and non-overlapping) unless segregated categories are explicitly requested. | Prevents artificial grid segregation and avoids visual dividers between classes. | May increase the burden on `inspector` to verify that overlapping does not occur in random or scattered layouts. |
| **3. Stricter Gate on Redundant Text Markups (`markup_specialist` / `merge_plans`)**<br>

<br>Enforce a rule: *Do not generate count/name labels if the total object count is under 10 and visually identifiable at a glance.* | Removes clutter and avoids redundant labels (`"6 Brown Mice"`). | Could occasionally omit helpful clarifying labels on complex, multi-variable diagrams. |
| **4. Broaden Anatomical & Realism QA in `image_verifier` (`review_aesthetics`)**<br>

<br>Instruct the verifier to explicitly audit living subjects for missing limbs, unnatural silhouettes, or artificial partitioning artifacts. | Catches deformed, legless, or aesthetically unappealing renderings before final sign-off. | Increases review rejections/retry loop costs if the model flags minor stylized cartoon abstractions as defects. |


Scaffolding Usable?: Maybe - would rather mice be mixed together
Illustration Usable?: No - mice kind of ugly and have no legs
Other notes: those labels make it seem like special ed, insulting
General Problem Class: aesthetics poor, too much info

**Post-Mortem Analysis & Failure Breakdown**

---

### What Went Wrong

1. **Unnecessary Informational Scaffolding (Pedagogical Hand-Holding):**
* The pipeline generated explicit, high-visibility text banners (`"Brown Fur: 6"` and `"White Fur: 2"`) directly above each group.
* This cluttered the canvas, spoon-fed the counts, and made the visual feel patronizing rather than an intuitive illustrative supplement to a word problem.


2. **Artificial & Rigid Grouping:**
* Separating the mice into two distinct, segregated sub-grids (2x3 on the left, 2x1 on the right) destroyed the natural feel of a litter or random sample, clashing with the intuitive premise of a genetics experiment.


3. **Severe Anatomical & Aesthetic Deficiencies:**
* The mice lacked basic anatomy (limbs, feet/paws), resulting in limbless "blob/bean" silhouettes with tails attached.
* The artist pass faithfully anchored to the rudimentary 2D ellipse primitives without adding necessary organic features.


4. **Upstream Environment Knowledge Mismatch (Inspection Loop Waste):**
* The `inspection_manager` burned multiple retry cycles penalizing the coder for omitting `ax.axis('off')` and `ax.set_aspect('equal')`, directly violating the engine's `EXECUTION_CONTRACT` where the wrapper injects those post-execution.



---

### Involved Agents

* **`markup_specialist` & `image_detail_planner`:** Introduced and cemented the idea of adding explicit group count headers (`"Brown Fur: 6"`, `"White Fur: 2"`), directly violating Core Directives #2 (Clarity/Uncluttered) and #3 (Utility without giving away answers/over-simplifying).
* **`layout_expert` & `arrangement_planner`:** Defaulted to rigid, segregated tabular sub-grids rather than naturalistic or mixed clusters.
* **`scaffolding_designer` & `coder`:** Over-simplified the mouse morphology to pure floating ellipsoids with zero limb/paw geometry.
* **`artist`:** Interpreted the anchor constraint too conservatively by failing to extrapolate limbs/feet onto the base ellipses during image-to-image refinement.
* **`image_verifier` & `issue_aggregator`:** Rubber-stamped the final render without checking for organic plausibility, aesthetic realism, or over-labeling.
* **`inspection_manager`:** Lacked awareness of the `EXECUTION_CONTRACT`, hallucinating non-compliance on axis stripping and aspect ratio.

---

### Root Causes in Agent Instructions

| Failure Domain | Agent / Prompt Location | Root Cause in System / Task Instructions |
| --- | --- | --- |
| **Over-Labeling** | `markup_specialist` (`plan_markings`) | Prompts ask *"What labels would this diagram normally have?"* without a strong negative bias against counting/trivial identity labels. |
| **Rigid Layouts** | `layout_expert` / `arrangement_planner` | Directive says *"think about real-world environments"*, but lacks guidance on natural scatter vs. math tables for discrete probability populations. |
| **Missing Anatomy** | `scaffolding_designer` / `artist` | `scaffolding_designer` is told to *"abstract complex objects to simple primitives"*, while `artist` is told *"do not expand beyond original bounding boxes"*, trapping the image model into limbless blobs. |
| **False QA Loops** | `inspection_manager` | Lacks the `EXECUTION_CONTRACT` block present in `reviewer` and `review_manager`, causing it to flag missing wrapper-handled calls. |

---

### Suggestions for Pipeline Improvement

* **Inject `EXECUTION_CONTRACT` into `inspection_manager`:** Ensure QA managers evaluate the script under the identical wrapper contract as the coder and reviewers, preventing spurious rejections over `ax.axis('off')`.
* **Add a Strict "Anti-Trivial-Labeling" Directive to `plan_markings`:** Explicitly instruct the markup specialist: *"Never add labels that merely state the identity, count, or obvious color of objects (e.g., 'Brown: 6'). Restrict labels strictly to measured geometry, coordinates, or necessary problem variables."*
* **Loosen Morphological Freedom for `artist` (Anchor vs. Embellishment):** Update the stage 4 prompt to clarify that anchoring applies to **spatial centroid, scale, count, and color palette**, while biological subjects MUST receive natural anatomical features (paws, limbs, posture) beyond the bare geometric primitive.
* **Introduce a `distribution_style` Parameter in `plan_composition`:** Allow the layout planner to distinguish between **structured data matrices** (coordinate grids, bar charts) and **naturalistic populations** (mixed litters, scattered collections, V-formations) to avoid artificial segregation of sample sets.


Scaffolding Usable?: No - Mouse morphology too bizzare; would rather be mixed together
Illustration Usable?: YES!!
Other notes: 
General Problem Class: 

### Post-Mortem Analysis

**General Problem Class:** Discrete Object Counts & Organic Entity Scaffolding (Counting / Probability Arrays)

---

### What Went Wrong

1. **Bizarre Geometric Scaffolding (Unusable Standalone):** The generated Matplotlib scaffolding failed the system's "STANDALONE RULE" (the requirement that a scaffold be legible enough to ship on its own). The programmatic mouse primitives (stacked ellipses, circles, Bezier tails, and detached paws) resulted in an uncanny, fragmented creature rather than a clean icon.
2. **Artificial & Clustered Sorting (Lack of Natural Mixing):** Rather than distributing the mice naturally or in a mixed arrangement to represent a random sample/litter, the pipeline segregated them rigidly (Row 1: all brown; Row 2: 2 brown, 2 white).
3. **Over-Specification in Scaffolding Design:** The scaffolding designer attempted to programmatically construct detailed organic anatomy (snout coordinates, ear borders, paws) in Matplotlib instead of placing simplified bounding boxes/primitives, forcing the Python coder to write brittle patch geometry.

---

### Agents Involved

* **`scaffolding_designer` & `coder`:** Over-engineered the anatomy with raw Matplotlib patches rather than using simple placeholder silhouettes or utilizing available primitive PNGs (`cat_primitive.png` / asset loading).
* **`arrangement_planner` & `layout_expert`:** Defaulted to a sorted, segregated 2x4 grid rather than considering an educational, randomized/interleaved distribution (e.g., alternating or naturally dispersed to reflect probability).
* **`scaffolding_manager` / `selector`:** Selected `STANDARD_DIAGRAM` with custom code instead of checking if `COMPOSITE_PRIMITIVES` (pre-rendered icon assets/stamps) was better suited for organic counting sets.

---

### Root Causes in Agent Instructions

| Issue | Agent / Directive | Root Cause in Prompt / System Identity |
| --- | --- | --- |
| **Bizarre Morphology** | `scaffolding_designer` & `DIRECTIVE_PRIMITIVES` | The directive advises: *"Break down complex objects into geometric primitives (e.g., 'circles for cats')… If an object can be modeled precisely by a few simple primitives, use them."* The agent interprets "model precisely" as assembling complex multi-part creatures out of raw ellipses and circles rather than using abstract bounding marks. |
| **Segregated Layout** | `plan_composition` & `plan_object_arrangement` | Prompts prioritize counting ease (*"effortless counting", "orderly grid"*) without instructing agents to consider whether a mathematical scenario (like random genetic sampling or probability trials) benefits from an **interleaved/mixed distribution** versus a **sorted set**. |
| **Premature Pass of Scaffold** | `inspector` & `inspection_manager` | The inspection prompt emphasizes checking if shapes match the blueprint rather than evaluating if the resulting primitives look like recognizable standalone objects. |

---

### Suggestions for Improvement

* **Establish an Asset/Primitive Library Hierarchy:**
Explicitly instruct `scaffolding_designer` and `coder` that complex organic entities (animals, people, vehicles) should **never** be assembled from >3 geometric patches from scratch. They should either:
* Load a clean pre-made vector/PNG silhouette (`plt.imread`), or
* Use a single bounding shape (e.g., a colored circle/capsule placeholder with an icon label) when serving strictly as scaffolding.


* **Add "Sampling vs. Categorization" Layout Guidance:**
Update `layout_expert` and `arrangement_planner` instructions with a directive on probability problems:
* *When depicting random trials, outcomes, or litters, interleave/mix categories (e.g., Brown, White, Brown, Brown...) rather than sorting all identical classes together, unless pure classification/tallying is explicitly requested.*


* **Clarify Standalone vs. Pure Scaffolding Roles:**
If an image is destined for Stage 4 ControlNet/img2img and the final output is consistently great (as seen here), relax the requirement that Matplotlib draw anatomical features. Let Python define **spatial centers, bounding boxes, and color zones**, and let the generative model handle 100% of organic anatomy.


Scaffolding Usable?: No - Mouse morphology too bizzare; shadows are wrong; would rather be mixed together
Illustration Usable?: No - they look like dogs, their shadows are wrong; would rather they be mixed together
Other notes: I don't think aesthetics on finished img are that bad. we dont need numbers of mice labeled though
General Problem Class: aesthetics poor, too much info

### Post-Mortem Analysis & Executive Summary

The failure on this run stems from a mismatch between **procedural geometric abstraction** and **natural biological illustration**, compounded by prompt instructions that over-segregated the visual layout and over-labeled obvious visual counts.

---

### What Went Wrong

* **Bizarre Mouse Morphology & Dog-Like Render:** The composite primitive defined in Python used an awkward stack of disjointed shapes (large horizontal ellipse body, high circle head, pink inner circle ear, flat Bezier tail). Because the `artist` agent was strictly instructed to *lock geometry and stay within bounding boxes*, the generative model was forced to skin this unnatural scaffolding rather than drawing natural rodent anatomy, resulting in dog/rodent hybrid features.
* **Flawed Drop Shadows:** The Python code rendered hardcoded ellipse shadows at fixed offsets `(x, y - 0.75)` directly below the body ellipse. Because they were baked into the base diagram, the img2img pass preserved them as disconnected floating artifacts rather than realistic ambient occlusion.
* **Artificial Spatial Segregation:** The mice were split into rigid, separate grids (a 3×2 block of brown mice on the left and a 1×2 block of white mice on the right). A realistic or engaging textbook illustration should show a natural mix of brown and white mice within a single group.
* **Unnecessary & Redundant Labeling:** The labels (`6 Brown Mice`, `2 White Mice`) were spoon-fed to the viewer, cluttering the diagram and defeating the purpose of having students observe or count the population themselves.
* **QA False Positives on `ax.axis('off')`:** Multiple review agents flagged the missing `ax.axis('off')` as a potential defect, completely ignoring the `[EXECUTION ENVIRONMENT]` contract which explicitly states the wrapper strips axes post-execution.

---

### Agents Involved & Root Causes

| Agent | Action / Failure | Root Cause in Prompt / Directives |
| --- | --- | --- |
| **`markup_specialist`** | Injected explicit category count labels (`6 Brown Mice`, `2 White Mice`). | Instruction asks *"What labels, if any, would this sort of diagram normally have?"* without firmly enforcing a high threshold for omitting self-evident counts. |
| **`image_detail_planner`** | Approved labels and altered the layout to rigidly separate brown and white mice into separate spatial blocks. | Prioritized avoiding "ambiguity" for the labels over natural, mixed grouping and real-world biological composition. |
| **`scaffolding_designer` & `coder**` | Built a rigid primitive out of stacked geometric patches (ellipses/circles) plus static oval drop shadows. | The `COMPOSITION & PRIMITIVE CONSTRAINTS` directive instructs agents to *"break down complex objects into geometric primitives (e.g., 'circles for cats')"*, which produces stiff, unnatural skeletons for organic creatures. |
| **`artist` (`plan_finishing`)** | Strictly commanded the AI image generator to preserve every bounding box and line from the primitive plot. | *Directive 1 (THE ANCHOR)* explicitly mandates locking exact bounding boxes and geometry, preventing the diffusion model from correcting awkward underlying anatomy. |
| **`inspection_manager` & `final_reporter**` | Waved through bizarre morphology while hallucinating code omissions (flagging `ax.axis('off')`). | Managers focused on checkbox adherence (counts match, labels match) rather than biological plausibility and ignored the execution contract. |

---

### Key Takeaways & Trade-Offs

**1. Scaffolding Depth vs. Artistic Flexibility (The "Over-Constrained Anatomy" Trap)**

* *The Problem:* Geometric scaffolding works well for rigid physics diagrams and geometric constructions, but forces organic subjects (animals, humans) into uncanny valley silhouettes.
* *The Trade-off:* If you allow the artist to deviate from the scaffold bounding boxes, count fidelity can drift on high object counts ($>5$). If you lock bounding boxes strictly, organic shapes look deformed.

**2. Over-Clustering vs. Natural Blending**

* *The Problem:* The planner grouped identical classes together purely to align text labels cleanly underneath them.
* *The Trade-off:* Cleanly separated groups make labeling easier, but destroy the realism and visual appeal of natural scenarios (e.g., a litter of baby mice).

---

### Systemic Improvement Suggestions

* **Deprecate Complex Multi-Patch Animal Primitives:**
* For organic subjects in `COMPOSITE_PRIMITIVES`, instruct `scaffolding_designer` to place **single bounding boxes/ellipses with center coordinates and orientation vectors**, or load standardized silhouette PNG assets (`cat_primitive.png`, `mouse_primitive.png`), rather than constructing awkward Frankenstein shapes from Matplotlib patches.


* **Remove Artificial Shadows from Scaffolding Code:**
* Prohibit `coder` from drawing drop shadow patches in Matplotlib. Shadowing, ambient occlusion, and lighting should be handled entirely by the `artist` pass in Stage 4.


* **Enforce Strict Label Minimization:**
* Update `plan_markings` and `image_detail_planner` instructions with an explicit negative rule: *“Do NOT add count labels (e.g., '6 Brown Mice') for distinct countable objects unless the problem specifically demands a numbered reference key. Let the illustration represent the objects directly.”*


* **Relax Strict Silhouette Clamping for Organic Forms in `plan_finishing`:**
* Instruct the artist prompt planner to treat organic scaffolding primitives as **loose spatial anchors and scale references**, explicitly granting the diffusion model license to blend and refine anatomical silhouettes.


* **Reinforce Execution Environment Spec in QA Manager Prompts:**
* Add a reminder to `inspection_manager` that axis stripping and whitespace cropping are handled by the execution harness, preventing wasted tokens and false warnings about missing `ax.axis('off')`.


Scaffolding Usable?: No - Brown Fur White fur labels are misleading
Illustration Usable?: No - Brown Fur White fur labels are misleading; do not look like mice; no legs
Other notes: the labels don't really line up with the groups even.
General Problem Class: aesthetics poor, too much info

### Summary & Post-Mortem Analysis

---

### 1. What Went Wrong

* **Misaligned and Ambiguous Group Labeling:** Placing `"Brown fur"` under columns 1–2 and `"White fur"` under columns 3–4 of the bottom row created major visual ambiguity. A student naturally reads column-wise or row-wise: it was unclear whether `"Brown fur"` applied only to the two mice above it in Row 2, the column pair across both rows, or all 6 brown mice.
* **Over-Specification & Unnecessary Markup ("Too Much Info"):** The problem merely asked to illustrate *"6 brown and 2 white mice."* Adding text labels was entirely redundant—the coat colors are self-evident. The labels cluttered the visual and introduced cognitive confusion.
* **Anatomical & Aesthetic Deficiencies:** Morphing basic geometric primitives (ellipses and circles) without specifying anatomical grounding led the artist model to produce legless, floating "blob" mice with poor physical fidelity.
* **Flawed Grid Partitioning:** Splitting an 8-item set (6 brown, 2 white) into an unsegmented 2×4 grid meant brown mice occupied the entire top row plus half the bottom row. Without distinct spatial grouping (e.g., separate clusters or distinct containers/panels), the 6:2 split looked disjointed.

---

### 2. Agents Involved

| Agent | Role in Failure |
| --- | --- |
| `markup_specialist` | Proactively invented unnecessary category labels (`"Brown fur"` / `"White fur"`) for self-evident visual attributes. |
| `layout_expert` & `arrangement_planner` | Designed an unsegmented 2×4 grid that forced a 6:2 split across row boundaries without structural separation. |
| `image_detail_planner` (`merge_plans`) | Failed to enforce its directive (*"Don't add extraneous labels just because the markup specialist wants to"*), rubber-stamping the labels into the blueprint. |
| `coder` & `scaffolding_designer` | Constructed abstract primitives without feet/limbs, creating a legless anchor that the img2img model carried over. |
| `image_verifier` & `issue_aggregator` | Evaluated checklist compliance rather than visual coherence, failing to catch the anatomical flaws or confusing label alignment. |

---

### 3. Root Causes in Agent Instructions

* **Self-Justifying Specialization Bias:** The `markup_specialist` prompt asks *"What labels, if any, would this sort of diagram normally have?"* This implicitly nudges the model to invent labels even when objects are visually distinct.
* **Lax Gatekeeping on Redundant Text:** The `image_detail_planner` prompt advises against extraneous labels in theory, but lacks an explicit negative rule barring labels that merely restate self-evident visual attributes (e.g., color, basic shape).
* **Grid-First Assumption vs. Natural Grouping:** Composition planners default to uniform rectangular grids (2×4) rather than cluster-based or partitioned layouts (e.g., two distinct pens/groups: a group of 6 and a group of 2).
* **Primitive Blueprint Lacks Limb Anchoring:** The primitive blueprint specified ellipses for bodies, circles for ears/heads, and paths for tails, but omitted limb/grounding geometry. The downstream img2img model locked onto this legless silhouette.
* **QA Vision Tunnel Vision:** Verification tasks (`review_math`, `review_aesthetics`, `aggregate_feedback`) checked whether text was legible and counts matched, but did not evaluate layout semantics (e.g., *"Does this label unambiguously map to its intended target?"* or *"Are organic subjects anatomically plausible?"*).

---

### 4. Systematic Improvement Suggestions

**Refine the `markup_specialist` & `image_detail_planner` Prompts (Negative Rules)**

* Add an explicit negative constraint: *Do not add text labels for intrinsic visual properties (color, shape, object identity) unless the math problem explicitly requires reading a label (e.g., variable names $x$, measurement scales, axes).*
* Direct the `image_detail_planner` to reject purely descriptive labels on countable object sets during `merge_plans`.

**Improve Grouping Logic in `plan_composition` / `plan_object_arrangement**`

* When displaying categorical splits (e.g., $A:B$), instruct planners to favor **cluster-based layouts** (two visually separated groups with clear whitespace or subtle background cards) over continuous, wrapped grids that break a category across rows.

**Enforce Grounding & Limb Primitives for Animals in `design_scaffolding**`

* Update composite primitive guidelines: when abstracting four-legged animals or characters, include basic base/limb anchor points or a grounding line so the image generator does not produce floating blobs.

**Add Grouping & Label-Mapping Checks to QA Prompts**

* In `check_overlaps` and `verify_adherence`, add a specific verification rule: *If group labels or brackets are used, verify that they enclose or point to their entire target set without ambiguity.*
* In `review_aesthetics`, explicitly check organic subjects for missing essential anatomy (legs, limbs, proper posture).


Scaffolding Usable?: Maybe - would rather mice be mixed together
Illustration Usable?: Maybe - would rather mice be mixed together; they have no legs
Other notes: labels redundant
General Problem Class: aesthetics poor, too much info

### 1. What Went Wrong

* **Segregated Layout Instead of Natural Mixing:** The 6 brown mice and 2 white mice were split into two rigidly segregated groups (a $2 \times 3$ grid on the left and a $1 \times 2$ column on the right) separated by a large negative space gap. For a probability/genetics word problem, students benefit from counting a mixed population rather than viewing a pre-sorted set.
* **Redundant Labels / "Too Much Info":** The badges explicitly reading `Brown Fur (6)` and `White Fur (2)` spoon-fed the counts. This violated Core Directive 3 (utility/insight) and cluttered what was requested as a simple illustration.
* **Missing Anatomy (Legless Mice):** The final render produced floating, pill-shaped mouse bodies without feet or legs because the base diagram lacked limb primitives and the artist prompt over-constrained the generative model to the scaffold's silhouettes.

---

### 2. Agents Involved

| Agent | Task(s) | Role in the Failure |
| --- | --- | --- |
| **`markup_specialist`** | `plan_markings` | Proposed adding explicit count badges (`Brown Fur (6)`, `White Fur (2)`), creating the initial demand for visual separation and label clutter. |
| **`layout_expert` & `arrangement_planner**` | `plan_composition`, `plan_object_arrangement` | Structured the scene into two isolated clusters/columns to accommodate the category badges rather than an interleaved, organic layout. |
| **`image_detail_planner`** | `merge_plans`, `review_description` | Failed to prune the redundant labels despite explicit prompt warnings to moderate overzealous markup agents. |
| **`scaffolding_designer`** | `design_scaffolding` | Omitted leg/paw primitives, drawing only body ellipses, heads, ears, and tails. |
| **`artist`** | `plan_finishing` | Imposed rigid bounding-box constraints in `image_prompt` and `style_guidelines` without prompting the model to add biological features like paws/feet. |

---

### 3. Root Causes in Agent Instructions

* **Ambiguity on Problem Types in `plan_composition` / `plan_object_arrangement`:**
The instructions emphasize keeping objects "distinct, non-overlapping" and "arranged in a grid or cluster," but do not distinguish between **Categorization/Sorting problems** (where grouping makes sense) and **Probability/Sampling/Simulation problems** (where items represent a combined pool and should be mixed/interleaved).
* **Weak Gatekeeping Against Redundant Labels in `merge_plans`:**
Although `merge_plans` includes the cautionary note (*"Don't add extraneous labels just because the markup specialist wants to"*), it lacks a strict rule: *If the diagram is an illustrative count of objects whose identity is visually obvious, textual count badges are forbidden.*
* **Over-Constrained img2img Anchoring vs. Morphing:**
In `plan_finishing`, the instruction commands the artist to morph primitives *“WITHOUT expanding beyond their original bounding boxes.”* When base primitives are simple ovals without limbs, the diffusion model interprets this as a mandate to keep the silhouettes as legless blobs.

---

### 4. Suggestions for System Improvement & Trade-off Analysis

**A. Introduce Problem-Type Layout Heuristics**

* **Change:** Add explicit layout guidance in `layout_expert` and `arrangement_planner`:
* *Probability / Sampling / Mixed-set problems:* Interleave classes naturally in a single cohesive array or cluster (e.g., alternating grid or semi-regular scatter).
* *Comparison / Ratio / Sorting problems:* Use distinct grouped columns or segmented containers.


* **Trade-off:** If prompts push too hard toward "natural/random scattering," object counts become harder to inspect algorithmically, and the coder/image model will frequently introduce overlapping bounding boxes or miscounts.

**B. Strict Label Minimization Rule**

* **Change:** Update `plan_markings` and `image_detail_planner`: Bounding badges/labels should only exist if the object cannot be identified visually (e.g., unlabeled geometric variables $x, y$) or if numerical measurement indicators (dimension lines, axis values) are explicitly requested.
* **Trade-off:** Stripping labels completely might cause confusion in cases where two classes look similar in grayscale/colorblind rendering (e.g., slight shade differences). The rule should specify *“rely on visual differentiation (color/shape); do not add textual count badges unless requested.”*

**C. Decouple Anatomical Details from Primitive Skeletons**

* **Change:** In `plan_finishing` (Artist), refine the anchor directive: *“Primitives denote object center, scale, and general mass—allow natural anatomical extensions (legs, paws, stems, handles) outside the primitive boundary.”*
* **Trade-off:** Giving the diffusion model freedom to extend beyond bounding boxes increases the risk that limbs from adjacent objects overlap or merge into visual artifacts. This requires the `scaffolding_designer` to maintain slightly wider default padding (e.g., $\ge 1.5\times$ object radius) between centers.


Scaffolding Usable?: Yes
Illustration Usable?: Maybe - They have no legs, and differences in tails may be distracting
Other notes: different tails probably not reliably detectable by img model; rare fluke
General Problem Class: aesthetics poor

**What Went Wrong**

1. **Missing Anatomical Features (Legs / Paws):** The generated mice in the final illustration appear legless / floating. Because the base scaffolding represented the body as a single flat ellipse sitting on the coordinate plane with no primitive placeholders for legs/feet, the img2img model faithfully locked onto the rounded ellipse bounding box without inferring or rendering paws beneath them.
2. **Inconsistent / Fluked Tail Anatomy:** The tails across the 8 mice exhibited slight generative drift/variations in curvature and thickness that could be visually distracting in a uniform counting grid.
3. **Spurious Upstream Inspection Loop:** `inspection_manager` rejected the first base diagram pass over missing `ax.axis('off')` and `ax.set_aspect('equal')`, violating the `EXECUTION_CONTRACT` and burning an unnecessary planning/coding iteration.

---

**Agents Involved**

* **`scaffolding_designer` & `coder` (Stage 3):** Designed and plotted mice using only an ellipse, circle head, ear, eye, and tail line—omitting basic foot/leg base geometry.
* **`artist` (`plan_finishing` & `render_final`, Stage 4):** Strictly adhered to the anchor bounding boxes of the ellipses without prompting for grounded anatomical details (e.g., small paws/feet resting on the ground plane).
* **`image_verifier` (`review_aesthetics`, Stage 5):** Looked primarily at macro balance, style, and lack of visual artifacts, rating aesthetic quality as "exceptional" while missing missing limb anatomy and tail inconsistencies.
* **`inspection_manager` (Stage 3):** Triggered a false-positive rejection by inspecting Python code conventions rather than evaluating visual output against the execution wrapper spec.

---

**Root Causes in Agent Instructions**

* **Over-Simplification in Primitive Abstraction:** `DIRECTIVE_PRIMITIVES` explicitly encourages "circles for cats" and minimal shapes. While this works for macro layout, `scaffolding_designer` lacks a cue to anchor grounding anatomy (like tiny base wedges/capsules for paws) when animals are rendered in profile.
* **Over-Constrained Anchor Directives in `plan_finishing`:** Directive 2 commands the artist to morph primitives *"WITHOUT expanding beyond their original bounding boxes"*. When given a legless ellipse, the model interprets drawing legs below the ellipse as a boundary violation.
* **Aesthetics Review Lack of Grounding Check:** `review_aesthetics` checks color balance, coherence, and glitch artifacts, but lacks a check for anatomical completeness or uniform consistency across repeated grid tokens.
* **Manager Prompt Desynchronization:** `inspection_manager` lacked the strict `EXECUTION_CONTRACT` block present in `reviewer` and `coder`, causing it to judge Matplotlib scripts by standard standalone conventions rather than wrapper environment rules.

---

**Suggestions for Improvement**

* **Add `EXECUTION_CONTRACT` to `inspection_manager` System Identity:** Ensure the inspection manager's system prompt includes the exact execution contract string so it does not evaluate Python code for missing wrapper functions (`set_aspect`, `axis('off')`).
* **Calibrate Grounding / Anatomy Prompts in `plan_finishing`:** Update Stage 4 instructions to clarify that when morphing animal/creature primitives into full illustrations, the artist should add grounding limbs/feet along the bottom baseline even if they were omitted in the abstract scaffolding.
* **Refine `DIRECTIVE_PRIMITIVES` for Side-Profile Creatures:** Update primitive guidelines to note that side-profile living subjects should include simple grounding bases or foot indicators to prevent "floating bean" silhouettes.
* **Add Uniformity/Consistency Criterion to `review_aesthetics`:** Include an explicit check for repeated token grids: *"When an image depicts multiple instances of the same object in a grid/pattern, ensure secondary features (limbs, tails, ears) remain anatomically complete and uniformly styled across all instances."*



Problem 2 User Notes:
- For these type of problems it would be nice if the mice were randomly scattered and non overlapping, not in grids
- labels displaying "2 mice" and other things that can plainly be see are redundant
- weird lines or fences around / between the mice



**Problem 3**


Scaffolding Usable?: Yes
Illustration Usable?: Yes
Other notes: Don't want labels too close to edge
General Problem Class: 

### Summary & Failure Mode Analysis

* **General Problem Class:** Layout Margin & Boundary Buffer Management (Canvas Edge Proximity / Text Clipping Risk).
* **Execution Outcome:** Both the base scaffolding and the final illustration were marked **Usable**, completing with zero retries. However, user feedback noted that annotations were placed uncomfortably close to the image canvas boundary: **"Don't want labels too close to edge."**
* **Specific Defect:**
* The scale note `Scale: 1 cm = 90 m` was rendered at $(x=5, y=-3.5)$ against an axis limit of $y_{\min} = -4.5$.
* The left dimension label (`4 cm`) was positioned at $x = -6.1$ against $x_{\min} = -7.5$.
* While within bounds mathematically, matplotlib's bounding box text expansion and downstream automated cropping (`strips all axes... crops surrounding whitespace`) brought the outer boundary flush against the text edges, leaving virtually zero visual padding between the labels and the outer edge of the canvas.



---

### Agents Involved

| Agent | Role | Contribution to Defect |
| --- | --- | --- |
| **`coder`** | Execution Planning & Coding | Set tight axis limits (`xlim=[-7.5, 7.5]`, `ylim=[-4.5, 4.5]`) and placed the scale label at $y=-3.5$ without accounting for text bounding box dimensions and post-process auto-cropping. |
| **`reviewer`** / **`review_manager`** | Pre-Execution Review | Evaluated coordinates purely for mathematical logic and syntax. Did not check whether canvas padding was sufficient for text margins. |
| **`inspector` (`check_overlaps`, `detect_artifacts`)** | Post-Execution Scaffolding QA | `check_overlaps` asked: *"Is any text cut off at the edge?"* Because the text was completely inside the frame (not cut off), the inspector gave it a pass, failing to detect tight edge proximity. |
| **`inspection_manager`** | QA Consolidation | Concurred with the inspector's pass because no severity tags or flags were raised regarding margins. |

---

### Root Causes in Agent Instructions

* **Coder Instructions Lack Quantitative Padding Rules:**
* `coder` system identity states: *"the limits(xmin, xmax, ymin, ymax, etc.) need to be a bit larger than the object."*
* "A bit larger" is subjective. The coder sized limits relative to the central rectangle (width 10, height 4) rather than the outer envelope of all annotations, dimension lines, and scale text blocks.


* **Wrapper Behavior vs. Coder Assumptions:**
* The execution wrapper automatically *"crops surrounding whitespace"*.
* When axes are cropped down to the tight bounding box of artist elements, text placed near the edge of drawn items ends up touching the actual border of the saved PNG.


* **Inspector Criteria Only Evaluates Binary Clipping:**
* `check_overlaps` only looks for binary failures: *"Is any text cut off at the edge?"* It lacks a constraint for aesthetic minimum padding (e.g., clearance margins or buffer thresholds).



---

### Systemic Improvement Suggestions & Trade-Offs

**1. Quantitative Padding Directives for Coder & Scaffolding Blueprint**

* **Change:** Update `EXECUTION_CONTRACT` and `coder` instructions to require explicit margin calculation:
> *"When setting plot limits (`set_xlim`, `set_ylim`), calculate the bounding box of ALL drawn elements (including dimension arrows, text labels, and corner annotations) and add at least 15–20% visual padding in all directions beyond the outermost text or graphic element."*


* **Trade-Off / Risk:** Setting padding too large can cause the core geometric diagram to appear small or low-resolution if the post-processing wrapper's auto-crop behaves inconsistently across different aspect ratios.

**2. Refine Inspector Check for Margin Proximity**

* **Change:** In `check_overlaps` and `detect_artifacts`, expand Check #2:
> *"2. Is any text or element cut off at the edge, or placed uncomfortably close (< 10% canvas margin) to the image boundary?"*


* **Trade-Off / Risk:** Visual models evaluating image renders may hallucinate edge proximity or flag intentional full-bleed layouts as minor issues, causing unnecessary QA retry loops if not balanced by manager tolerance.

**3. Standardize Annotation Utilities in Internal Helper Library**

* **Change:** Provide standardized helper functions or layout guidelines for off-canvas text placement (e.g., dedicated legend/scale zones with fixed offset anchors).
* **Trade-Off / Risk:** Reduces flexibility if custom or non-standard diagram layouts require unique positioning.


Scaffolding Usable?: Maybe - dotted line is likely too distracting

Illustration Usable?: Yes

Other notes: 

General Problem Class: 

**Post-Mortem Analysis & Evaluation**

### General Problem Class

**Geometry / Scale Drawing with Contextual Map Abstraction (2D Vector Illustration)**

---

### What Went Wrong

* **Scaffolding Over-Specification:** The scaffolding included internal decorative geometry (a dashed rounded rectangle/loop intended to represent a walking path). In a mathematical scaffolding plot, high-contrast dashed lines often signify hidden edges, auxiliary geometric constructions, or paths of motion rather than decorative trails, creating potential visual ambiguity and clutter for students.
* **Decorative Bleed into Scaffolding:** Purely aesthetic elements ("walking path", "tree symbols") that belong to the Stage 4 generative/artistic pass were hardcoded into the Stage 3 Matplotlib blueprint and code.
* **Discrepancy Drift on Non-Essential Items:** The `coder` placed 6 circles and a dashed loop instead of the 3–4 tree placeholders originally planned, triggering an adherence note during inspection. While correctly waved through by `inspection_manager`, this consumed verification tokens and attention on irrelevant decorative artifacts.

---

### Involved Agents

* **`artistic_planner` (Stage 2):** Introduced the idea of the "dashed gray walking path loop" to convey the park theme.
* **`image_detail_planner` (`merge_plans` / `review_description`):** Preserved the dashed walking path in `latest_description` without scoping whether it should live in the Python base diagram or wait for the Stage 4 image generator.
* **`scaffolding_designer` (Stage 3):** Explicitly translated the dashed path into a concrete Python requirement (`FancyBboxPatch` / dashed loop) rather than keeping the scaffolding purely structural.
* **`coder` (Stage 3):** Implemented the dashed path directly onto the Matplotlib axes with a prominent dashed style (`linestyle='--'`).

---

### Root Cause Analysis in Agent Instructions

| Agent / Stage | Instruction Issue | Manifested Behavior |
| --- | --- | --- |
| **`scaffolding_designer`** | **Vague Abstraction Boundary:** Rule 3 says *"Abstract Complex Objects: ... If their positions do not need to be pixel perfect, you can leave them off"*, but Rule 5 says *"The artist can make mistakes drawing these from scratch"*. It lacks a clear directive distinguishing **mathematical structural features** from **thematic internal background details**. | Converted a decorative walking path into a rigid geometric patch in the Python scaffolding. |
| **`coder`** | **No Standalone Scaffolding Sanity Rule:** The instructions encourage minimal objects, but do not warn that geometric conventions (like dashed lines) carry distinct mathematical meanings in textbook diagrams. | Rendered the path using standard dashed lines (`--`), which visually mimic bounding boxes or auxiliary math constructions. |
| **`image_detail_planner`** | **Lack of Layer Separation:** Combines all visual and mathematical requirements into a single flat `latest_description` text block without tagging which elements are **Strict Geometric Constraints (Stage 3)** vs. **Illustrative Aesthetic Enhancements (Stage 4)**. | Upstream artistic suggestions were interpreted by Stage 3 as mandatory plotting elements. |

---

### Suggestions & Systemic Trade-Offs

**1. Explicitly Scope Scaffolding to Problem Geometry Only**

* **Suggestion:** Update `scaffolding_designer` instructions to state: *"Do not plot decorative internal textures, paths, or background scenery (e.g., walking paths, grass lines, clouds, desks) unless the word problem specifically measures or calculates them. Leave the interior solid/plain for the artist to illustrate over."*
* **Trade-Off / Systemic Risk:** For problems where object count or spatial clustering *is* the math (e.g., counting 12 trees in a grid), over-suppressing primitives could cause `scaffolding_designer` to omit required counts, forcing the image model to hallucinate numbers.

**2. Reserve Dashed Lines (`linestyle='--'`) for Mathematical Primitives**

* **Suggestion:** In `coder` / `EXECUTION_CONTRACT`, specify that dashed/dotted lines are strictly reserved for:
* Auxiliary geometric constructions (altitudes, hidden 3D edges, projections)
* Asymptotes or grid subdivisions


* **Trade-Off / Systemic Risk:** Low risk; decorative elements should rarely, if ever, use dashed line styling in Matplotlib scaffolding.

**3. Two-Tier Description in Stage 2 (`latest_description`)**

* **Suggestion:** Have `merge_plans` separate `latest_description` into two clear structured sections:
1. `[SCAFFOLDING CORE]`: Dimensions, aspect ratio, bounding shapes, tick marks, labels, scale boxes.
2. `[ARTISTIC OVERLAY]`: Textures, themes, foliage, paths, and aesthetic color transitions.


* **Trade-Off / Systemic Risk:** Slightly increases prompt token overhead across downstream stages, but eliminates ambiguity for both `scaffolding_designer` (who only reads the Core section) and `artist` (who reads both).


Scaffolding Usable?: Yes

Illustration Usable?: Yes (although don't love that tree goes over border)

Other notes: 

General Problem Class: 

**General Problem Class**
Scale Drawing / Geometric Bounding & Feature Containment in Image-to-Image Pass.

---

### What Went Wrong

1. **Feature Bleed Across Defined Boundary:** In the final rendered illustration, a tree canopy placed near the edge of the park spilled over the crisp $10 \times 4$ teal perimeter border.
2. **Review Miss / Flawed Inspection Standard:** Neither the `inspector` (Stage 3), `image_verifier` (Stage 5), nor `issue_aggregator` flagged boundary clipping/overflow as an issue because the base scaffold placed circles too close to the boundary line, and the artist prompt lacked an explicit hard-edge containment directive.
3. **Spurious Manager Hallucination (Stage 3):** `inspection_manager` remarked that the coder forgot `ax.axis('off')`. In reality, the `EXECUTION_CONTRACT` explicitly instructs the coder *not* to include `ax.axis('off')` because the wrapper handles it. The manager hallucinated this omission as a flaw (though it passed the run anyway).

---

### Agents Involved

| Agent | Stage | Role in the Failure |
| --- | --- | --- |
| `scaffolding_designer` | Stage 3 | Placed circular tree primitives right up against the boundaries (e.g., center at $(9, 3.2)$ with radius $0.4$ reaches $y=3.6$, while boundary is $y=4.0$; $(8.5, 0.8)$ sits close to the bottom/right). |
| `artist` (`plan_finishing`) | Stage 4 | Prompt instructed the model to morph green circles into "bushy tree canopies with soft, layered gradient shading" without enforcing a hard bounding box rule against the outer border. |
| `image_verifier` / `issue_aggregator` | Stage 5 | Evaluated the image holistically for aesthetic balance and label accuracy, completely overlooking that foreground elements breached the mathematical outer container. |
| `inspection_manager` | Stage 3 | Displayed contract drift by flagging the absence of `ax.axis('off')`. |

---

### Root Causes in Agent Instructions

* **Loose Definition of "Bounding Box" in Stage 4:**
`plan_finishing` contains the guideline:
> *"command the Artist to morph those primitives into the described objects WITHOUT expanding beyond their original bounding boxes or center points."*


However, the generated `image_prompt` in `plan_finishing` softened this directive to: *"transform the solid green circles into stylized, top-down bushy tree canopies with soft, layered gradient shading"*. When the generative model parsed the positive prompt, it expanded the foliage into an organic shape that crossed the border.
* **Lack of Padding Rules in Scaffolding Blueprinting:**
`scaffolding_designer` has no instruction requiring margin/padding when placing internal decorative shapes near mathematical boundaries. Placing a $0.8$-unit diameter tree canopy $0.4$ units from a line guarantees visual overlap once rendered organically.
* **Inspection Manager Scope Alignment:**
`inspection_manager` does not carry the `EXECUTION_CONTRACT` in its `system_identity` (unlike `reviewer` and `review_manager`), causing it to incorrectly penalize or note standard wrapper-handled behaviors like axes stripping.

---

### Systematic Suggestions for Improvement

* **Inject `EXECUTION_CONTRACT` into `inspection_manager`:** Ensure `inspection_manager` has the exact same wrapper contract as `reviewer` to eliminate false positives regarding `ax.axis('off')` or figure saving.
* **Formalize Interior Feature Padding in `scaffolding_designer`:** Add a directive requiring decorative internal elements (trees, ponds, icons) to remain at least $1.5\times$ their radius away from perimeter bounding lines unless explicitly designed to intersect them.
* **Harden Perimeter Containment Directives in `plan_finishing`:** Update the Stage 4 template to require negative prompts like `spilling over frame`, `overlapping outer border`, `clipped boundary lines` whenever an enclosed area (like a park, room, or plot) is specified.
* **Add Boundary Integrity Check to `review_aesthetics` / `review_math`:** Include an explicit check in Stage 5 asking: *"Do internal decorative elements respect and remain inside the defined container/boundary lines without spilling over?"*


Scaffolding Usable?: Yes

Illustration Usable?: Yes

Other notes: 

General Problem Class: 

**General Problem Class:** False-Positive Rejection via Execution Environment Specification Mismatch (Inspector/Inspection Manager checking for wrapper-handled properties), followed by an unhandled Loop Desynchronization (Coder re-emitted identical code, Review Manager overrode the upstream rejection).

---

### What Went Wrong

1. **False-Positive Inspection Rejection:**
* In `consolidate_inspection`, the **`inspection_manager`** rejected Attempt 1. It claimed the code lacked `ax.set_aspect('equal')` and `ax.axis('off')`, leading to a stretched aspect ratio and visible axes.
* However, the system's `EXECUTION_CONTRACT` explicitly specifies that the **execution wrapper automatically handles `set_aspect('equal')`, strips spines/ticks, and crops whitespace**, and that omitting these calls is correct behavior.


2. **Review Loop Desynchronization & Overruling:**
* After the rejection, the workflow looped back to `plan_logic` and `write_code`. The **`coder`** generated the exact same Python code again because it recognized that the wrapper handled those properties.
* When the review pipeline ran again, **`review_manager`** noted that `inspection_manager`'s rejection was erroneous and passed the unchanged code directly into execution.


3. **Wasted Pipeline Cycles & Token Overhead:**
* The pipeline burned extra execution steps (`troubleshoot_visual`, a redundant `plan_logic` -> `write_code` -> `syntax_check` -> `logic_check` -> `math_check` -> `consolidate_review` loop) on a non-issue before reaching Stage 4.



---

### Agents Involved

* **`inspection_manager` (and `inspector`):** Evaluated visual output and read code assumptions through the lens of standalone Matplotlib scripts rather than the sandboxed wrapper contract.
* **`coder`:** Correctly adhered to `EXECUTION_CONTRACT`, causing it to produce identical code when told to "fix" an aspect/axis issue it was explicitly forbidden from adding.
* **`review_manager`:** Acted as the eventual circuit breaker by recognizing the inspector's mistake and passing the code through on retry.

---

### Root Causes in Agent Definitions & Instructions

| Agent / Context | Root Cause in Prompt Definition |
| --- | --- |
| **`inspection_manager` System Identity** | Contains `${DIRECTIVE_MANAGER_WITH_RETRY}` and general directives, but **lacks `${EXECUTION_CONTRACT}**`. Unlike `reviewer` and `review_manager`, the inspection manager was never informed that axes and aspect ratio are post-processed by the wrapper. |
| **`inspector` System Identity** | `detect_artifacts` instructs the inspector to check: *"Are axes, ticks, or grids visible?"* without noting that the code under review is executed inside an auto-stripping harness. When inspecting code or comparing mental models, it defaults to standard Matplotlib behavior. |
| **`inspection_manager` Scope vs. Visual Feedback** | `inspection_manager` has `model_type: "text"` but scopes history from `coder`. Instead of judging purely on the text reports of the vision inspectors (who all reported *"No issues found"* and *"aspect ratio matches"*), it inspected the Coder's text/code directly and hallucinatively deduced that axes *must* be visible because `axis('off')` wasn't in the raw script. |

---

### Systemic Improvement Suggestions

* **Inject `EXECUTION_CONTRACT` into Stage-3 Inspection Agents:**
* Add `${EXECUTION_CONTRACT}` directly to the system identities of `inspector` and `inspection_manager`. Just as reviewers were updated so they don't flag missing `axis('off')`, inspectors must know that axes/ratios are enforced wrapper-side.


* **Constrain `inspection_manager` to Visual Evidence (Prevent Code-Peeking Hallucinations):**
* In `consolidate_inspection`, clarify that the inspection manager evaluates *the rendered visual reports*, not whether specific Matplotlib boilerplate was written in Python. If all 4 visual inspectors report `passed: true` with zero artifacts, the manager should not reject based on speculative static code analysis.


* **Align Rejection Instructions with Scaffolding Directives:**
* In `troubleshoot_visual` and `consolidate_inspection`, add explicit guardrails: *"Do not instruct the coder to add `plt.show()`, `plt.savefig()`, `ax.axis('off')`, or `ax.set_aspect('equal')`, as these are executed automatically by the harness."*


* **Cross-Phase Trend Consideration:**
* *Watch out for over-correction:* Ensure that telling inspectors to ignore missing `ax.set_aspect('equal')` does not lead them to ignore *actual* aspect-ratio distortions caused by improper manual `xlim`/`ylim` data bounds in custom plotting logic.


Scaffolding Usable?: Yes

Illustration Usable?: No - the shape of the trees is too ball-like

Other notes: it's not that bad

General Problem Class: aesthetics poor

### Diagnostic Analysis

#### 1. What Went Wrong

* **Visual Artifact / Aesthetic Failure:** In the final illustration, the top-down tree canopies rendered as solid, spherical "ball-like" green orbs rather than organic, natural foliage or textured vegetation clusters.
* **Secondary Workflow Friction:** A false-positive retry loop occurred in Stage 3 where `inspection_manager` rejected code for missing `ax.set_aspect('equal')` and `ax.axis('off')`, directly contradicting the `EXECUTION_CONTRACT` in its instructions.

---

#### 2. Agents Involved

* **`coder` & `scaffolding_designer` (Stage 3):** Encoded trees strictly as `patches.Circle(tc, radius=0.25)`.
* **`artist` (Stage 4 - `plan_finishing` & `render_final`):** Prompted the model to *"Transform the dark green circles into beautiful, detailed top-down tree canopies with smooth gradient shading"*, which the img2img model interpreted literally as shaded 3D spheres/balls.
* **`image_verifier` & `issue_aggregator` (Stage 5):** Evaluated top-level composition and math, giving an aesthetic pass without flagging the artificial, ball-like geometry.

---

#### 3. Root Causes in Agent Instructions

* **Geometric Anchor Constraint Too Rigid:**
The Stage 4 `plan_finishing` directive instructs the artist to:
> *"morph those primitives into the described objects WITHOUT expanding beyond their original bounding boxes or center points."*


When provided with a circular Matplotlib patch, the img2img model treats the circular perimeter and radial symmetry as a strict structural anchor, producing shaded spheres instead of organic shapes with irregular foliage, leaf clusters, or branching.
* **Style Prompt Overfitting to Smooth Gradients:**
The injected style description (`"casual mobile game art style"`, `"smooth gradient shading"`, `"soft highlights"`) combined with circular anchors pushes generative models directly toward glossy, ball-like 3D rendering rather than crisp, 2D top-down vector canopy art (e.g., scalloped or cloud-edged tree clusters).
* **Scaffolding Primitive Oversimplification:**
Stage 3 instructions encourage falling back to basic circles for any complex object (`DIRECTIVE_PRIMITIVES`: *"If in doubt, fall back to circles"*). For organic shapes like trees, a circle lacks the structural cues (irregular contours, trunks/branches, foliage clumps) needed for natural generative painting.

---

### Systemic Suggestions & Trade-off Analysis

| Suggestion | Implementation Mechanism | Potential Side-Effects & Trade-offs |
| --- | --- | --- |
| **Organic Primitive Library / Polygon Scaffolding** | Update `DIRECTIVE_PRIMITIVES` and `coder` guidelines to draw organic entities (like vegetation) using multi-lobed irregular polygons or composite overlapping patches rather than single pure circles. | Increases Python code complexity and execution token overhead; risk of geometric errors or messy scaffolding if coordinates are calculated poorly. |
| **Foliage & Texture Guidance in Stage 4 Prompting** | In `plan_finishing`, add explicit negative/positive prompt guidance for foliage (e.g., *"organic, scalloped tree canopies with irregular leafy edges; no solid spheres, no glossy balls"*). | Over-constraining prompt keywords across diverse domains can cause negative prompt bleed-through or ignore subtle style variations requested by the user. |
| **Loosen Geometric Bounding for Organic Objects** | Refine the Stage 4 anchor directive: enforce strict bounding boxes for *man-made/mathematical objects* (boxes, tables, tracks), but allow soft, irregular boundary expansion for *natural/organic elements* (trees, shrubs, clouds, water edges). | If the boundary relaxation is too loose, generative fill may leak into adjacent mathematical regions, dimension lines, or text callouts. |
| **Align `inspection_manager` with `EXECUTION_CONTRACT**` | Re-verify that `EXECUTION_CONTRACT` is strictly enforced in `inspection_manager` system prompts to eliminate false rejections on automatic wrapper behaviors (`axis('off')`, `set_aspect('equal')`). | High value, zero negative side-effects. Directly reduces wasted execution cost and latency. |


Scaffolding Usable?: Yes
Illustration Usable?: No - too plain/non-aesthetic
Other notes: its literally an empty field of plain grass, lame
General Problem Class: aesthetics poor

**Post-Mortem & Incident Analysis: Overly Plain / Sterile Illustration**

---

### What Went Wrong

The pipeline successfully locked down mathematical accuracy (a clean 10:4 rectangular perimeter, dimension markings, and scale box), but the final rendered illustration was visually sterile—described by the user as *"literally an empty field of plain grass, lame."*

Instead of generating an engaging, contextual textbook illustration of a park (e.g., footpaths, pond, trees, playground zones, benches, sports fields, or flowerbeds within the bounding box), the pipeline treated the entire object as a bare, textured green polygon.

---

### Agents Involved

| Agent / Role | Task | Failure Contribution |
| --- | --- | --- |
| **`image_detail_planner`** | `review_request` | Prematurely marked `requires_artistic: false`, reasoning that *"it does not strictly require complex illustrations, textures, or real-world objects."* |
| **Specialist Swarm** (`layout_expert`, `visual_director`, `educator`) | Stage 2 Planning | Skipped injecting park context (pathways, amenities, landmarks) and focused 100% on dimension arrows and scale callouts. |
| **`artistic_planner`** | `artistic_planning` | Did not execute because `situational_planning` did not trigger arrangement or rich aesthetic expansion for a generic "park". |
| **`artist`** | `plan_finishing` & `render_final` | Over-indexed on negative prompts (`realistic trees, foliage, clutter`) and literal adherence to the base diagram, filling the interior with only a *"soft pastel park-green gradient and very subtle 2D lawn texture."* |
| **`image_verifier`** & **`issue_aggregator`** | `review_aesthetics` & `aggregate_feedback` | Graded on the absence of rendering glitches rather than richness or educational storytelling, rating a plain green box as *"exceptional clarity and visual appeal."* |

---

### Root Causes in Agent Instructions

* **The `requires_artistic: false` Trap in `review_request`:**
* *Mechanism:* The prompt frames "Artistic" as things a Python script *cannot* draw. Because Python *can* draw a green rectangle, the planner classified this as purely technical, disabling creative ideation early.
* *Flaw:* A textbook problem about a "park" is meant to look like an actual park, not an abstract CAD bounding box.


* **Negative Prompt Hyper-Correction in `plan_finishing`:**
* *Mechanism:* In an effort to keep lines clean and avoid AI hallucinations, the artist explicitly banned `trees`, `foliage`, and `background details` in `style_guidelines`.
* *Flaw:* The model obeyed the negative constraints strictly, removing any possibility of internal park features.


* **QA Reviewers Grade for "Absence of Defects" Rather than "Delight/Richness":**
* *Mechanism:* `review_aesthetics` checks for blur, artifacts, and contrast. A completely plain green box has 0 visual artifacts, earning a 100% pass score.
* *Flaw:* There is no metric or directive evaluating whether an illustration is **engaging, context-appropriate, or visually compelling**.



---

### Actionable Suggestions for System Improvement

**1. Reframe `requires_artistic` from "Can Python Draw It?" to "Does It Represent a Real-World Entity?":**

* Update `review_request` instructions: Abstract mathematical shapes (polygons, lines, coordinate grids) are technical; real-world entities (parks, farms, classrooms, rooms, swimming pools) **always require artistic enhancement** within the bounded dimensions.

**2. Introduce an "Interior Detailing / Contextual Filling" Directive for Scaffolding Overlays:**

* Add an explicit instruction to `plan_finishing`:
> *"When the base diagram establishes an outer boundary for a real-world place or object (e.g., a park, room, garden, field), decorate the interior with stylized, non-distracting visual elements (top-down paths, stylized trees, benches, water features) that fit the theme without obscuring measurement lines or altering boundary dimensions."*



**3. Distinguish "Visual Clutter" from "Thematic Context" in Negative Prompts:**

* Instruct `plan_finishing` not to blindly ban core subject nouns (like `trees` in a park) in `style_guidelines`. Negative prompts should target stylistic defects (`photorealism`, `perspective distortion`, `unreadable text`), not thematic elements that bring the scene to life.

**4. Add an "Engagement / Emptiness Check" to `review_aesthetics`:**

* Add a check for **Visual Depth and Thematic Richness**: Flag diagrams that consist solely of flat un-rendered geometric fills when representing tangible real-world places or things.


Scaffolding Usable?: No - trees at corners are weird
Illustration Usable?: No - trees at corners are weird
Other notes: 
General Problem Class: aesthetics poor

**Summary of What Went Wrong**

* **Uncanny Symmetrical Detailing:** An artistic embellishment ("trees near inner corners") was overly formalized into an artificial, rigid layout: exactly one circular tree anchored in each of the four corners of the park rectangle.
* **Scaffolding Over-Specification:** Instead of treating "park context" as a texture or artistic background, the pipeline treated decorative trees as critical geometric primitives that had to be hardcoded into the Python script (`patches.Circle` at 4 coordinates), reviewed for aspect ratio distortion, and locked into the img2img diffusion pass.
* **Pipeline Fixation on Irrelevant Scaffolding:** Upstream QA and managers spent multiple expensive retry loops debating whether the 4 corner circles were distorted into ellipses or whether the coder called `set_aspect('equal')`, completely missing the fundamental aesthetic awkwardness of having 4 isolated corner trees.

---

**Agents Involved & Propagation Path**

* `artistic_planner` (**Origin**): First hallucinated/injected the concept of "minimalist tree icons near the inner corners" to represent a park without considering naturalistic landscape composition.
* `image_detail_planner` (**Amplifier**): Merged the corner trees into the formal visual spec (`latest_description`), locking it into the core blueprint.
* `scaffolding_designer` (**Hardcoder**): Converted the subjective artistic note into strict, mandatory Python geometry: *"place four small dark green circles near each of the four inner corners"*.
* `coder` (**Implementer**): Hardcoded exact coordinates `(2.6, 3.6), (11.4, 3.6), (11.4, 6.4), (2.6, 6.4)` for the 4 patches.
* `inspection_manager` & `image_verifier` (**Distraction**): Evaluated the 4 circles as core mathematical requirements, failing to flag the aesthetic weirdness while obsessing over pixel roundness and dimension line arrow lengths.
* `artist` (**Final Render**): Bound strictly by the anchor directive, morphed the 4 corner circles into 4 symmetrical corner bushes/trees, cementing the unnatural look.

---

**Root Causes in Agent Instructions**

1. **Vague Creative Mandates with Rigid Cascades:**
* `artistic_planner` instructions encourage adding details ("fill in missing details... anything not directly mentioned that still should be drawn... don't consider artistic talent a limiting factor"). When an LLM tries to add "park objects" under the constraint of "don't clutter the center," it predictably defaults to the simplest geometric trope: putting identical objects in the 4 corners.


2. **Confusion of "Scaffolding" vs. "Artistic Context":**
* Directives tell `scaffolding_designer` to abstract objects and strip flair, but also state *"If an object can be modeled precisely by a few simple primitives, use them"* (`DIRECTIVE_PRIMITIVES`). The agent misinterpreted decorative environmental background elements (trees) as mathematical primitives that needed Python coordinates.


3. **Anchor Directives Over-Constraining the Diffusion Pass:**
* `plan_finishing` enforces strict anchoring: *"morph those primitives into the described objects WITHOUT expanding beyond their original bounding boxes or center points."* This forbade the Stage 4 artist from fixing or naturally clustering the trees during the diffusion step.



---

**Suggestions for Systemic Improvement**

* **Distinguish Math Geometry from Environmental Set Dressing:**
* *Instruction Adjustment:* Update `scaffolding_designer` directives to explicitly state: *“Only plot geometry, bounds, graphs, and measuring lines that are essential to the mathematical problem. Never plot decorative background objects (e.g., individual trees, clouds, people, furniture) as Python primitives unless their exact quantity/position is explicitly queried in the problem.”*
* *Trade-off considerations:* If you strip primitives too aggressively, problems that *do* require discrete counting (e.g., "5 apples in a row") might fail to render the correct count. The rule must explicitly distinguish between **counting/word-problem items** vs. **environmental set dressing**.


* **Instruct Planners on Organic vs. Symmetrical Composition:**
* *Instruction Adjustment:* Add a negative constraint in `artistic_planner` and `plan_composition`: *“Avoid rigid 4-corner placements or unnatural geometric symmetries for natural/environmental objects (trees, plants, clouds). Prefer subtle background fills, edge textures, or natural clusters, leaving the central mathematical region clean.”*
* *Trade-off considerations:* Purely geometric diagrams (like a square with vertices $A, B, C, D$) require strict corner points. Ensure this negative constraint applies only to organic/scenic illustrations.


* **Empower the Stage 4 Artist to Ignore Decorative Scaffolding:**
* *Instruction Adjustment:* Clarify in `plan_finishing` and `render_final` that *mathematical boundaries and annotations* (rectangles, axis lines, arrowheads, labels) are locked anchors, but *decorative textures/fill elements* can be freely painted organically across the bounded region rather than mapped 1:1 to circle coordinates.


Scaffolding Usable?: Yes
Illustration Usable?: Maybe - not super aesthetic
Other notes: 
General Problem Class: 

**Problem Class: 2D Geometric Scale Drawing / Technical Layout**

**What Went Wrong**

1. **Spurious Inspection Rejection Loop (Stage 3):** The `inspection_manager` rejected a perfectly valid scaffolding script because it lacked `ax.set_aspect('equal')` and `ax.axis('off')`. The wrapper environment already handles these post-execution, making the rejection a false positive that burned an extra coding iteration and caused conflicting flags during code review.
2. **Sub-Optimal Aesthetics / Style Leakage (Stage 4):** The AI image model introduced faint isometric trees/paths into a strictly flat 2D geometry problem. While mathematically non-destructive, it created a visually awkward texture inside the rectangle ("not super aesthetic").
3. **Stage 1 Misclassification of Input Type:** `problem_validation` marked `math_found: false` and routed to the `visual_only` branch simply because the input contained a worked example rather than an unsolved question, injecting the dummy string `"No problem given, do your best without, use common sense."` into subsequent math prompts.

---

**Agents Involved**

* **`problem_validation`:** Classified worked example text as missing a math problem.
* **`inspection_manager` & `reviewer`:** Clashed over the execution environment contract (`set_aspect` / `axis('off')`).
* **`artist` & `image_verifier`:** Prompted and generated decorative 3D internal textures on a 2D orthographic diagram.

---

**Root Causes in Agent Instructions**

| Agent / Task | Root Cause in Prompt / System Identity |
| --- | --- |
| **`inspection_manager`** | Lacks the `EXECUTION_CONTRACT` block present in `reviewer` and `coder`. It does not know the wrapper automatically strips axes and enforces 1:1 aspect ratios post-run, leading it to hallucinate code-level omissions. |
| **`problem_validation`** (`extract_math_and_visual`) | Prompts ask strictly for a *"specific, solvable math problem"*. Worked examples, definitions, or reference diagrams get flagged `math_found: false`, stripping the actual math context from downstream tasks. |
| **`artist`** (`plan_finishing`) | Encouraged to *"add some objects or details"* and exercise creative license when descriptions are sparse, causing it to inject 3D/isometric park paths where a flat fill was required. |

---

**Suggestions for System Improvement**

* **Inject `EXECUTION_CONTRACT` into Inspection Manager:** Add the execution environment contract to `inspection_manager`'s `system_identity` so it does not flag missing wrapper duties.
* **Broaden Validation Scope in Stage 1:** Update `extract_math_and_visual` to accept worked examples and educational reference statements as valid math context (`math_found: true`) rather than forcing a strict "must be an unsolved question" test.
* **Add Flat 2D Texture Guardrails to Artist Planning:** In `plan_finishing`, add an explicit constraint for technical 2D geometry: when rendering flat maps/rectangles, restrict decorative fills to flat 2D vector fills or solid colors rather than perspective-based decorative elements.


Scaffolding Usable?: Yes
Illustration Usable?: No - same as scaffolding image - no illustrative properties
Other notes: 
General Problem Class: aesthetics poor

### Post-Mortem Analysis: Aesthetic Failure & Lack of Illustrative Transformation

---

### What Went Wrong

The pipeline successfully generated a precise, clean Matplotlib scaffolding diagram, but the downstream artistic pass completely failed to add illustrative value.

* **The Output Was a Clone of the Scaffolding:** The final generated image was visually indistinguishable from the Python base diagram (a flat green rectangle with a grid and text).
* **Failure to Illustrate the Subject:** The prompt requested a "park," but the pipeline produced an abstract wireframe coordinate box. No contextual or illustrative park elements (pathways, trees, park benches, grass textures, pond, playground) were incorporated to make it feel like a textbook illustration.
* **Over-Constrained Suppression of Artistry:** The system treated "purely technical 2D scale drawing" as a mandate to forbid all artistic flair, rendering Stage 4 (the image generation model) redundant and burning token/image API cost for zero transformation.

---

### Agents Involved

1. **`image_detail_planner` (Stage 2 - `review_request` & `merge_plans`):**
* Explicitly set `requires_artistic: false` and `requires_technical: true`.
* Stripped all artistic requirements in `merge_plans`, instructing the pipeline: *"Do not include any 3D elements, real-world textures, or the actual dimensions."*


2. **`artist` (Stage 4 - `plan_finishing`):**
* Anchored the prompt on maintaining a flat UI/vector diagram rather than morphing the green rectangle into an illustrated park view.
* Explicitly placed `"realistic grass textures, trees"` in the negative style guidelines (`style_guidelines`).


3. **`issue_aggregator` & `image_verifier` (Stage 5 - `review_aesthetics` & `aggregate_feedback`):**
* Gave unanimous passes to an image that had zero illustrative properties because they evaluated it against the over-sanitized `latest_description` rather than the system's global mandate for engaging textbook illustrations.



---

### Root Causes in Agent Instructions

* **Binary Categorization Trap (`review_request`):**
The prompt sets up an artificial dichotomy:
> *"An image of a hamster... ARTISTIC only... a pie chart... TECHNICAL only."*


Because this was a scale drawing, `image_detail_planner` marked it purely technical. The pipeline lacks guidance on **hybrid illustrative diagrams** (e.g., top-down park map with trees and pathways overlaid on an exact mathematical bounding box).
* **Overzealous Anti-3D / Anti-Texture Directives:**
Instructions like *"Strip away all the artistic flair"* and *"Don't try to render a 3d/rotated illustration of a fundamentally 2d problem"* were interpreted by the LLMs as *"render only flat geometry and eliminate all textures/objects."*
* **Ambiguity Between "Scaffolding" and "Final Product":**
The `artist` agent was instructed:
> *"CONSTRAINT: You must preserve the geometry of anything related to the original problem exactly..."*


Fearing a math/geometry QA failure, the artist opted for zero deviation, outputting a near-identical vector reproduction instead of painting a stylized top-down park over the bounding area.

---

### Key Suggestions & Mitigation Trade-Offs

| Suggestion | Implementation | Trade-Off & Risk Analysis |
| --- | --- | --- |
| **Introduce "2D Thematic Texturing" Concept** | In `GLOBAL_TASK_EXPLANATION` and `STAGE_4_CONTEXT`, clarify that 2D geometric regions representing real-world places (parks, fields, rooms) should receive **top-down stylized thematic illustrations** (e.g., stylized 2D grass, winding walking paths, cartoon trees viewed from above) within the exact bounding geometry. | **Risk:** The image generation model might warp rectangular boundary lines or obscure internal grid lines/dimension markers. |
| **Refactor `review_request` Prompt** | Replace the binary categorization (`requires_technical` vs `requires_artistic`) with a three-tier model: `PURE_ABSTRACT_MATH` (e.g., pure coordinate axes/number lines), `THEMATIC_ILLUSTRATED_MATH` (scale maps, word problem settings), and `FREEFORM_ILLUSTRATION`. | **Risk:** Might encourage the planner to add unnecessary decorative clutter to purely abstract geometric proofs or calculus graphs. |
| **Decouple "No 3D Distortion" from "No Textures"** | Refine the directive: *"Do not apply 3D perspective to 2D problems, but DO apply rich 2D flat-lay / top-down cartographic illustration styles to represent physical environments."* | **Risk:** Models sometimes confuse "top-down art" with isometric perspective, which could distort scale measurements. |
| **Calibrate Stage 5 Aesthetic Gatekeeper** | Add an explicit check in `review_aesthetics`: *"Did the artistic pass add meaningful illustrative value over the base scaffolding, or is it merely an identical duplicate of the raw plot?"* | **Risk:** Might trigger spurious retry loops on diagrams that genuinely only need clean geometric lines. |


Problem 3 user notes: this problem was really simple, not many things should go wrong



**Problem 4**


Scaffolding Usable?: No - ruler inaccurate (goes past 20)
Illustration Usable?: Maybe - would rather have 20 cm not printed on tape measure - - placement makes math connection unclear
Other notes: its surprisingly hard to even figure out HOW to illustrate this problem clearly
General Problem Class: 

**General Problem Class:**
**Physical Tool Realism vs. Abstract Geometric Markup (Circumference & Arc-Length Visualization)**

---

### 1. What Went Wrong

* **Failure to Conceptualize Measurement Functionality:** In the real world, a tape measure wrapped around a 20 cm object starts at 0 cm and overlaps at 20 cm. Instead, the agents created a continuous ribbon where "20 cm" was placed in the dead center like a static coordinate on a tape extending from 15 to 25. The ruler went past 20 because the agents treated the tape measure as a static number line centered at 20 rather than a measuring instrument beginning at 0.
* **Loss of Mathematical Connection:** Inscribing "20 cm" directly onto the tape fabric blurred the line between a *label* denoting total circumference and a *graduated scale value*. The final image lacked a clear visual indicator that 20 cm was the complete perimeter/circumference rather than just a point along an infinite strip.
* **Over-Complicated 3D Visual Mapping:** The system chose a whole sphere with a 3D equatorial ribbon instead of illustrating the problem's other given alternative: a **flat, sliced half-orange cross-section** with a clear 2D dimension line/arc markup.

---

### 2. Agents Involved

| Agent | Task / Role | Direct Contribution to Failure |
| --- | --- | --- |
| **`image_detail_planner`** | `merge_plans`, `review_request` | Arbitrarily eliminated the "sliced half-orange" option, committing the pipeline to a complex 3D wrapped tape measure. |
| **`scaffolding_designer`** | `design_scaffolding` | Concretized the instruction to center "20 cm" directly on the tape arc (`(0, -0.15)`) instead of specifying start/end endpoints or an external dimension callout. |
| **`coder`** | `plan_logic`, `write_code` | Implemented a static slice of a tape measure ($15\text{ cm} \to 25\text{ cm}$) centered at 20, rather than a tape wrapping from $0\text{ cm} \to 20\text{ cm}$. |
| **`reviewer` / `review_manager**` | `math_check`, `consolidate_review` | Validated the local mathematical differential geometry ($\Delta \theta$ vs. $\Delta x$) while missing the physical measurement logic (a tape measuring 20 cm must show the loop meeting at the 20 mark). |
| **`image_verifier`** | `review_math` | Rationalized the error during the second pass by stating that *"The '20 cm' text functions as a diagrammatic label rather than a reading at an overlapping point,"* giving false QA approval to an ambiguous educational visual. |

---

### 3. Root Causes in Agent Instructions

* **Instruction Bias Toward "3D Realism" Over Diagrammatic Utility:**
* In `situational_planning` and `plan_3d`, agents are heavily nudged to model objects as 3D solids. The instruction set lacks a directive stating: *When measuring a 1D or 2D property (perimeter, diameter, circumference), prioritize a 2D cross-section or flat projection if a 3D rendering obscures the start/end of the measurement.*


* **Ambiguity Between Dimension Callouts and In-Scene Labels:**
* In `plan_markings` and `design_scaffolding`, instructions do not distinguish between **in-world textures** (the printed numbers on a ruler) and **pedagogical dimension lines** (leader lines, dimension arrows, and floating callout tags like $C = 20\text{ cm}$).


* **Reviewers Focus on Code/Pixel Geometry Over Tool Semantics:**
* `math_check` instructions emphasize recomputing coordinates, proportions, and angles. It does not instruct the agent to evaluate **physical affordances** (e.g., "Does this measuring tool operate how a real student would use it?").



---

### 4. Suggestions for System Improvement

* **Establish Explicit Rules for Measuring Tools:**
* Add a directive to `scaffolding_designer` and `coder`: *When a measuring tape or ruler is depicted measuring an object, the tool must visibly originate at 0 and terminate/overlap at the measured value ($L$ or $C$), OR the tool should have blank ticks with an external dimension leader line indicating total measurement.*


* **Add a 2D vs. 3D Decision Gate for Geometric Properties:**
* Update `visual_director` and `image_detail_planner`: *For properties of cross-sections, perimeters, or flat projections (like a sliced orange, pizza, or cylinder base), prefer a flat 2D orthographic view with dimension arcs over a 3D perspective solid unless 3D volume is explicitly required.*


* **Separate Graphic Dimension Annotations from AI Render Prompts:**
* Do not ask the diffusion model to render text like "20 cm" onto textured surfaces. Instead, have the pipeline paint the clean, photorealistic/stylized fruit and tape, and composite vector labels/dimension arcs on top during post-processing.


* **Watch Out for Cross-Task Trade-Offs (System Trend Warning):**
* *Trade-off Risk:* Over-correcting to eliminate in-scene text can cause image generators to omit necessary scale context entirely. Ensure that when in-scene text is suppressed, the scaffolding/markup instructions explicitly mandate clean leader lines or bracket annotations instead.


Scaffolding Usable?: Maybe - would rather have 20 cm not printed on tape measure - placement makes math connection unclear
Illustration Usable?: Maybe - would rather have 20 cm not printed on tape measure - - placement makes math connection unclear
Other notes: 
General Problem Class: 

**General Problem Class:** Pedagogical Dimensioning & Measurement Indicator Semantics (Label vs. Object Literalism)

---

### What Went Wrong

The system generated an illustration where the label `"20 cm"` was rendered as literal, static text printed directly onto the yellow body of the tape measure (like a brand logo or static graphic), rather than functioning as an active **measurement readout** or dimension callout.

* **Pedagogical Ambiguity:** Printing `"20 cm"` directly on the tape makes it look like the tape measure itself is branded "20 cm" or has arbitrary text written on it, rather than showing the zero-mark aligned with the 20 cm tick mark at the overlap to indicate that the *circumference of the orange* equals 20 cm.
* **Loss of Mathematical Intent:** Instead of using standard textbook dimensioning conventions (e.g., an external dimension leader line labeled "Circumference = 20 cm", a bracket, or a realistic tape measure where the 0 and 20 cm tick marks align/meet at the seam), the model treated the measurement string purely as a decorative texture on the object.

---

### Agents Involved

1. **`markup_specialist` (`plan_markings`):** Recommended printing the text directly on the tape (`"with a clear text label reading '20 cm' printed along the tape"`) instead of specifying callout lines or explaining how a tape measure indicates measurement (alignment at overlap).
2. **`scaffolding_designer` (`design_scaffolding`):** Hardcoded the literal string directly onto the front band at `(0, -0.75)` on the tape measure geometry without leader lines or measuring end-caps.
3. **`coder` (`plan_logic` & `write_code`):** Placed `ax.text(0, -0.75, '20 cm', ...)` with a yellow bounding box directly on top of the tape path.
4. **`artist` (`plan_finishing` & `render_final`):** Instructed the image model to lock and render `"20 cm"` as static surface text on the tape.
5. **`review_math` & `image_verifier` / `issue_aggregator`:** Evaluated text legibility rather than pedagogical clarity, rubber-stamping the literal text placement as "mathematically precise."

---

### Root Causes in Agent Instructions

* **Literal Interpretation of Prompts in `plan_markings`:** The task prompt asks *"What labels, if any, would this sort of diagram normally have?"* but lacks guidance distinguishing **object markings** (labels on physical items) from **diagrammatic dimensioning** (dimension lines, callout arrows, leader text, or realistic measurement registration).
* **Oversimplification in Scaffolding Directives:** `design_scaffolding` instructs agents to *"keep things simple and strip away artistic flair"*, leading the agent to slap text onto the nearest polygon rather than generating standard textbook measurement cues (leader lines, offset callouts, dimension arcs).
* **Superficial "Math Check" Criteria:** In `math_check` and `review_math`, agents check whether numbers contradict the problem statement (e.g., "Is 20 cm present? Yes. Is the calculation plausible? Yes."), but do not evaluate **whether the visual representation conveys how the measurement is taken**.

---

### Suggestions for Improvement

* **Establish Dimensioning Standards in `markup_specialist`:**
* Explicitly instruct `markup_specialist` and `scaffolding_designer` on **Measurement vs. Labeling**: Measurements representing lengths, circumferences, or perimeters should prefer external leader lines, dimension callout brackets (e.g., $\leftrightarrow$), or clear start/end alignment indicators rather than embossing text onto the object itself.


* **Add a "Measurement Semantics" Check to `math_check` and `review_math`:**
* Add a check: *"Does the visual clearly explain WHAT quantity is being measured and HOW, or does it merely paint a number onto an object?"*


* **Refine `coder` & `scaffolding_designer` Tape/Ruler Conventions:**
* When real-world measuring tools (tape measures, rulers, scales) are used, ensure the instructions specify depicting the **measurement mechanism** (e.g., showing the start at 0 cm wrapping to the 20 cm tick mark at the overlap seam, or using an offset dimension callout pointing to the tape).


Scaffolding Usable?: No - a tape measure could not be layed out like that - physically impossible; only 12 units instead of 20
Illustration Usable?: No - a tape measure could not be layed out like that - physically impossible; only 12 units instead of 20
Other notes: the stretchy circular tape measure is not that bad imo. But having the wrong number of units is unforgivable, is there not a mathematical correctness check from some agent before coding?
General Problem Class: measurement off

Here is an analysis of the pipeline failure, the responsible agents, and how to harden the system against this class of error.

## What Went Wrong

The core failure was a mathematical disconnect between the visual representation of the measuring tool and its labeled value.

* To represent a 20 cm tape measure wrapping a full circle, the tape required exactly 20 major tick marks (one every 18 degrees).
* Instead, the scaffolding was built with 12 major tick marks (one every 30 degrees, mimicking a clock face).
* This 12-unit base was locked in during the python phase, forcing the downstream image generator to either hallucinate extra marks or render a physically impossible tape measure.

## Agents Involved

* **`scaffolding_designer`**: Devised the initial geometry but failed to map the 20 cm requirement to angular intervals, vaguely requesting ticks "every 6 degrees" (60 total ticks).
* **`coder`**: Arbitrarily programmed a major tick every 30 degrees (`if angle % 30 == 0`), resulting in exactly 12 major units.
* **`reviewer` (Task: `math_check`)**: Completely failed to flag the error. It explicitly counted the ticks but concluded there were "no strict mathematical conditions on the sizes," missing the direct contradiction with the 20 cm label.

## Root Causes in Instructions

* **No Pre-Coding Math Check:** The user observation is spot-on—there is no mathematical verification of the `scaffolding_blueprint`. The `math_check` only occurs *after* the code is written, meaning blueprint logic errors are already baked into the script.
* **Blind Spot for Scalar Tools:** The `math_check` prompt instructs the agent to "Recompute every quantity," but it lacks a specific directive to verify that the intervals on drawn measuring tools (rulers, graphs, tape measures) physically match their numeric labels.

## Suggestions for Improvement

* **Implement a Blueprint Review:** Add a `verify_blueprint` task assigned to a reviewer agent immediately after `design_scaffolding`. Catching proportional and interval errors before the `coder` attempts to implement them will prevent impossible geometry from reaching the Python stage.
* **Update the `math_check` Directive:** Amend the strict checks in the `math_check` instruction to explicitly mandate: *"When a measuring tool or graph is depicted, verify that the number of drawn subdivisions physically corresponds to the maximum labeled value (e.g., a 20cm ruler must have 20 major divisions)."*


Scaffolding Usable?: No - tape measure has 40 units broken up into thirds; a tape measure could not be layed out like that - physically impossible
Illustration Usable?: No - tape measure has 40 units broken up into thirds; a tape measure could not be layed out like that - physically impossible; texture of orange is weird
Other notes: texture not that bad, but units have to be right!
General Problem Class: measurement off

**General Problem Class**
**Physical Measurement & Scale Incoherence (Algorithmic Tick/Graduation Hallucination & Upstream Specification Drift)**

---

### What Went Wrong

1. **Nonsensical Ruler Subdivision & Unit Count:**
The prompt requested a tape measure indicating a circumference of **20 cm**.
* In `write_code`, the coder used `range(0, 360, 3)` with major ticks at `angle % 9 == 0`. This produced **40 major divisions**, each subdivided into **thirds** (3 minor ticks per major interval = 120 total ticks).
* A metric tape measure wrapping a 20 cm perimeter cannot have 40 units, nor are centimeters subdivided into thirds (metric units subdivide into tenths/millimeters or halves).


2. **Scaffolding vs. Reality Disconnect:**
The coder generated arbitrary decorative ticks rather than mapping real-world physical units (e.g., exactly 20 cm increments with millimeter or half-centimeter subdivisions).
3. **Multi-Agent QA Blindspot:**
The code review, mathematical verification, visual inspection, and final gatekeeper all rubber-stamped the tick marks as "realistic" or "cosmetic detail," failing to verify if the tick count and base subdivision made physical sense for a 20 cm tape.
4. **Diffusion Aesthetic Artifacts:**
The Image-to-Image pass preserved the broken mathematical ticks and added organic painterly noise, yielding an odd pulp/rind texture and jittered graduations.

---

### Involved Agents & Failure Points

| Agent | Task | Failure Point |
| --- | --- | --- |
| **`scaffolding_designer`** | `design_scaffolding` | Vaguely requested *"20 major/minor tick marks along the outer perimeter pointing outward at uniform angular intervals"*, failing to specify base units, subdivisions (e.g., mm / 0.5 cm), or start/end graduations. |
| **`coder`** | `plan_logic` & `write_code` | Implemented arbitrary loop bounds (`range(0, 360, 3)` and `% 9`), inventing 40 units divided by 3 instead of modeling a 20 cm scale. |
| **`reviewer`** | `logic_check` & `math_check` | Treated tick generation as decorative styling rather than checking whether the tick math matched the 20 cm measurement in the problem. |
| **`inspector`** | `verify_adherence` | Noticed there were ~120 ticks instead of 20, but marked it minor because it *"makes it look more realistically like a tape measure."* |
| **`inspection_manager`** | `consolidate_inspection` | Explicitly overruled the inspector's tick mark note with: *"The minor issue regarding the number of tick marks (120 instead of 20) is noted, but this deviation improves the diagram..."* |
| **`image_verifier` / `issue_aggregator**` | `review_math` / `aggregate_feedback` | Accepted the final render because the text label read "20 cm," ignoring the physical impossibility of the ruler markings behind it. |

---

### Root Causes in Agent Instructions

1. **"Scaffolding vs. Decoration" Loophole:**
* Agent guidelines emphasize that *"cosmetic details can be estimated"* and that the Python script is just *"scaffolding."* Coder and Reviewer agents classified ruler graduations as decorative texture rather than **mathematical data**.


2. **Missing Real-World Unit & Tool Grounding:**
* No directive instructs `scaffolding_designer` or `coder` on how standard measurement tools (rulers, protractors, clocks, thermometers, gauges) work (e.g., metric = base 10/base 2 subdivisions; clocks = base 12/60; protractors = base 10/180).


3. **Superficial Mathematical QA (`math_check` / `review_math`):**
* Reviewer instructions focus heavily on high-level quantities (total label matching "20 cm", sum of angles = 360°). They lack specific checks for **internal scale consistency** (e.g., counting graduation intervals between 0 and total value).


4. **Managerial Leniency Bias on Retries/Scaffolding:**
* `consolidate_inspection` is told *"cosmetic blemishes the artistic pass will cover don't matter."* This caused the manager to actively defend arbitrary code parameters as a visual improvement rather than a mathematical flaw.



---

### Suggestions for Improvement & Systemic Tradeoffs

* **Introduce a "Measurement Tools & Gauges" Standard Directive:**
* **Action:** When measurement apparatuses (rulers, tape measures, gauges, clocks, number lines) are present, enforce strict mathematical alignment:

$$\text{Major Intervals} = \text{Total Displayed Units}$$


$$\text{Minor Subdivisions} \in \{2, 5, 10\} \text{ (for metric/standard decimal)}$$


* **Tradeoff / Risk:** Over-specifying minor details in prompts can cause coders to spend excessive token budgets generating hundreds of tick lines, occasionally causing label clutter or Matplotlib rendering slowdowns.


* **Tighten `math_check` for Visual Markings & Measuring Scales:**
* **Action:** Add an explicit verification item to `math_check` and `review_math`: *"If a measurement scale, ruler, grid, or axis is shown, calculate the number of subdivisions per major unit. Do they conform to real-world standard units (e.g., base-10 for metric, base-12/16 for imperial)?"*
* **Tradeoff / Risk:** Reviewers might become overly pedantic and reject harmless illustrative sketches or stylized background elements that do not impact the core math problem.


* **Constrain `inspection_manager` Overrule Authority on Numerical Quantities:**
* **Action:** Update `consolidate_inspection` so that discrepancies in **quantities, counts, and scale graduations** cannot be dismissed as "cosmetic improvements."
* **Tradeoff / Risk:** May trigger more retry loops in Stage 3, increasing token costs and latency for minor visual discrepancies.


* **Explicit Texture/Shading Directives for Organic Cutaways:**
* **Action:** In `plan_finishing`, ensure prompts for organic cross-sections (like citrus fruit) explicitly define standard botanical features (e.g., distinct translucent pulp vesicles, clean segment borders) to prevent generative diffusion from rendering strange, muddy, or synthetic-looking pulp textures.
* **Tradeoff / Risk:** Highly prescriptive texture prompts can sometimes conflict with ControlNet constraints, leading to edge artifacts around the boundary rings.


Scaffolding Usable?: No - a tape measure could not be layed out like that - physically impossible; only 12 units instead of 20
Illustration Usable?: No - a tape measure could not be layed out like that - physically impossible; only 12 units instead of 20
Other notes: circular tape measure not that bad, wrong units unforgivable
General Problem Class: measurement off

Here is a breakdown of the pipeline failure based on the user feedback and project history.

### What Went Wrong

The system generated an illustration of a tape measure with a glaring logical and mathematical flaw: the tape measure was labeled "20 cm" for the full circumference, but the physical tick marks drawn on the tape divided the circle into exactly 12 units.

Because a full circle is 360 degrees, placing a major tick mark every 30 degrees (as instructed by the blueprint) results in 12 major divisions. The AI essentially drew a clock face (12 hours) and labeled it as a 20-centimeter measuring tape, creating a visual contradiction that ruined the educational utility of the diagram.

### Agents Involved

1. **`scaffolding_designer` (The Architect of the Flaw):** This agent created the geometric blueprint. It correctly deduced that a circle has 360 degrees but lazily defaulted to standard geometric divisions (minor ticks every 6°, major ticks every 30°) instead of calculating the degrees needed to represent 20 units (360° / 20 = 18° per major tick).
2. **`coder` (The Faithful Executor):** Wrote the Python code exactly as instructed by the blueprint, implementing the 30-degree loop.
3. **`reviewer` [math_check] (The Missed Catch):** The math reviewer verified that the Python loop correctly divided 360 by 30, but failed to cross-reference that resultant count (12) against the physical reality of the "20 cm" label in the prompt.
4. **`image_verifier` [review_math] (The Final Oversight):** The final visual math check noted the presence of the tick marks and the "20 cm" label, but failed to actually count the major units to ensure they aligned with the numeric label.

### Root Causes in Agent Instructions

* **Abstract Geometry vs. Physical Reality:** The `scaffolding_designer` is instructed to "Identify the Math: What counts... must be perfectly accurate?" but lacks a specific directive connecting abstract geometric shapes to physical measurement tools. It treats a circle as a geometric object (360 degrees) rather than a physical tape measure (20 cm).
* **Shallow Mathematical Verification:** The `math_check` instruction asks the agent to "Recompute every quantity the code hard-codes... Do they match what the problem's math actually implies?" The agent interpreted this purely mechanically—verifying the code's internal consistency (that a 30-degree step works in a 360-degree circle) rather than contextually (that 12 ticks do not equal 20 cm).
* **Visual QA Blindspot:** The visual inspectors are trained to look for overlaps, text legibility, and artifacts, but are not explicitly instructed to count drawn units on scales or measuring tools to ensure they match text labels.

### Suggestions for Improvement

When making prompt adjustments across a system of this scale, it is crucial to avoid "whack-a-mole" fixes that create new hallucinations.

1. **Update `scaffolding_designer` (Measurement Tool Directive):**
Instead of adding a hyper-specific rule about tape measures, add a general directive regarding scales and measurement tools.
* *Suggested Addition:* "When drawing rulers, measuring tapes, graphs, or scales, you must mathematically calculate the exact number of tick marks or divisions required to match the stated numeric labels. (e.g., A 20cm tape measure requires exactly 20 major divisions)."


2. **Update `reviewer` [math_check] (Visual Counting Verification):**
Force the math reviewer to explicitly connect drawn elements to labels.
* *Suggested Addition:* "Scale & Unit Verification: If the diagram includes a scale, ruler, grid, or repeated tick marks, manually count the number of divisions generated by the code loops. The number of drawn divisions MUST perfectly match the numeric labels describing them."


3. **Caution on Visual QA:** Do not rely on `image_verifier` to count 20 tiny tick marks on a rendered image, as vision models struggle with dense, exact counting. The fix must be caught in Stage 3 (Base Diagram Generation) by the `reviewer` before the image is ever rendered.


Scaffolding Usable?: NA
Illustration Usable?: No - Inaccurate - top of tape measure is not in the middle of the; 20 cm label is in misleading position on ruler; centimeters divided up into eigths
Other notes: OBVIOUSLY has actual measurements in it, how is it getting classified as illustrative only?
General Problem Class: wrong problem classification

### Summary and Analysis

**What Went Wrong**
The image was erroneously routed to `DIRECT_IMAGE_GEN`, completely bypassing the Python-based precision scaffolding stage. Generative image models notoriously struggle with precise typography and accurate scale markings. Because it was drawn entirely by the AI Image Generator, the tape measure suffered from severe hallucinations: mixing imperial subdivisions (eighths) with metric units (centimeters) and misplacing the "20 cm" label. Furthermore, the QA verification agents failed to catch these specific geometric and typographical errors, approving a mathematically inaccurate image.

**Agents Involved**

* **`selector`:** Made the critical error in the `choose_path` task by routing the request to `DIRECT_IMAGE_GEN` instead of `STANDARD_DIAGRAM`.
* **`image_detail_planner`:** Correctly identified the danger during the `review_request` task, noting: *"Generative AI models typically struggle to accurately render precise ruler tick marks on curved surfaces."* However, this insight was lost downstream.
* **`artist`:** Hallucinated the metric subdivisions and text placement (expected behavior for a pure diffusion model asked to draw a ruler without a ControlNet anchor).
* **`image_verifier` (Math):** Failed to notice that the tape measure subdivisions were mathematically incorrect (divided into eighths instead of tenths) and that the text was misaligned.

**Root Causes in Agent Instructions**

1. **Context Loss:** The `selector` agent's history scope (`STAGE3_AGENTS`) prevents it from seeing the output of the Stage 2 `image_detail_planner`. The `selector` only saw `{latest_description}` and completely missed the earlier warning that precise tick marks were required.
2. **Vague Path Definitions in `choose_path`:** The instructions for `choose_path` do not explicitly warn the `selector` that measuring tools (rulers, protractors, tape measures) require mathematical accuracy. The `selector` assumed that because it was an organic object (an orange), it should skip the coding stage.
3. **QA Blind Spots in `review_math`:** The `review_math` prompt instructs the verifier to check if "drawn numbers contradict the problem", but does not explicitly instruct it to verify the *structural validity* of measuring tools (e.g., checking if metric rulers have 10 subdivisions).

### Suggestions for Improvement

* **Pass Technical Flags to the Selector:** The output from the `review_request` task (`requires_technical`, `requires_artistic`, and `reasoning_technical`) should be templated directly into the `choose_path` prompt. If the Stage 2 planner flags an image as requiring technical precision, the `selector` needs to know *why* before it makes a routing decision.
* **Update `choose_path` Guidelines:** Explicitly define "measuring instruments (rulers, tape measures, protractors, thermometers) with specific tick marks or measurement readouts" as a hard trigger for the `STANDARD_DIAGRAM` path.
* **Enhance Math QA Instructions:** Update the `review_math` instruction in the `image_verifier` prompt to explicitly state: *"Verify the structural accuracy of any measuring tools depicted (e.g., ensure metric rulers are divided into tenths, not eighths, and that labels align perfectly with their corresponding tick marks)."*
* **Re-evaluate 3D vs. 2D Trade-offs:** The `merge_plans` task resolved a conflict by forcing a 3D perspective to show the tape wrapping around the orange. While aesthetically pleasing, wrapping a mathematically accurate tape measure in 3D using Matplotlib is exceptionally difficult for the coder agent. The instructions for `merge_plans` may need a stronger directive: if an object requires exact technical markings, default to a 2D orthographic or cross-sectional view to guarantee programmatic accuracy.


Scaffolding Usable?: NA
Illustration Usable?: No - placement of 20 cm marker is weird and confusing
Other notes: not terrible for - a no scaffolding, but it shouldn't have been classified that way
General Problem Class: wrong problem classification

**Analysis of Pipeline Execution Failure**

**What Went Wrong**

1. **Misclassification & Workflow Bypass:** The pipeline routed the request to `DIRECT_IMAGE_GEN` instead of building a programmatic base diagram (`STANDARD_DIAGRAM`). This bypassed Python/Matplotlib scaffolding entirely.
2. **Measurement & Marker Placement Failure:** Without geometric scaffolding, the diffusion model was forced to free-draw the tape measure, curvature, and label placement. This resulted in an awkward, confusing placement of the "20 cm" marker along the rim/equator.
3. **Upstream QA Leniency:** Stage 5 reviewers (`image_verifier` and `issue_aggregator`) evaluated the second render as mathematically and aesthetically sound, failing to detect that the marker placement lacked educational utility and clean alignment.

---

**Agents Involved**

* **`selector` (`choose_path`):** Selected `DIRECT_IMAGE_GEN` based on the premise that organic textures (citrus pulp, peel) cannot be drawn in Matplotlib, ignoring the need for precise geometric placement of the circumference loop and label.
* **`image_detail_planner` (`review_request`):** Correctly identified both `requires_technical: true` and `requires_artistic: true`, but the downstream selector overrode the technical necessity.
* **`artist` (`plan_finishing` & `render_final`):** Had to generate both the organic subject and the spatial measurement marker simultaneously in text-to-image mode.
* **`image_verifier` & `issue_aggregator` (`review_math`, `aggregate_feedback`):** Passed the second render because the text was legible and the geometry was physically possible, overlooking whether the marker placement made intuitive mathematical sense.

---

**Root Causes in Agent Instructions**

* **Over-weighting Organic Texture over Geometric Anchor in `choose_path`:**
* The prompt for `choose_path` allows `DIRECT_IMAGE_GEN` when a request is "purely artistic, with no underlying geometric scaffolding required."
* The selector agent justified its choice with: *"Python/Matplotlib cannot easily render realistic organic citrus textures... DIRECT_IMAGE_GEN is the appropriate workflow."*
* *Instruction Flaw:* The selector conflated "final image needs organic textures" with "no scaffolding required." The system is explicitly designed for a two-pass approach where Python draws abstract primitives/guides (like a circle or ellipse with dimension arcs) and the AI paints organic textures over it.


* **Ambiguity in Direct vs. Scaffolding Scope:**
* The prompt did not clearly instruct `selector` that **any specific measurement annotation, tape line, or dimensional indicator** mandates a scaffolding path (`STANDARD_DIAGRAM`), regardless of the subject's realism.


* **Review Rubric Anchoring on Legibility instead of Visual Utility:**
* In `review_math`, the agent verified that $C = 20 \text{ cm} \implies D \approx 6.4 \text{ cm}$ was realistic, but did not check if the tape measure's end-marker and scale layout actually conveyed circumference clearly to a student.



---

**Suggestions for Improvement**

* **Hard Rule for Workflow Routing in `choose_path`:**
* *Update:* Add an explicit condition: *"If the diagram requires specific measurement lines, tape measures, dimension arrows, ticks, or numeric callouts on curved surfaces/geometry, you MUST choose `STANDARD_DIAGRAM` (or `COMPOSITE_PRIMITIVES`), even if the underlying object is organic (e.g., fruit, animals, terrain). Python must lay down the geometric bounding guides and label coordinates."*
* *Trade-off / Caution:* Forcing simple illustrations into `STANDARD_DIAGRAM` increases execution time and token cost for simple scenes, but prevents diffusion text/spatial hallucinations on measured word problems.


* **Reconcile `review_request` Output with `choose_path`:**
* *Update:* In the orchestrator logic or `choose_path` prompt, establish that if `review_request` flagged `requires_technical: true`, `DIRECT_IMAGE_GEN` is disallowed unless explicitly downgraded with justification.
* *Trade-off / Caution:* Ensures consistency across Stage 2 and Stage 3 decisions, eliminating pipeline contradictions.


* **Tighten Mathematical Annotation Review in `review_math`:**
* *Update:* Add a checklist item for measurement readability: *"Are measurement indicators (rulers, tape measures, dimension lines) positioned so that start/end points and total extents are unambiguous and intuitive?"*
* *Trade-off / Caution:* Stricter criteria might trigger extra retry loops on minor styling quirks, so severities must distinguish between confusing marker placement (MAJOR) versus slight tape curvature imperfection (MINOR).


Scaffolding Usable?: NA
Illustration Usable?: No - Inaccurate - top of tape measure is not exactly in the middle of the orange; 20 cm label is in misleading position on ruler; centimeters divided up into eighths
Other notes: 
General Problem Class: wrong problem classification

**Executive Summary & Pipeline Analysis**

The user rejected the generated illustration due to three distinct physical and mathematical inaccuracies:

1. **Misalignment:** The top of the tape measure was not positioned exactly along the equatorial center of the sphere.
2. **Misleading Scale Placement:** The `20 cm` label was placed misleadingly along the ruler rather than indicating the true wrap-around endpoint/total circumference measurement.
3. **Invalid Measurement Subdivisions:** The ruler marked centimeters divided into **eighths** (customary/inch fraction subdivisions) rather than metric **tenths (millimeters)**.

The systemic failure occurred because the system misclassified a precise measurement diagram as a purely artistic illustration (`DIRECT_IMAGE_GEN`), bypassing Python scaffolding and leaving fine geometric alignment and physical ruler semantics entirely to a stochastic diffusion model.

---

### Breakdown of What Went Wrong

* **Workflow Path Failure (`DIRECT_IMAGE_GEN` vs. `STANDARD_DIAGRAM`):** The pipeline bypassed Stage 3 programmatic drafting. Generative image models cannot be trusted to draw mathematically correct ruler tick-mark subdivisions (dividing a base-10 metric unit into 10 mm rather than 8 fractions) or precisely align tangent ribbon geometry to an exact sphere radius.
* **Semantic Ruler Misunderstanding:** Both the planning and prompt-generation agents treated the measurement `20 cm` as a floating text label to paste at the "front center" of the tape. In a physical tape measure wrapping a circumference, the front center would represent halfway ($10\text{ cm}$) or an arbitrary point, whereas the $20\text{ cm}$ mark should align with the zero index / overlapping seam.
* **Stage 5 Verification Blindspot:** All four QA checkers (`review_bias`, `review_aesthetics`, `review_safety`, `review_math`) and the `issue_aggregator` waved the image through on attempt 1. `review_math` confirmed that $20\text{ cm}$ was a realistic circumference for an orange, but completely failed to inspect the actual ruler tick marks, the physical meaning of the label location, or the equator alignment.

---

### Agents Involved & Decision Chain

| Agent / Step | Role in Failure | Impact |
| --- | --- | --- |
| **`image_detail_planner`** (`review_request`) | Identified that text/tape on curved surfaces is hard for AI, yet failed to mandate programmatic geometry. | Mixed signals allowed downstream bypass. |
| **`selector`** (`choose_path`) | Selected `DIRECT_IMAGE_GEN` on the grounds that an orange is an organic object. | **Critical failure.** Bypassed Matplotlib scaffolding where ruler subdivisions and equator alignment could be rendered with exact math. |
| **`artist`** (`plan_finishing`) | Instructed the prompt to place `"20 cm"` "prominently right at the front-center of the tape measure." | Prompted the model to draw a nonsensical ruler index. |
| **`image_verifier`** (`review_math`) | Evaluated only global plausibility (is 20 cm a reasonable size for an orange?) rather than inspecting tick marks and scale alignment. | Missed eighths vs. tenths subdivision and alignment errors. |
| **`issue_aggregator`** (`aggregate_feedback`) | Blindly trusted unanimous positive reports without scrutinizing the measurement instrument. | Approved a flawed asset. |

---

### Root Causes in Agent Instructions

**1. Weak Routing Thresholds in `choose_path**`

* **The Flaw:** `choose_path` instructions state that `STANDARD_DIAGRAM` is for "(Geometry, Graphs, Physics)" while `DIRECT_IMAGE_GEN` is for when "the request is purely artistic, with no underlying geometric scaffolding required."
* **Why it Failed:** Because the prompt featured a fruit, the agent treated it as "purely artistic," forgetting that **a measuring instrument (ruler/tape/scale) is a precision geometric tool**. If a measuring device appears, generative models will almost always hallucinate ticks (inches vs. cm, eighths vs. tenths, inconsistent spacing).

**2. Naive Label Placement Directives in `plan_finishing` & `merge_plans**`

* **The Flaw:** The prompt encouraged placing labels "prominently at the front-center for legibility."
* **Why it Failed:** Placing a total circumference number in the middle of a continuous measuring tape makes no physical or mathematical sense. The instructions lacked explicit guidance on **measuring tool semantics** (e.g., zero-origin, end-point matching, increment scale).

**3. Shallow Verification Checklist in `review_math**`

* **The Flaw:** `review_math` checks if "labels are legible" and "relative sizes match values," but lacks explicit heuristics for **instrument correctness** (e.g., "If a ruler/scale is depicted, count/verify tick subdivisions and units").

---

### Systematic Suggestions for Improvement

* **Enforce Instrument-Based Routing Heuristics:**
Update `choose_path` to explicitly mandate `STANDARD_DIAGRAM` (or a hybrid Matplotlib overlay) whenever **measuring tools (rulers, tape measures, protractors, balances, thermometers, coordinate axes)** are present. Image models should not be tasked with generating tick increments from scratch.
* **Add Physical Measurement Tool Semantics to `markup_specialist` / `educator`:**
Add a specific directive for measuring instruments:
> *"When displaying a measurement tool (e.g., tape measure, ruler), ensure the placement of the number reflects actual measurement logic (e.g., total circumference must align at the overlap/zero mark, metric scales must divide into 10 mm per cm, imperial into 8ths/16ths)."*


* **Tighten `review_math` & `inspector` Visual Inspection Heuristics:**
Add explicit checks into `review_math`:
1. *Subdivision Check:* Do unit subdivisions match the standard system (10 subdivisions for metric cm/mm)?
2. *Scale Logic Check:* Does the position of the numerical value on the measuring tool represent where that measurement is actually taken?
3. *Geometric Tangency & Centering:* Are dimension lines or wrapped bands aligned with the geometric centroid/equator of the target solid?


* **Decouple Organic Subjects from Geometric Markups:**
For problems combining organic illustrations with precision callouts (e.g., fruit with a tape measure, person with height marker), establish a **Hybrid Generation Contract**: Python renders the mathematically exact background/foreground vector measuring ribbon (with exact millimeter ticks and labels), while the diffusion model fills the organic subject bounded inside the scaffolding.


Scaffolding Usable?: NA
Illustration Usable?: No - Ruler is inconsistently broken up into ninths and eights; some tick marked not equally spaced; 20 cm label is not placed clearly.
Other notes: shouldnt be image only
General Problem Class: wrong problem classification

### Incident Summary & Failure Analysis

The pipeline misclassified a measurement-critical diagram as purely artistic, skipping the Python/Matplotlib scaffolding step (`choose_path` chose `DIRECT_IMAGE_GEN`). Delegating the tape measure's tick marks, subdivisions, and label placement directly to the diffusion image generator (`gemini-3-pro-image`) resulted in non-uniform tick intervals (erratic eighths/ninths divisions), uneven spacing, and poor label alignment. Furthermore, the downstream verification agents suffered from severe confirmation bias, issuing false-positive passes across all QA checks.

---

### Key Agents Involved

* **`selector` (`choose_path`)**: The primary point of failure. It reasoned that because an orange is an organic object, the task was pictorial rather than geometric, ignoring the strict mathematical requirement of a calibrated measuring tape.
* **`image_detail_planner` (`review_request`, `situational_planning`)**: Correctly identified that `requires_technical: true` and that generative models struggle with curved measurement markings, but failed to enforce code-scaffold constraints downstream in the workflow selection.
* **`image_verifier` (`review_math`, `review_aesthetics`)**: Visual QA blindness. Hallucinated perfection (claiming tick marks and text placement were crisp and accurate) and failed to audit the actual graduation intervals on the tape measure.
* **`issue_aggregator` (`aggregate_feedback`) & `final_reporter**`: Rubber-stamped the flawed QA reports, producing a self-congratulatory post-mortem that failed to catch obvious physical rendering defects.

---

### Root Causes in Agent Instructions

* **Ambiguity in `choose_path` criteria**: The prompt states `STANDARD_DIAGRAM` is for *"Geometry, Graphs, Physics"* and `DIRECT_IMAGE_GEN` is for *"purely artistic, with no underlying geometric scaffolding required."* The presence of an organic subject (an orange) overshadowed the presence of a measuring instrument (a ruler/tape). The prompt lacks an explicit rule: **if an image contains any calibrated measuring tool, scale, axis, or ruler, it cannot be `DIRECT_IMAGE_GEN**`.
* **Lax definitions of "Technical Precision" in Stage 3 handoffs**: While `image_detail_planner` flagged technical requirements during Stage 2, `choose_path` in Stage 3 operates in isolation without an explicit boolean override inheriting `requires_technical`.
* **Superficial VQA Verification Rubrics in `review_math**`: The instructions for `review_math` ask high-level questions (*"Are labels legible and correctly placed? Do relative sizes match?"*) rather than rigorous verification steps (*"Count and verify tick intervals between major marks; check whether subdivisions follow standard decimal or fractional increments"*).

---

### Architectural & Prompt Improvement Suggestions

| Area | Recommendation | System Tradeoff / Risk |
| --- | --- | --- |
| **Workflow Routing (`choose_path`)** | **Hard Rule / Constraint Injection:** Add an explicit gate: *"If the diagram includes any measurement apparatus (ruler, tape measure, protractor, gauge, coordinate axis), it MUST NOT use `DIRECT_IMAGE_GEN`."* | **Over-scaffolding:** Might force simple illustrative requests featuring incidental background items to generate unnecessary, clunky Python scaffolding. |
| **State Inheritance** | **Propagate `requires_technical`:** Ensure that if `review_request` outputs `requires_technical: true`, `choose_path` is restricted from choosing `DIRECT_IMAGE_GEN` without a high-confidence override. | **False Constraints:** If `review_request` is overly sensitive, the pipeline loses the ability to fast-track purely illustrative prompts. |
| **VQA Audit Steps (`review_math`)** | **Mandate Step-by-Step Counting:** Require the verifier to explicitly count tick marks, transcribe every visible number in sequence, and check graduation spacing before setting `passed_math_check`. | **Latency & Token Cost:** Increases verification latency and may trigger false rejections if the model over-analyzes minor aesthetic variations in hand-drawn styles. |
| **Composite Architecture** | **Hybrid Rendering Path:** For organic objects with technical overlays, draw the technical instrument via Python/SVG (or composite a real ruler asset) onto the AI-generated illustration rather than forcing pure img2img or pure Matplotlib. | **Pipeline Complexity:** Adds a secondary composition/blending stage and image masking overhead into n8n workflows. |


Scaffolding Usable?: NA
Illustration Usable?: No - ruler broke up into eigths; placement of 20 cm label confusing
Other notes: 
General Problem Class: wrong problem classification

### Post-Mortem Analysis

**What Went Wrong**

* **Misclassification of Workflow:** The pipeline selected `DIRECT_IMAGE_GEN` instead of constructing a programmatic base diagram (`STANDARD_DIAGRAM` or `COMPOSITE_PRIMITIVES`), relying entirely on the generative diffusion model for measurement markings and numeric precision.
* **Corrupted Ruler Subdivisions:** The tape measure rendered with stylized, arbitrary tick marks broken into eighths (or imperial-style fractions) despite labeling metric centimeters ($20\text{ cm}$).
* **Ambiguous Label Placement:** The text "20 cm" appeared as a floating, decorative graphic rather than aligning with an actual $20\text{ cm}$ endpoint or hash mark on the ruler ribbon.
* **Superficial QA Passing:** Reviewers and the Gatekeeper treated the ruler's nonsensical tick marks and disconnected measurement label as "cosmetic stylization" rather than a critical educational math defect.

---

### Agents Involved

| Agent | Task | Responsibility for Failure |
| --- | --- | --- |
| **`selector`** | `choose_path` | Routed the run to `DIRECT_IMAGE_GEN` solely due to "organic textures," ignoring that rulers, ticks, and exact lengths require programmatic scaffolding. |
| **`image_detail_planner`** | `review_request` / `merge_plans` | Correctly identified that text and tick marks require technical precision, yet failed to mandate programmatic/scaffold constraints. |
| **`artist`** | `plan_finishing` / `render_final` | Instructed the diffusion model to generate "crisp black millimeter and centimeter tick marks," which diffusion models notoriously hallucinate. |
| **`image_verifier`** | `review_math` | Downgraded nonlinear/broken tick marks and disconnected "20 cm" text to `MINOR` issues across all retry attempts. |
| **`issue_aggregator`** | `aggregate_feedback` | Overruled the math reviewer's minor flags as "negligible artistic interpretations" and approved a mathematically misleading image. |

---

### Root Causes in Agent Instructions

* **Vague `DIRECT_IMAGE_GEN` Criteria in `choose_path`:**
The instructions define `DIRECT_IMAGE_GEN` as *"purely artistic, with no underlying geometric scaffolding required."* However, `selector` weighted the presence of organic shapes (fruit pulp, peel) over the presence of measurement tools (rulers, scales), defaulting to direct image generation whenever organic assets were requested.
* **Permissiveness of "Scaffolding as Cosmetic Anchor":**
`review_math` and `aggregate_feedback` instructions explicitly encourage leniency on attempt 3+: *"Attempt 3 or later: pass a good-enough image rather than quibble over minor details."* The agents lacked a clear definition distinguishing "cosmetic flair" from "functional measurement integrity."
* **Missing Tool-Specific Negative Constraints in `artist`:**
`plan_finishing` did not provide explicit instructions on how a tape measure indicates total circumference (e.g., end-to-end alignment vs. an annotated dimension line vs. a labeled callout with leader arrows).

---

### Systemic Improvement Suggestions & Trade-Offs

**1. Tighten Routing Heuristics for Measurement Tools (`choose_path`)**

* *Action:* Add an explicit exclusion to `DIRECT_IMAGE_GEN`: if any measuring device (ruler, tape measure, protractor, thermometer, graduated cylinder, clock face) or exact numbered axis is present, the pipeline **must not** route to `DIRECT_IMAGE_GEN`.
* *Trade-Off:* Forces hybrid matplotlib generation for complex 3D objects, which may increase base diagram coding failures or require primitive compositing (`COMPOSITE_PRIMITIVES`).

**2. Separate Text Markup from Object Generation via Callouts**

* *Action:* For 3D or organic items where a physical ruler is difficult to draw programmatically, prompt the artist to depict an uncalibrated ribbon/band and place the numerical measurement ($20\text{ cm}$) in an external, unambiguous dimension callout/arrow rather than embedding false tick marks on the ribbon.
* *Trade-Off:* Reduces tactile visual flavor (a tailor's tape looks more contextual than a plain dimension arrow), but prevents hallucinated tick divisions.

**3. Redefine "Mathematical Defect" Thresholds in QA (`review_math` & `aggregate_feedback`)**

* *Action:* Explicitly state in `review_math` that **hallucinated, uneven, or incorrect scale divisions on measuring instruments are `CRITICAL` math defects**, never `MINOR` aesthetic variations.
* *Trade-Off:* Increases retry rates and pipeline termination costs if generative image models repeatedly fail to render fine line scales.

**4. Scaffolding-First Calibration for Rulers**

* *Action:* When measurement instruments are involved, enforce Matplotlib to generate the precise scale markings and label positions on the base canvas so the Image-to-Image pass merely shades or styles them without inventing increments.
* *Trade-Off:* Requires `scaffolding_designer` and `coder` to handle 2D projections/arcs of curved rulers in Matplotlib.


Problem 4 user notes:
- problems with rulers or other measurement devices with marks on them are definitely something we should have specific instructions for
- this problem OBVIOUSLY shows specific math quantities, how is it getting classified as image gen only so often?



**Problem 5**



Scaffolding Usable?: Yes
Illustration Usable?: No - a woven square is not really the same thing as a bag
Other notes: not that bad
General Problem Class: aesthetics poor

**Failure Analysis: Woven Patch/Square vs. Bag Representation**

---

### What Went Wrong

The mathematical layout and object counts succeeded (Bag 1 had 1 star / 4 moons; Bag 2 had 3 stars / 1 moon with zero overlaps). However, the visual semantic representation of the container failed:

* **Container Degeneration:** The two "bags" were rendered as flat, rigid rectangular cloth patches/swatches with stitched perimeter borders rather than recognizable pouches, sacks, or open-top drawstring bags.
* **Loss of Concept Utility:** While technically functioning as bounding boxes for counting, calling them "bags" in the text while illustrating flat square coasters/placemats degrades the aesthetic quality and educational intuition of drawing objects from a container.

---

### Agents Involved

| Agent | Task | Failure Contribution |
| --- | --- | --- |
| **`scaffolding_designer`** | `design_scaffolding` | Abstracted "bag" into a strict geometric `rounded rectangle centered at (x=3, y=2.5) with width=4.0, height=3.5`. |
| **`coder`** | `write_code` | Implemented this literal abstraction using `patches.FancyBboxPatch`, creating a rigid rectangular boundary with a heavy stroke border. |
| **`artist`** | `plan_finishing` & `render_final` | Instructed the image generator to strictly anchor to the rectangular bounding box ("*Transform the flat beige rounded rectangles into flat, tactile canvas bags with subtle fabric texture and neat stitched borders*"), forcing the diffusion model to skin the rectangle as a stitched square patch rather than generating a natural bag silhouette. |
| **`image_verifier`** / **`issue_aggregator`** | `review_aesthetics` / `aggregate_feedback` | Rubber-stamped the output (the verifier explicitly noted *"The image depicts two square cloth bags"* and passed aesthetics without flagging that square patches do not look like bags). |

---

### Root Causes in Agent Instructions

* **Over-Constrained Shape Anchoring:**
In `plan_finishing`, Directive 1 mandates: *"Explicitly instruct the Artist AI to lock the exact geometry, aspect ratios, spatial relationships... of the base diagram."* When combined with `scaffolding_designer` providing a literal bounding box, the artist interpreted the rectangular border of the scaffold as a strict outer edge of the object itself rather than a loose container guide.
* **Primitive Blueprint Ambiguity:**
`scaffolding_designer` is instructed to *"Strip away all artistic flair... describe ONLY geometric shapes."* It lacked guidance on how to represent open containers vs. bounding areas (e.g., drawing an open pouch contour or omitting the container boundary entirely in Python if the artist can draw a bag behind fixed item coordinates).
* **QA Bias Toward Checklist Compliance:**
`image_verifier` (`review_aesthetics`) checks for artifacts, color harmony, and text readability. It does not explicitly evaluate **semantic believability** (i.e., *"Does object X actually look like what the real-world problem describes, or is it an uncanny geometric surrogate?"*).

---

### Actionable Suggestions & System Trade-Offs

**1. Scaffolding Container Strategy: Bounding Areas vs. Physical Contours**

* *Suggestion:* When scaffolding non-rigid containers (bags, bowls, baskets, pockets), instruct `scaffolding_designer` and `coder` to either:
* Plot the objects in position and draw only a faint/dashed region guide or open U-curve/cinch top rather than a rigid closed `FancyBboxPatch`.
* Alternatively, designate the container as an *Artistic Addition*—plot the item coordinates and labels, but leave the container shape to the image model.


* *Trade-off / Risk:* Removing container borders in the scaffold risks the generative model placing items outside the bag or grouping both sets into a single container if spatial separation isn't heavily prompted.

**2. Relax Non-Critical Geometry in `plan_finishing**`

* *Suggestion:* Update `plan_finishing` directives to distinguish between **Strict Anchors** (item counts, coordinate centers, graph axes, mathematical angles) and **Loose Context Anchors** (container outlines, background silhouettes). Explicitly allow the artist to morph rectangular bounding regions into organic object shapes (e.g., canvas sacks, drawstring pouches, glass jars).
* *Trade-off / Risk:* Giving the diffusion model freedom on container silhouettes can occasionally cause it to clip or overlap edge items if the bag cinch narrows too much.

**3. Add Semantic Fidelity Check to `review_aesthetics**`

* *Suggestion:* Add a specific check to `review_aesthetics`: *"Do illustrated physical objects look like authentic, recognizable real-world items rather than flat geometric approximations (e.g., a bag looking like a bag rather than a square swatch)?"*
* *Trade-off / Risk:* May increase retry loop frequency and token/render costs on minor aesthetic stylizations that are technically acceptable.


Scaffolding Usable?: Yes
Illustration Usable?: Yes
Other notes: 
General Problem Class: 

**Run Summary & Analysis: Discrete Probability Diagram (Two Bags, Star & Moon Blocks)**

### 1. What Went Wrong (and What Went Right)

* **Zero Critical or Pipeline Failures:** The execution was end-to-end successful on its first attempt across all 6 stages with zero retry loops ($0.29 total cost).
* **Minor Agent Friction (Sub-optimal Markup Proposal):** In Stage 2, the `markup_specialist` recommended adding explicit text labels for block counts inside the bags (e.g., `"1 Star"`, `"4 Moon blocks"`). While not a failure, this introduced unnecessary text clutter for a diagram whose main utility is visual counting. The `image_detail_planner` successfully caught and overruled this during `merge_plans`.
* **Slight Review Ambiguity in Reviewer:** In `syntax_check`, the reviewer noted that `ax.set_xlim(0, 5/3)` and `ax.set_ylim(0, 1)` were called, questioning if it violated the execution wrapper constraints before determining it was safe to pass.

---

### 2. Agents Involved

* **`problem_validation` & `selector`:** Successfully identified the discrete counting requirements and routed to `COMPOSITE_PRIMITIVES`.
* **`markup_specialist` vs. `image_detail_planner`:** Conflict over text count annotations resolved cleanly by the manager.
* **`coder`, `reviewer`, & `review_manager`:** Seamless code authoring with custom Path objects; math and logic verified accurately.
* **`inspector`, `inspection_manager`, `artist`, `image_verifier`, & `issue_aggregator`:** Base diagram passed inspection, artist anchored exact counts via img2img prompt, and QA approved all metrics.

---

### 3. Root Causes in Agent Instructions

* **`plan_markings` lacks clear suppression guidance:** The prompt asks *"Does it actually warrant labels?"* but does not give a explicit negative constraint against writing out numeric counts for discrete countable objects (e.g., if there are 3 stars, do not label "3 stars").
* **Execution contract nuance in `coder` vs. `reviewer`:** `EXECUTION_CONTRACT` states that scripts *"must NOT set aspect ratios or hide axes themselves."* The coder set explicit mathematical limits (`ax.set_xlim`/`ax.set_ylim`) to establish the 5:3 coordinate bounding box, which caused slight hesitation in `syntax_check`.

---

### 4. System Improvement Suggestions (Trend & Cross-Run Context)

| Area | Suggestion | Cross-Run Impact / Trade-off Analysis |
| --- | --- | --- |
| **Markup Suppression** | Add an explicit heuristic to `plan_markings`: *"If discrete objects are explicitly depicted, non-overlapping, and intended for student counting, do NOT add text labels stating their count."* | **Positive:** Reduces Stage 2 token churn and prevents clutter in elementary counting problems.<br>

<br>**Risk:** In complex physics/geometry problems (e.g., scale bars, force vectors), agents might over-suppress necessary numeric annotations if not carefully scoped to countable sets. |
| **Execution Contract Clarification** | Clarify in `EXECUTION_CONTRACT` that `ax.set_xlim()` / `ax.set_ylim()` are permitted for data bounding, but `ax.set_aspect()` / `plt.axis('off')` are reserved for the wrapper. | **Positive:** Prevents reviewers from flagging valid bounding box setups as potential wrapper contract violations. |
| **Primitive Snippet Library** | Add standard Matplotlib `Path` formulas or reusable helper idioms (stars, regular polygons, crescent arcs) to coding reference context. | **Positive:** Saves generation time and token costs in `write_code` by avoiding manual re-derivation of parametric curve equations. |


Scaffolding Usable?: Yes
Illustration Usable?: Yes
Other notes: 
General Problem Class: 

**Run Evaluation & Post-Mortem Analysis**

---

### Run Summary & Outcome Assessment

* **Outcome:** Clean First-Pass Success ($0.27 total API cost across 19 execution turns).
* **Pipeline Path:** `STANDARD_DIAGRAM` (Stage 1 Validation $\to$ Stage 2 Planning $\to$ Stage 3 Matplotlib Scaffolding $\to$ Stage 4 Context-Preserving Img2Img $\to$ Stage 5 Visual QA $\to$ Stage 6 Reporting).
* **General Problem Class:** Discrete Probability Setup / Combinatorics (Discrete Counting & Grouping).

---

### What Went Wrong (Friction Points & Near-Misses)

While both the scaffolding and final illustration were fully usable and passed without triggering retry loops, several minor architectural and prompt-level frictions occurred:

1. **Redundant & Overlapping Marking Directives in Stage 2:**
* The `markup_specialist` attempted to inject text labels for discrete counts (e.g., `"1 Star"`, `"4 Moons"`), which was redundant and contradicted the core directive of avoiding visual clutter for easily countable objects.


2. **False Friction on Cosmetic Code Details:**
* The `reviewer` on `logic_check` flagged a 2-point font difference (`18pt` vs. `16pt`) as a `MINOR` discrepancy. While `review_manager` handled it correctly by dismissing it, reviewer time/tokens were spent on non-semantic styling.


3. **Redundant Visual Review Calls in Stage 5:**
* The pipeline executed four independent vision passes (`review_bias`, `review_aesthetics`, `review_safety`, `review_math`). For elementary object setups lacking humans or dangerous concepts, `review_bias` and `review_safety` were virtually guaranteed no-ops that added latency and token costs.



---

### Agent Involvement Breakdown

| Agent / Step | Role in This Run | Behavioral Notes |
| --- | --- | --- |
| **`problem_validation`** | Stage 1 Extraction & Conflict Check | Accurately split the math problem from the diagram request; correctly identified that no contradiction existed. |
| **`image_detail_planner`** | Stage 2 Management & Synthesis | Key stabilizing force: pruned redundant count labels suggested by `markup_specialist` and enforced a strict 2D layout. |
| **`coder` & `reviewer**` | Stage 3 Script Generation & Review | `coder` wrote modular vector path rendering inside the helper functions; `reviewer` and `review_manager` kept the pipeline on track. |
| **`artist`** | Stage 4 Img2Img Transformation | Accurately anchored the Matplotlib scaffolding coordinates while translating flat shapes into polished, tactile assets without hallucinating extra counts. |
| **`issue_aggregator`** | Stage 5 Final Gatekeeper | Unanimously released the render on attempt 1. |

---

### Root Cause Analysis in Agent Instructions

* **Instruction Siloing in Stage 2 Specialists:**
* In `plan_markings`, the instruction asks *"What labels, if any, would this sort of diagram normally have?"* without explicitly bounding it against discrete countable items. When given counters, the agent defaults to adding textual tallies.


* **Over-Index on Blueprint Literalism in `logic_check`:**
* In `logic_check`, the instruction states *"Compare the code against the Scaffolding Image Request"*, which leads the reviewer to treat non-functional scaffolding specs (like arbitrary font sizes or slight RGB shifts) as discrepancies rather than focusing strictly on topological and mathematical fidelity.


* **Fixed-Cost Static QA Fanout in Stage 5:**
* Stage 5 is hardwired to query four separate checks regardless of input content. For abstract discrete objects, bias/safety passes run identically to complex human scenarios.



---

### Suggestions for Pipeline Improvement

* **Tune `plan_markings` to Suppress Redundant Counter Labels:**
* Add a negative directive to `plan_markings`: *"If items are discrete, clearly visible, and under 10 in count, do NOT add numeric/text tallies (e.g., '3 Apples'); let the visual count convey the quantity unless explicitly requested."*


* **Calibrate `logic_check` Severity for Scaffolding:**
* Clarify in `logic_check` that scaffolding formatting (exact font sizes, minor padding deltas, non-semantic color shades) should not be flagged unless they directly cause overlap, clipping, or mathematical distortion.


* **Conditional / Combined Stage 5 Verification Passes:**
* For low-risk math diagrams (geometric shapes, counting blocks, abstract icons), combine `review_bias`, `review_safety`, and `review_aesthetics` into a single consolidated pass to shave ~15–20 seconds and token overhead off clean runs while keeping `review_math` isolated.


Scaffolding Usable?: Maybe - weird drop shadow things make shapes look messy/weird
Illustration Usable?: Yes
Other notes: scaffolding needs to be usable as final image; but shadows are probably fine to leave for artist.
General Problem Class: 

**General Problem Class**
Primitive Collision / Coordinate Under-Specification & Pseudo-Artistic Matplotlib Hacks (Fake Drop Shadows violating Standalone Scaffolding Rules).

---

**What Went Wrong**

1. **Geometric Overlap / Clipping (Iteration 1):** The initial bounding math for the block arcs failed to account for the object footprint ($50\times 50\text{ px}$) relative to container tops ($Y=220$) and adjacent shapes ($42\text{ px}$ delta between $50\text{ px}$ stars), causing immediate visual collisions.
2. **"Messy" Pseudo-3D Shadows in Python:** The coder manually faked a 3D drop shadow by layering offset solid polygons (`pts + [2, -3]`), creating jagged double-outlines and visual artifacts that degraded the base diagram's standalone legibility.
3. **Review Blindspot vs. Downstream Friction:** Pre-execution code reviewers missed the coordinate overlap because the code *syntactically* and *logically* mirrored the designer's flawed arc formulas, pushing error detection entirely onto visual inspection.

---

**Agents Involved**

* **`scaffolding_designer`:** Authored arc formulas with radii and centers that placed $50\text{ px}$ shapes too close to the bag bounding box and each other.
* **`coder`:** Injected ad-hoc polygon shadow duplicates to emulate "toy blocks" instead of keeping primitives clean, flat, and legible.
* **`reviewer` & `review_manager`:** Validated the script without checking geometric bounding box collisions.
* **`inspector` (`detect_artifacts` / `verify_adherence`):** Accurately flagged both collisions and double-outline artifacts.
* **`inspection_manager`:** Successfully resolved collisions via coordinate overrides, but dismissed the shadow artifacts by assuming the artist would fix it—violating the system's *Standalone Rule*.

---

**Root Causes in Agent Instructions**

* **Ambiguity in "Standalone Scaffolding" vs. "Aesthetics":** `coder`'s prompt encourages style planning ("colors, alpha, style strategy"), leading the model to attempt graphic design tricks (fake shadows/bevels) via code rather than sticking to clean vector primitives.
* **No Explicit Clearance Margin Rule:** `scaffolding_designer` lacks a concrete directive requiring explicit safety buffers: $\Delta \ge \text{width} + \text{margin}$ and $(Y_{\text{bottom}} - \text{Container}_{\text{top}}) \ge \text{margin}$.
* **Reviewers Relying on Semantic Fidelity Over Coordinate Arithmetic:** `logic_check` and `math_check` verify entity counts and variable assignments, but neither prompt requires bounding box collision checks before approving execution.

---

**Suggestions for Improvement & Systemic Trade-Offs**

* **1. Ban Procedural Shading/Shadow Hacks in `write_code**`
* *Change:* Add an explicit negative constraint in `EXECUTION_CONTRACT` / `write_code`: *"Do not attempt pseudo-3D drop shadows, multi-layer offset strokes, or fake bevels. Draw strictly clean, flat, single-layer 2D geometric shapes."*
* *Trade-off:* Maximizes clean standalone readability and eliminates false-positive artifact flags. However, pure 2D base diagrams may give the Stage 4 img2img model slightly less visual depth hint for textured objects.


* **2. Enforce Mandatory Clearance Math in `design_scaffolding**`
* *Change:* Instruct `scaffolding_designer` that whenever arranging objects above containers or in arcs, center coordinates must be spaced such that $\vert{}x_1 - x_2\vert{} \ge (\text{size} + 15\text{ px})$ and $y_{\text{center}} - \frac{\text{size}}{2} \ge y_{\text{boundary}} + 15\text{ px}$.
* *Trade-off:* Prevents 1st-turn collision retries. However, over-constraining the geometry prompt can make the blueprint verbose and rigid, occasionally compressing diagrams on small canvases.


* **3. Calibrate `inspection_manager` to the Standalone Mandate**
* *Change:* Clarify in `consolidate_inspection` whether aesthetic defects on primitives (like offset strokes) should trigger a code fix or an artist wave-through when the *Standalone Rule* is active.
* *Trade-off:* Strict enforcement causes additional code-retry cycles for cosmetic issues; lenient enforcement risks shipping messy base diagrams if the workflow ever routes directly or if img2img preserves the double-edges.


Scaffolding Usable?: Yes
Illustration Usable?: Yes
Other notes: 
General Problem Class: 

**General Problem Class:** Discrete Probability & Combinatorics (Discrete Object Counting & Container Allocations)

---

### Executive Summary

Overall, this run was a **clean success**: the pipeline took the `COMPOSITE_PRIMITIVES` route, successfully plotted custom geometric vectors for stars, crescent moons, and pouches, and rendered a mathematically accurate ($P(\text{Bag 1}) = 1\text{ star}, 4\text{ moons}$; $P(\text{Bag 2}) = 3\text{ stars}, 1\text{ moon}$) final image without triggering retry loops.

However, a subtle prompt-architecture leak occurred: **hallucinated/spurious critique regarding `ax.axis('off')**` surfaced in downstream reports due to asymmetrical injection of the execution contract.

---

### What Went Wrong & Friction Points

* **Spurious Defect Flagging (`ax.axis('off')`):** `inspection_manager` recorded a note that *"Axes borders and ticks may be visible since `ax.axis('off')` was omitted..."* which then caused `final_reporter` to generate a developer suggestion to enforce `ax.axis('off')`. In reality, the execution wrapper automatically strips spines and ticks post-execution. Omitting it was the *correct* specified behavior.
* **Redundant Description Polish Loop:** `image_detail_planner` rejected its own first description draft in `review_description` (`ready_for_artist: false`) solely to append a minor sentence about relative block scaling (~1/4 pouch width), consuming an extra slow-tier round trip for a cosmetic refinement.

---

### Agents Involved

| Agent | Task | Role in Issue |
| --- | --- | --- |
| **`inspection_manager`** | `consolidate_inspection` | Inferred a non-existent flaw from code inspection because it lacked the environment execution contract. |
| **`final_reporter`** | `generate_archival_report` | Amplified the spurious critique into an actionable system suggestion. |
| **`image_detail_planner`** | `review_description` | Triggered a self-critique loop over trivial prose refinement rather than structural defects. |

---

### Root Causes in Agent Instructions

* **Contract Asymmetry Across Lifecycle:** `${EXECUTION_CONTRACT}` is injected into `reviewer` and `review_manager`, but omitted from `inspector`, `inspection_manager`, and `final_reporter`. When `inspection_manager` evaluated the coder's output, it defaulted to standard Matplotlib knowledge where omitting `axis('off')` leaves visible ticks.
* **Overly Strict `ready_for_artist` Gate:** In `review_description`, the prompt instructs the model to set `ready_for_artist = false` if there are *any* flaws. Because LLMs are biased to find room for improvement when asked to critique, it generated a micro-fix rather than accepting a functionally complete prompt.

---

### Suggestions for Improvement & Systemic Tradeoffs

* **Inject `EXECUTION_CONTRACT` into Inspection & Reporting Personas**
* *Fix:* Add `${EXECUTION_CONTRACT}` to `inspection_manager` and `final_reporter` (or fold the environment rules into `GLOBAL_TASK_EXPLANATION`).
* *Tradeoff:* Slightly increases prompt token counts across all turns, but prevents downstream managers from diagnosing phantom bugs or escalating false alarms.


* **Thresholding `ready_for_artist` in Description Review**
* *Fix:* Reframe `review_description` to reject only on *blocking omissions* (missing object counts, conflicting constraints, lack of layout/colors) rather than minor stylistic prose additions.
* *Tradeoff:* Reduces slow-tier API latency and cost on simple prompts, but risks passing slightly vaguer descriptions on edge-case complex queries.


* **Pre-compiled Primitive Library in Coder Blueprint**
* *Observation:* The coder wrote custom Bézier curves for crescent moons and pouches from scratch. While it succeeded here, complex path math in Matplotlib is a frequent source of syntax/coordinate failures in other runs. Providing boilerplate path snippets in `plan_logic` / `write_code` prompt libraries for common textbook primitives (cards, urns, dice, spinners, coins) prevents code-level drift.


Scaffolding Usable?: Yes
Illustration Usable?: Maybe - dotted line within bag is confusing/distracting
Other notes: was [minor] dotted line artifact noticed by visual agents?
General Problem Class: 

## General Problem Class

**Extraneous Decorative Scaffolding / Visual QA Artifact Blindspot**

## Summarize and Analyze

### What Went Wrong

The `coder` proactively added a decorative dashed line to the base diagram to represent "stitching" on the bags, exceeding its mandate to provide purely geometric scaffolding. Because the `artist` prompt strictly anchors the geometry of the base diagram, this unrequested dashed line was locked in and forced into the final render. Rather than looking like subtle stitching, it manifested as a confusing, distracting dotted line.

To answer your specific question: **No, the visual QA agents completely missed it.** The `inspector` (`detect_artifacts`) and the `image_verifier` (`review_aesthetics`) both filed "No issues found" reports.

### Which Agents Were Involved

1. **`coder` (Root Error):** Authored the unrequested decorative geometry (`ls='--'` in the `draw_canvas_bag` function) instead of sticking to the basic trapezoid outline requested by the blueprint.
2. **`inspector` / `inspection_manager` (Missed Catch):** The `verify_adherence` and `detect_artifacts` tasks failed to flag the unrequested dashed line in the base diagram.
3. **`artist` (Propagation):** Faithfully locked in the geometry of the dashed line as instructed by the `plan_finishing` prompt ("lock the exact geometry... do not warp straight lines").
4. **`image_verifier` / `issue_aggregator` (Missed Catch):** The `review_aesthetics` and `review_safety` tasks viewed the final rendered dotted line as a deliberate artistic choice rather than a distracting artifact.

### Root Causes in Agent Instructions

* **The `coder`'s Scope is Too Loose:** While the `coder` is told to "Focus ONLY on drawing the mathematical shapes," it is not explicitly forbidden from hallucinating decorative features. In the absence of a strict negative constraint, the LLM defaulted to its inherent behavior of trying to be "helpful" by making the base diagram look more like a real bag.
* **The `inspector` Assumes Good Intent:** The `detect_artifacts` prompt specifically looks for technical failures like "blank images," "distorted aspect ratios," and "axes/grids." It does not check for rogue, unrequested geometry. Because the dashed line was cleanly rendered, the inspector didn't recognize it as an "artifact."
* **The `image_verifier` Lacks Context on Intent:** The `review_aesthetics` prompt looks for generated glitches (blur, distortions, AI melting). Because the dashed line was hard-coded into the ControlNet/img2img anchor, it rendered cleanly. The verifier has no way to know that a cleanly rendered dotted line is mathematically/pedagogically confusing.

### Suggestions for Improvement

* **Update the `coder` Prompt (Strict Negative Constraint):**
* *Suggestion:* Add a directive stating, "DO NOT add decorative flourishes, textures, or secondary physical details (e.g., stitching, shading, patterns) unless specifically requested by the blueprint. The artist will add all beauty and physical realism; you must draw only the bare minimum structural outlines."
* *Trend Warning:* Be careful not to make this constraint so strict that the coder refuses to draw mathematically necessary dashed lines (e.g., altitudes in triangles or hidden edges in 3D geometry). Frame the ban strictly around *decorative/physical* details.


* **Update the `verify_adherence` Prompt (Scope Creep Check):**
* *Suggestion:* Add a specific check to the `inspector`: "Did the coder add any rogue decorative elements, shapes, or lines that were NOT in the Scaffolding Blueprint? Base diagrams must be structural only."


* **Update the `plan_finishing` Prompt (Artist AI Guidelines):**
* *Suggestion:* While you want to lock the exact geometry of mathematical graphs, you might want to give the `artist` agent slight leeway for physical objects. Modifying the anchor directive to say "lock the mathematical geometry, but seamlessly integrate physical boundaries" might prevent rigid scaffolding lines from ruining organic textures, though this risks the image model warping necessary proportions.


Scaffolding Usable?: No - it looks like a trash bin and the dotted line doesn't serve a purpose
Illustration Usable?: Yes
Other notes: object count labels unnecessary
General Problem Class: aesthetics poor

### What Went Wrong

1. **Scaffolding Failed the "Standalone Rule":** The programmatic base diagram drew rigid trapezoidal polygons with top ellipses and an overhanging horizontal dashed line. Rather than resembling a fabric bag, the geometry read like an open trash bin or bucket with an artifact line across the rim. While the downstream image-to-image step recovered the visual, the scaffold failed the requirement that it must stand on its own as a legible diagram.
2. **Redundant Markups Generated:** Redundant bottom captions (`1 Star, 4 Moons` and `3 Stars, 1 Moon`) were planned and plotted directly beneath diagrams where the counts were already distinct, non-overlapping, and immediately countable.
3. **Manager Overruled a Legitimate Aesthetic/Artifact Defect:** The `inspection_manager` dismissed the `detect_artifacts` inspector's rejection by rationalizing the dashed line as an intentional "drawstring," failing to recognize that the coder drew a crude, unconstrained line that actively degraded the scaffold's aesthetics.

---

### Agents Involved

* **`markup_specialist` (Stage 2):** Introduced the redundant count captions (`1 Star, 4 Moons` / `3 Stars, 1 Moon`).
* **`image_detail_planner` (Stage 2 - `merge_plans` / `review_description`):** Failed to filter out the redundant labels despite instructions warning against overzealous markup.
* **`scaffolding_designer` (Stage 3):** Prescribed an abstract geometric container (trapezoid polygon + top ellipse) without specifying bag-like features or delegating the container to the artist.
* **`coder` (Stage 3):** Implemented the bag as a bucket/can geometry and hardcoded a dashed line (`plot([center_x - 1.6, center_x + 1.6], ...)`) extending past the container's body bounds (`center_x ± 1.5`).
* **`inspection_manager` (Stage 3):** Overruled the `inspector`’s valid artifact flag, approving an unusable standalone scaffold because it assumed the artist would paint over it.

---

### Root Causes in Agent Instructions & Prompts

* **Over-Specification by Abstract Primitives:** `scaffolding_designer` instruction 3 states: *"Abstract Complex Objects... you do not plot a farmer... You generally do not need to draw objects that are not part of the math problem, the artist can handle them."* However, the prompt does not clearly define where to draw the line for *containers*. When an object is just context (a bag holding countable items), trying to draw it in Matplotlib with basic polygons often produces unnatural, rigid shapes (bins, cans) rather than clean diagrams.
* **Lack of Container Primitive Guidance in Coder:** The `coder` prompt lacks guidelines or parameterized presets for common real-world containers (bags, boxes, jars, bowls), leading the model to construct awkward primitives from scratch.
* **Label Bias in `markup_specialist`:** The instruction asks *"What labels, if any, would this sort of diagram normally have?"* This frames label creation as expected rather than exceptional, despite later saying *"Does it actually warrant labels?"*
* **Loose Manager Leniency ("Scaffolding Bias"):** `consolidate_inspection` explicitly tells the manager: *"Remember this base diagram is scaffolding: a professional artist will paint the final illustration over it. Cosmetic blemishes the artistic pass will cover don't matter..."* This directly contradicts the **STANDALONE RULE** injected in `scaffolding_manager` (*"the scaffolding... must stand on its own — good enough to ship as the final illustration if the artistic pass added nothing"*). The manager used the "artist will fix it" escape hatch to wave through poor code.

---

### Suggestions for Improvement & System Trade-Offs

| Intervention | Potential Benefit | System Trade-Off / Risk to Monitor |
| --- | --- | --- |
| **1. Enforce a Strict "No Redundant Count Labels" Rule**<br>

<br>Instruct `markup_specialist` and `image_detail_planner` that if discrete objects are rendered cleanly and countable by sight, text captions stating the count are strictly forbidden unless requested in the original prompt. | Eliminates clutter and prevents diagrams from duplicating what the visual already conveys. | **Under-labeling risk:** On problems with dense object counts ($N > 10$) or where items represent grouped place values/units, agents might omit necessary clarifying callouts. |
| **2. Container Abstraction Policy for `scaffolding_designer**`<br>

<br>Clarify whether containers (bags, baskets, boxes) must be plotted in Python. If exact container boundaries aren't part of the math geometry, plot the grouped objects in bounded clusters/regions and let Stage 4 draw the container, OR use a standard bounding box/dashed grouping region. | Prevents the coder from writing awkward polygon approximations for organic objects like cloth bags. | **Composition drift:** If the scaffold doesn't provide container bounds, the img2img model might place items loosely on the canvas rather than inside a distinct container. |
| **3. Align `consolidate_inspection` with the Standalone Rule**<br>

<br>Remove the instruction telling the manager that cosmetic blemishes don't matter because the artist will cover them. Direct the manager to judge whether the base diagram is acceptable as a published line drawing. | Forces retries when the coder generates crude, unnatural, or artifact-heavy geometric primitives. | **Retry loop inflation:** May increase API costs and latency by triggering retries on minor aesthetic flaws that the artist model effortlessly resolves anyway. |
| **4. Bounding Box & Coordinate Clamping in Coder Directives**<br>

<br>Add an explicit rule to `coder` and `reviewer`: any decorative line, accent, or patch must not extend outside its parent object's calculated bounding box without explicit coordinate clipping. | Fixes line overshoots (like the overhanging drawstring) programmatically. | Minimal downside; slightly increases coder token usage. |


Scaffolding Usable?: Yes
Illustration Usable?: Yes
Other notes: 
General Problem Class: 

Here is a comprehensive summary and analysis of the run:

### Run Overview & Problem Classification

* **General Problem Class:** Discrete Probability / Combinatorics (Discrete Object Set Comparison with Exact Counts)
* **Outcome:** Success on retry (Scaffolding: **Usable**, Illustration: **Usable**)
* **Workflow Route:** `STANDARD_DIAGRAM` (Scaffolding pass $\rightarrow$ Img2Img finishing pass)

---

### What Went Wrong

1. **Missing Coordinate Offset Bug in Python Script:**
* In the first execution of `write_code`, the coder defined `draw_block` to render the `moon` primitive using relative coordinates (`y_inner` and `y_outer` from $-0.15$ to $0.15$). It forgot to add the vertical anchor offset `cy` when concatenating (`y_moon = np.concatenate([y_inner, y_outer])`).
* This caused all cyan moon icons across both bags to render along the bottom edge of the canvas (near $Y=0$, underneath the caption text) rather than inside the wooden block tiles at $Y \approx 3.9$, leaving the block tiles blank.


2. **Review Chain Blind Spot:**
* `syntax_check`, `logic_check`, and `math_check` all reported "No issues found" and gave passing recommendations on Attempt 1.
* `review_manager` passed the code without catching that the generated polygon points were anchored at the wrong Y-coordinate.


3. **Detection and Remediation:**
* The visual inspection stage (`verify_adherence`, `detect_artifacts`) correctly flagged the misplaced moons as a `CRITICAL`/`MAJOR` flaw.
* `inspection_manager` and `troubleshoot_visual` diagnosed the exact line/math failure and passed actionable instructions to `coder`, which fixed it on Attempt 2.



---

### Agents Involved

| Agent | Role in Incident |
| --- | --- |
| **`coder`** | Introduced the bug by neglecting the coordinate translation offset $cy$ in custom parametric shape math. |
| **`reviewer`** (`logic_check` / `math_check`) | Missed the coordinate bug during code review, verifying loop counts and high-level structure without sanity-checking relative-to-global point transformations. |
| **`review_manager`** | Relied on reviewer passes and approved flawed code into the execution pipeline. |
| **`inspector`** (`verify_adherence` / `detect_artifacts`) | Successfully identified the visual defect (blank blocks, floating moons at bottom). |
| **`inspection_manager`** & **`error_expert`** | Pinpointed the exact code bug ($+cy$ missing) and cleanly directed the retry. |

---

### Root Causes in Agent Instructions

* **Coder Instruction Lacks Relative-vs-Global Coordinate Guardrails:**
* The `write_code` instruction heavily emphasizes scoping restrictions for `exec()` (passing arguments and local imports), but lacks an explicit reminder that **all parametric/procedural polygon vertices must be translated by their anchor coordinates `(cx, cy)**`.


* **Reviewer Instruction Over-Indexes on Logic Counts over Math-in-Code:**
* The `math_check` prompt asks to *"Recompute every quantity the code hard-codes (coordinates, lengths, angles, counts...)"*. In practice, LLM reviewers check the macro-level constants (e.g., "draws 4 moons at X=1.9, 2.5...") but gloss over the micro-level vertex math inside procedural helper functions.


* **No Primitive Library / Reusable Path Standard:**
* When agents are forced to compute custom trigonometric and polynomial curves on the fly (e.g., moon crescents from scratch), coordinate translation bugs frequently occur.



---

### Suggestions for Improvement & Trend Analysis

* **Inject a Standard Parametric Anchor Checklist into `coder`:**
* Add a concise directive under `STRICT CONSTRAINTS` in `write_code`:
> *"When procedurally generating custom shapes (stars, crescents, arcs), verify that every vertex $(x, y)$ explicitly includes the center offset $(cx, cy)$ so elements do not collapse to $(0,0)$."*




* **Tighten `math_check` / `logic_check` for Procedural Helper Functions:**
* Add a specific check to the reviewer prompt:
> *"Inspect helper functions for local-to-global coordinate transformations: are all inner polygon points properly offset by the function's anchor arguments?"*




* **System-Level Tradeoff Awareness (Macro vs. Micro):**
* *Risk:* Adding too many granular shape-drawing rules can bloat system prompts and cause the coder to overcomplicate simple matplotlib primitives.
* *Trend to Monitor:* Track whether mathematical/geometric errors in Stage 3 arise from **canvas-level layout logic** (e.g., overlapping boxes) or **primitive-level vertex math** (e.g., custom polygons). If vertex math is the primary failure mode across runs, providing a canned snippet library for common non-standard shapes (moons, stars, gears, pointers) via `config.directive_library` will eliminate code hallucination without increasing prompt fragility.


Scaffolding Usable?: No - placement of moons weird; shape of moon weird
Illustration Usable?: maybe - moons hang over lip of bag, looks weird
Other notes: 
General Problem Class: aesthetics poor

Here is an analysis of the pipeline failure based on the execution history and user feedback.

## Breakdown of the Failure

* **Malformed Primitives:** The Matplotlib script generated an awkward, misshapen crescent moon by attempting to use a custom quadratic bezier curve rather than reliable, standard geometric overlaps.
* **Unnatural Placement:** The moons were placed exactly on the y-coordinate of the bag's top rim. Because they were mathematically centered on the edge, they appeared to hover or hang over the lip unnaturally.
* **Flaw Propagation:** The inspection team hyper-focused on exact block counts and bounding-box overlaps. They completely ignored the deformed shapes and physics-defying placement, passing the flawed scaffolding to the artist, which then locked the geometry into the final render.

## Agents Involved

* **`coder`:** Hand-coded the error-prone bezier crescent geometry and selected the awkward vertical coordinates for the layout.
* **`inspector` & `inspection_manager`:** Successfully flagged and fixed object overlap, but failed to catch the aesthetic degradation and physical implausibility of the layout.
* **`artist`:** Followed its strict directives to anchor the scaffolding's spatial relationships perfectly, preserving the weird placement in the final image.

## Root Causes in Instructions

* The `coder` lacks directives advising it to rely on standard, stable Matplotlib patches (like `Wedge` or boolean operations) for basic shapes, leaving it prone to drawing deformed custom paths.
* The `inspector` prompts (`verify_adherence`, `check_overlaps`) do not ask the vision model to evaluate physical plausibility, gravity, or basic shape integrity.
* The `scaffolding_designer` and `coder` instructions lack guidelines on how items should logically interact with containers (e.g., objects should be clearly inside, resting beside, or spilling out).

## Suggestions for Improvement

* **Primitive Standardization:** Update the `coder` execution constraints to strictly prefer standard `matplotlib.patches` over custom bezier math when constructing simple geometric primitives.
* **Physical Sanity Checks:** Add a directive to the `inspector` and `scaffolding_designer` prompts to ensure objects obey basic spatial logic (e.g., verifying that items inside a bag don't balance perfectly on its mathematical edge).

Want me to draft the specific prompt updates for the `coder` and `inspector` agents to prevent this in future runs?


Scaffolding Usable?: Yes
Illustration Usable?: Yes
Other notes: labeling things we can see just by looking is redundant
General Problem Class: 

**General Problem Class**
**Visual Redundancy / Over-Labeling of Directly Observable Attributes**

---

### What Went Wrong

1. **Redundant Content Labels:** The final illustration and scaffolding explicitly labeled the contents below each bag (e.g., `"1 Star, 4 Moons"` and `"3 Stars, 1 Moon"`). Because the blocks are few in number, non-overlapping, and visually distinct (yellow stars vs. cyan moons), these text labels state what the student can already see at a glance.
2. **Pedagogical Hand-Holding:** By explicitly writing out `"1 Star, 4 Moons"` directly next to the bag, the diagram reduces the need for the student to actively observe and count the initial setup before constructing their tree diagram.
3. **Scaffolding/Visual Coordinate Mismatch (Initial Attempt):** During Stage 3, `scaffolding_designer` set block row coordinates at $y = -0.3$ and $y = 1.2$ inside a bag with an opening at $y = 0.5$, causing the bottom blocks to draw across the front patch rather than inside/above the opening. While resolved via retry, it consumed unnecessary execution turns.

---

### Agents Involved

* **`markup_specialist` (Stage 2):** Introduced the redundant labels. In `plan_markings`, it recommended adding explicit content labels: `"1 Star, 4 Moons"` and `"3 Stars, 1 Moon"`.
* **`image_detail_planner` (Stage 2):** Failed to filter out the redundant labels in `merge_plans` and `review_description`, even though its instructions explicitly caution against overzealous markup.
* **`scaffolding_designer` & `coder` (Stage 3):** Encoded the redundant text labels directly into the blueprint and Matplotlib script (`ax.text(..., '1 Star, 4 Moons')`).
* **`artist` (Stage 4):** Anchored on the text from the scaffolding and faithfully retained the redundant text labels in the final render prompt and generation.

---

### Root Causes in Agent Instructions

1. **Weak Threshold in `plan_markings` Instruction:**
* *Current text:* `"What labels, if any, would this sort of diagram normally have? The student can see both the original problem and the diagram. Does reading the problem already convey all the information we need without labels? Does it actually warrant labels?"*
* *Root cause:* The instruction frames the check primarily against the *word problem*, not against the *immediate visual legibility of the objects themselves*. When asked to define markups, the specialist defaults to adding summary labels if it cannot think of geometric markings (like angle arcs or measurement bars).


2. **Passive Synthesis in `merge_plans`:**
* *Current text:* `"Don't add extraneous labels just because the markup specialist wants to. A group of mice doesn't need to be labeled 'mice', if that's readily apparent at a glance."*
* *Root cause:* While the guideline exists, `image_detail_planner` treats specific item counts/sub-labels as "educational clarity" rather than clutter because no negative rule explicitly forbids restating visible discrete object counts.


3. **Container vs. Inset Coordinate Blindspot in `scaffolding_designer`:**
* The designer prompt lacks an explicit layout calculation check to ensure inner objects are bounded strictly by container openings (e.g., $y_{min\_object} > y_{top\_rim}$ if placing objects floating above, or proper masking/clipping if resting inside).



---

### Suggestions for Improvement

* **Add an "Observable Attribute" Negative Constraint to `plan_markings` & `markup_specialist`:**
* Explicitly instruct: *"Do NOT generate text labels that merely recount or name countable, distinct objects that are already visually obvious (e.g., do not add '3 Blue Balls', '1 Cat', '4 Stars'). Restrict labels to container identifiers ('Bag A'), mathematical dimensions/variables ($x$, $5\text{ cm}$), or abstract axes."*


* **Tighten `merge_plans` Pruning Rules:**
* Add a specific check to `image_detail_planner`: *"Strip out any secondary sub-labels that narrate visual counts. If an object group has an identifier (e.g., 'Bag 1'), do not append count subtitles beneath it."*


* **Systemic Balance Consideration (Cross-Run Impact):**
* *Tradeoff:* For large sets ($N > 10$) where exact counting is tedious or impossible to depict discretely (e.g., a bag of 50 marbles), summary count labels are necessary. The rule must specify: **Do not label counts if individual items are explicitly drawn and intended to be counted ($N \le 10$). Only use count labels when items are abstracted/clumped.**


* **Bounding Check in `scaffolding_designer`:**
* Add a constraint verifying that when items are placed "inside or above an open container," their $(x, y)$ bounding boxes do not cross the polygon boundaries of the container's front wall.


Problem 5 user notes:
- A lot of these turned out well.
- Figuring out how to depict items inside a bag gave the system some trouble
- there should not be object counts as labels. If the math students can't count, learning how should be part of the problem.


**Problem 6**


**Problem 7**


**Problem 8**


**Problem 9**


**Problem 10**
