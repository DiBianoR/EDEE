🔜=priority  🛠=WIP  🌗=partway  ✔=Done

✔- improve aesthetic instructions - images are ugly, or follow original too much rather than morphing [artist: plan_finishing, render_final]
  - do some manually and use what we learn on artist
  ✔- look up best practices
✔- improve aesthetic review, concentrating on things other than beauty atm, downside is it's costly if we have to redraw [image_verifier: review_aesthetics]
✔- enforce shapes better [dimension_expert: estimate_dimensions, 3d_specialist: plan_3d, image_detail_planner: merge_plans, review_description, scaffolding_designer: design_scaffolding]
✔- too much information - some give away solutions / give hints, or restate on a label what you can see at a glance [markup_specialist: plan_markings, image_detail_planner - merge_plans, review_description]
- labels can be close to image edge, some kind of algorithmic final check maybe - ok for textbook? but looks bad in image
✔- ruler / tape measure specific rules
✔- problem 1 - mice should not always be in separate groups, or a grid, or have a dotted line between groups
✔- "highly customizable products", cereal, soda, t-shirts, video games, tv show etc.
✔no shadows in scaffolding
✔no redundant labels
✔fix misrouted as artistic image
✔artist creativity step
grids

automate review & correction process

- poor prompts
 - screen prompt / ask for feedback / improvement
 - Meta-Prompting / Prompt Expansion / Prompt Augmentation
Streamline workflow states - The Standard "Core 8" States (status) + sub-state (step)
It would be more efficient to stack many constrained fields in one agent rather than have multiple agents, any time information crossover isn't an issue(or is needed), we should do this more.

remove redundancy between consolidate_* and troubleshoot
✔revamping/scoping phase 4-5 properly
special instructions for rulers & tickmarks

✔Prompt events aren't replayed. Node 1 logs every prompt with author: "system", and scoping is by author, so putting "system" in any scope pulls in every system prompt from the whole run. And we need alternating prompts so either way it's illegal right? I think the desired behavior is only include user prompts that are in scope if the reply is also in scope. And if we have replies next to each other because we decided to scope-out prompts; we need to squash everything into one turn like we are already doing in some cases.

✔I want a way to label user prompts author as "user" as well as "system"; depending on whether our system or the end user originated them

✔QUEUE PROCESSING & CONTENT FLUSHING was supposed to reconstruct multi-turn history, but does it handle json responses properly? If it's a single response, I think the json is fine, but if it's part of a multi-turn flattened into one user prompt, how should we display it?

✔Cost counter
sharply cut run costs
  ✔cheap model by default
  🛠less context
    🛠scope history much tighter!
    ✔ADK-style session.events(history), session.state?
  ✔ChatGPT for image gen
  see if we can deny any loops without losses

work on managerial agents
  ✔As a manager type, error_expert should get a smarter model. Look for other manager types as well and see that they are at least medium.
  ✔Use some methods from Gemini chain-of-thought samples
  🛠go through and limit scope of all agents, put managers anywhere a lot of data needs to be synthesized, or jobs need to be re-planned after errors.
  🛠examine the workflow around each manager & apply DIRECTIVE_MANAGER if it makes sense

chance to run without error 70%->90%
  good-enough result still beats a crash; cutting edge agents just give an apology too
  check logs for common failure modes
  threaten/pressure managers for results	
  check in with user for feedback
  allow manager to modify query after failure[maybe; this is what a human would do]

User Feedback
  choose between scaffolding or final
  chance to give feedback after scaffolding, and again after final
  multiple(3) scaffolding images -> multiple final images
  
🛠Empirical results - Would like 100 sample images of each sample problem, cost permitting. 10 of each is more realistic at current pricing.
  ✔Batch processing

Field test and tune: propose_diagram
Get COMPOSITE_PRIMITIVES branch up and running

🌗Add capabilities universal agent
  ✔Route workflow(decide which branch to take)
  ✔Close workflow(return success/failure and an explanation)
  Pause workflow(wait for real time user feedback)
  2-part tasks, ex. analysis with slow agent, reprint full code with fast agent
🌗Work on failure review agents’ logic, keep them from wasting time in loops
  ✔Update logic from my notes
  ✔Limit retries on the same task to 3 max everywhere
  Decide where review agents should just attempt to patch on the fly rather than full rewrite.
Fallback models, for model unavailability(quota, outage, etc.)
  ✔Toggle to use fast model
✔Git with
  Example results
  Editable agent definitions
Job browser in frontend
  Short name for each job
Let managers have more autonomy
  ✔DIRECTIVE_MANAGER="You are a manager. Your job is to review, synthesize, remove redundancies, review subordinates' decisions, address concerns, resolve conflicts/contradictions, double check correctness, and make the final call."
  Failure loop detection and handling
  Let them choose who to send the task back to in case of error
  It would be nice for managers to be able to forcibly send an error or instructional message to any other agent; even if the manager is normally not listed in their scope. It might need to decide whether to send the message, where to send it, and what to send; so this might take up to 3 additional return fields, plus a special event type, so one more field in events. It might be easier to just always put the manager on a subordinate's scope. Might managers need to message each other?
  Second guess subordinates, check code against visual reports, ask subordinates to be sure if their findings don’t make sense
  Give them authority to upgrade model used by subordinate
  Give them authority to allow a sub-par product through instead of causing a failure
  Let them try to consult user during a job(with a timeout)
  ✔Let them leave warning and error messages for user if there is an issue
  ✔Let them write to a log about issues they noticed with subordinates, for iterative process improvement later.

Matplotlib's default figure 4:3 aspect ratio. -> toggle or have system choose between 4:3 and square images

Agent Improvement/fixes
finish DIRECT_IMAGE_GEN route
New situational planning
  Isolate 3d/2d/isometric early
    Fix wording throughout the workflow
  Distinguish between simple and complex 3d objects
Study and integrate paper banana - feature matching
Test how well image readers perceive different issues
Ask grad students to list failure modes
Revise System Architecture and Components - finish top level add sub diagrams / details
OpenAI text prompts. Right now openai is img2img only(/v1/images/edits), which takes a flat prompt string, so Node 1's forceFlatten squashes all history into one labeled user turn. To use openai for text/view_img we'd need a contents->messages translation(role model->assistant, systemInstruction->system message, inlineData->image_url parts) alongside the existing image translation. forceFlatten is already gated on modelType==="img2img" so it stays off for the chat path; the queue processor would emit proper alternating turns as-is.
Experiment with coteach
  - Closed source vs. our open source
  - Limited application to proprietary materials
  - No information suggesting they are pursuing grants
  - No information suggesting they are pursuing research into effectiveness, etc.
  - Quick development suggests lack of barriers to develop similar features
